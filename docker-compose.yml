services:
  backend:
    build:
      context: ./backend
    ports:
      - "8000:8000"
    environment:
      DEEPSEEK_API_KEY: ${DEEPSEEK_API_KEY:-}
    volumes:
      - ./backend/data:/app/data
    restart: unless-stopped
