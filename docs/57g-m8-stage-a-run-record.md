# 57g M8：第四次验证阶段 A 的运行回执

> 日期：2026-09-23。
>
> 本文件接在 `docs/57f` 之后，沿用 `docs/30` §10.1 D6 的编号族约定。`docs/20` 至 `docs/57f` 未被改写。
>
> 本文件记录：`J3` 第 1 步（阶段 `A`）的运行命令、实测读数与估时对照。
>
> **本篇不给出任何质量结论**：阶段 `A` 的 `completed` 与 `violations=[]` 是**机制与门禁**结论，**不是**质量证据。本篇**不做任何判据计算**。

## 1. 授权原文与边界

用户答复原文：

> 「A. 授权阶段 A（跳过 A0，照上表参数与包络）」

| 项 | 内容 |
|---|---|
| 被授权 | 单独一轮运行阶段 `A`（清单已冻结；跳过可选的 `A0`） |
| 明确排除 | 确定性补算；阶段 `B`；改判据 / 门槛 / 机会分母 / 估值块 / 区间方法；默认切换；任何重跑 |
| 包络 | wall ≤1800 s；RSS ≤1 GiB；输出 ≤100 MiB；单进程不并行 |
| 未消耗 | 未消耗补算与阶段 `B` 的授权；`docs/37` §3.1 **未触发** |

## 2. 只读基线核验（运行前）

```text
$ git status --short --branch
## master...origin/master

$ git rev-parse HEAD
1d3666521fb57273d3778ab964e516a1644f1517

$ find /Users/bryanylliu/holdem-mixed-bot-validation-v8 -type f | wc -l
1（仅冻结清单）
```

工作区**零改动**。v7 七份工件 sha256 在运行前后**逐字未变**。

## 3. 运行命令与工件

```text
$ uv run python -m tests.mixed_bot_validation \
    /Users/bryanylliu/holdem-mixed-bot-validation-v8/input/frozen-fixture-manifest.json \
    A /Users/bryanylliu/holdem-mixed-bot-validation-v8/runs/stage-a \
    --allow-stage=A --family-set=iqv --emit-observations
（退出码 0；stdout 重定向到 /dev/null，读数取自落盘工件）
```

| 工件 | sha256 | 大小 |
|---|---|---|
| `<v8>/input/frozen-fixture-manifest.json`（未改动） | `484a6ce83687be436a2676081c6f9c35a7deed5c4e867b9bb95eec868141a225` | `215,371 B` |
| `<v8>/runs/stage-a/mixed-validation-stage-A.json` | `b96420985ba2e0a16e76c892f4e7895ec62f2ac384b30cc0d0379b0601ce3319` | `10,120,702 B` |
| `<v8>/runs/stage-a/mixed-observations-stage-A.json` | `f776ab7cce242a5f191fd78973c02d2d0648d9f6e65ee4e0637004e22eac8c0a` | `179,574 B` |

**首跑成功**：本轮阶段 `A` **一次通过**，未发生失败重跑（v7 的阶段 `A` 与补算**各失败过一次**，本次未复现该类缺陷）。

## 4. 实测读数

### 4.1 身份与自洽

| 项 | 读数 |
|---|---|
| `schema_version` / `stage` / `status` | `mixed-validation.v2` / `A` / `completed` |
| `violations` | `[]` |
| `strategy_id` | `mixed-local@6` |
| `config_digest` | `b6678f65069c19779f683bd6a8df31f7bc3567cf813654d2758278c13151b4a1` |
| `manifest_digest` | `0a1b1ea8985dfbd0f9d5ddeeedbca53e348c97ca0f5c31d9f2273e87f912e8f6`（与冻结清单一致） |
| `code_identity` | `22e669a879acbf862aa8eb8953ca6fa90c29bf2c`（**冻结时的预承诺值**，见 §6） |

### 4.2 覆盖

| 项 | 读数 |
|---|---|
| 成本格 / 成本样本 | `288 / 288`、`288 / 288` |
| 补充场景 | `24 / 24` |
| 对抗对照 | `128 / 128` 手 |
| `executed_nodes` / `planned_nodes` / `not_run_nodes` | `125` / `128` / `0`（`executed_nodes` 为**清单库存口径**，不是本轮执行数） |
| 结构性不适用 | `3` |
| 截断手数 | `0` |

### 4.3 决策路径（阶段 `A` 样本数 `288`）

| 项 | 读数 |
|---|---|
| 超 `100 ms` 次数 | **`0`** |
| p50 / p95 / p99 / max | `0.2841` / `0.4494` / `0.8614` / `0.9548` ms |
| 逐人数分组 | `2–9` 人各 `36` 样本，各组超时 **`0`** |

### 4.4 逐节点明细与对抗对照

| 项 | 读数 |
|---|---|
| 逐节点行数 | **`18,000`**（`125` 节点 × `3` 风格 × `3` 手模式 × `16` 种子块） |
| 行标识后缀 | 全部以 **`-iqv3`** 结尾 |
| 种子块 | **`16`** 个，与清单种子**逐字一致** |
| 对抗对照 | `completed_hands = 128`、`planned_hands = 128`、`truncated_hands = 0`；`interval_status = not-estimated-small-fixed-seed-set` |

### 4.5 资源（包络为**人工比对**，代码不据此改 `status`）

| 项 | 读数 | 包络 | 余量 |
|---|---|---|---|
| wall | **`3.949 s`** | ≤1800 s | 远未触及 |
| CPU | `3.884 s` | — | — |
| 峰值 RSS | **`73.22 MiB`**（`76,775,424 B`） | ≤1 GiB | 远未触及 |
| 输出 | 报告 `10,120,702 B` + 观测 `179,574 B` ≈ **`9.82 MiB`** | ≤100 MiB | 远未触及 |
| 并发 | `single_process=true`、`parallel=false` | 单进程不并行 | 满足 |

## 5. 估时对照（估算 vs 实测）

| 项 | 运行前估算（`R3`） | 实测 | 说明 |
|---|---|---|---|
| 阶段 `A` wall | `5 s` 量级 | `3.949 s` | 同量级；估算**偏高约 `27%`** |
| 报告输出 | `10 MB` 量级 | `10,120,702 B` | 同量级 |
| 观测工件 | `0.2 MB` 量级 | `179,574 B` | 同量级 |
| 峰值 RSS | **无可靠外推依据** | `73.22 MiB` | v7 阶段 `A` 为 `72.72 MiB`，两次接近 |

**本轮结论**：本次估时在 wall 与体积两项上**落在同一量级**，但**不得**据此把估算表述为读数，也**不得**据此为阶段 `B` 或补算承诺耗时——`docs/54h` 的「估算偏差 `41%`」先例与「峰值 RSS 无可靠外推依据」的现状**均未改变**。

## 6. 身份差异（必须持续披露）

| 项 | 值 |
|---|---|
| 报告中的 `code_identity`（**冻结时的 HEAD**） | `22e669a879acbf862aa8eb8953ca6fa90c29bf2c`（`docs:` 提交） |
| `@6` 与 `-iqv3` 的**实际实现提交** | `4fbb9833425475c593916969ba0605fc3525a2c9`（`feat:`）、`720ee2d57d69e5e1af75013456331e6413484ac6`（`feat:`） |
| 差异内容 | 冻结时的 HEAD 与实际执行代码之间**只有文档差异**，代码内容与 `720ee2d…` 相同 |

**不得**把本次报告表述为「由 `22e669a` 代码产生」。

## 7. 契约、API 与默认策略

| 对象 | 状态 |
|---|---|
| 公开 API 字段 / 数据库列 / `history_json` | **零改动** |
| `mixed-validation.v2` / `mixed-observations.v1` / `mixed-recheck.v1` | **零改动**（无新增字段） |
| `MixedFixtureManifest` / 冻结清单 | **零改动**（摘要逐字未变） |
| 策略身份 | **未新增**（仍为 `mixed-local@6`） |
| `docs/37` §3.1 | **不触发** |
| 默认策略 | **仍为 `heuristic@1`** |
| v1–v7 外部证据 | **零触碰** |
| 清单文件 | **零改动**（sha256 与冻结回执一致） |

## 8. 未做事项与仍未证

- **未做**：确定性补算（阶段 `A` 的补算需单独授权）；阶段 `B`；任何判据计算；任何质量结论；完整标定；默认切换。
- **仍未证**：L1–L5 的**任何判据读数**（阶段 `B` 为主来源，且判据计算属判定轮次）；`@6` 的强度、可剥削性、抗针对性；域二与 L4 是否改善；根因 C 是否成立；阶段 `B` 与补算的 wall / RSS。
- **本篇不使用阶段 `A` 的分布类读数做任何判据判断**：逐节点明细只按「行数、后缀、种子块、截断」四项做**机械自洽核对**；L3 任何指标的读数**留待判定轮次**，以免在判定前形成事实上的口径选择。
- **未消耗**：本轮未消耗补算与阶段 `B` 的授权。

## 9. 不得声称

- 不得把阶段 `A` 的 `completed`、`violations=[]`、覆盖 `288/288` 或毫秒级延迟读数说成**质量证据**或判据达标。
- 不得把 `18,000` 行逐节点明细的存在说成不可预测性已达标。
- 不得把本次估时对照说成「估算已被验证」；不得为阶段 `B` / 补算承诺耗时。
- 不得把本次报告说成由 `22e669a` 代码产生（§6）。
- 不得把阶段 `A` 说成阶段 `B` 的替代；**不得**用阶段 `A` 的读数替代判定轮次的判据计算。
- 不得据此修改 `docs/20` 至 `docs/57f` 与任何已落盘工件；不得重跑 v6 / v7 冻结集合；不得重跑本次阶段 `A`。

## 10. 本篇唯一下一步

阶段 `A` 已完成并如实记账。按 `docs/57f` §10 的顺序，**下一步是确定性补算**（`--allow-recheck`，须单独授权；它与运行**各自单独一轮**，不得合并），随后才是阶段 `B`（须引用阶段 `A` 的 `completed` 回执通过门禁）。

**运行补算前须给实测支撑的估时**：可依托本次阶段 `A` 的真实读数（wall `3.949 s`、峰值 RSS `73.22 MiB`、输出约 `9.82 MiB`），但**峰值 RSS 仍无可靠外推依据**；且 v7 阶段 `B` 的补算峰值 RSS 为 `917.38 MiB`（**未归因**），本轮补算须按 `R4(a)` 沿用既有 `≤1 GiB` 包络并保留「未归因」标注。
