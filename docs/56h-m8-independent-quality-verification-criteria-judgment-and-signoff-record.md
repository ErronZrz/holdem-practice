# 56h M8：v7 独立质量验证的判据判定与签收回执

> 日期：2026-09-23。
>
> 本文件接在 `docs/56g` 之后，沿用 `docs/30` §10.1 D6 的编号族约定。`docs/20` 至 `docs/56g` 未被改写。
>
> 本文件记录：按 `docs/55` §7 的 L1–L5 判据、在**已冻结样本**（v7）上作出的**判定读数**，以及由 D2（`docs/55` §10.4）绑定推出的**本次质量签收结论**。
>
> **措辞约束**：按 `docs/55` §7.5 第 1 条，判定只输出**解释性状态**，不使用「合格 / 不合格 / 通过 / 达标」。文中「满足 / 不满足」仅作为 D4（§10.6）与 D7（§10.7）原文所用的**绑定术语**出现。
>
> **本篇同时是披露文件**：它披露本次判定所依赖的若干口径（X3–X9）是在**读数产生之后**才固定的，因而**弱于**预注册固定（§4 逐条留痕），并披露一处使原口径无法实现的实测事实（§4.10）。

## 1. 授权原文与边界

用户答复原文依次为：

> 「A.」——进入**口径空白逐项裁定**（第一批：L3 的 X3 / 域映射 / X5b / X7）。
>
> 「A.」——第二批裁定（X9 / X8 / X8b）。
>
> 「A.」——X8b 修订为「街起始底池」。
>
> 「A.」「A.」——选定**完整版签收原文**，并授权**写本回执 + 单条 `docs:` 提交 + 推送**。

| 项 | 内容 |
|---|---|
| 被授权 | 按已裁定口径做判定计算；出具本判定与签收回执；提交并推送 |
| 计算口径 | 只读**已落盘工件的现有字段** + **内联确定性计算**；不新增文件、不改动代码、不重跑、不产生新样本 |
| 明确排除 | 修改任何判据、门槛、机会分母、估值块或区间方法；重跑任何阶段或补算；删除任何样本；默认策略的自主切换 |
| 保留 | v7 七个工件与 v1–v6 全部工件**原样保留**；v6 失败阶段 A 回执 `86e84fed…` **未被触碰** |

## 2. 只读基线核验（判定前）

```text
$ git status --short --branch
## master...origin/master

$ git rev-parse HEAD
ae5043b3484fa04bc895bd26e81ff27617f8c9cb
```

判定计算**开始前**工作区**零改动**；全部计算为**内联确定性计算**，未新增文件、未改动代码。本回执是判定**之后**的记录文件，其中不含计算代码。

**一处环境异常（如实记账）**：判定期间出现过一次瞬时读取失败（`FileNotFoundError`，目标为阶段 B 回执）。随即复核确认：该文件在位、七个工件 sha256 与运行时记录**逐字相同**、期间**未发生任何写入**。该异常不影响任何读数。

## 3. 判定输入工件（v7，共 7 个）

| 工件 | sha256 | 角色 |
|---|---|---|
| `input/frozen-fixture-manifest.json` | `a68e9776c180400b80fccecd9698b0d12a32e7b95ddf650246f87c2c487cae61` | 身份与摘要基准 |
| `runs/stage-a/mixed-validation-stage-A.json` | `980a62f6503c533501c7640b136bd2e71e77819132ecb7d6e89833c3b0a34571` | L1 |
| `runs/stage-a/mixed-observations-stage-A.json` | `464daefe26b281b4ae50ba4b72f5612acdb79a89fb6ca908faa0d75e8a86db20` | 备用 |
| `runs/stage-a-recheck/mixed-recheck-stage-A.json` | `dd89e5fb114b1074178ecdb8d8dd40e38fbe1962baa72d70eff494e055482104` | L1（`128` 手） |
| `runs/stage-b/mixed-validation-stage-B.json` | `f2b83bc1a7cbd0f451c72efec3e818ffdc38ef8521ada00f0d7350035ae97c4c` | **主来源** |
| `runs/stage-b/mixed-observations-stage-B.json` | `04a5acb2e57058f9929f3f39e0c0e64d2d1b490032f3bff3de2ea300a5043b13` | L5 第 2 项 |
| `runs/stage-b-recheck/mixed-recheck-stage-B.json` | `215835e5c89eed73d58c3cda60d809b88d5a016a732822eefa13eb8a8bbcbb6d` | L1（`33,792` 手） |

身份：`strategy_id=mixed-local@5`、`config_digest=8896013bbcd873cd493c6319756c353365b49cc7b4e846e927c4216d6d1f82f2`、`manifest_digest=68ab0ca3f73eeb566a08d78542cd9624c644b476ad59cd936033f3fc5afe4547`。

## 4. 判定计算口径与**固定时点**（本篇最重要的披露）

### 4.1 读数前已锁定的口径（强）

| 编号 | 口径 | 锁定位置 | 时点 |
|---|---|---|---|
| X1 | L4 区间唯一方法：块均值双侧 95% t 区间，`df=S−1`，`S=16` ⇒ `t(0.975,15)=2.131`；**强制并列**块均值/块标准差/块极值/逐人数分解；两口径不一致判**未满足** | `docs/56a` §6 | **读数之前** |
| X2 | L3 第 1/2/3/4 项的固定估值种子块 = 第 1 个主种子块 `2518403890`；第 5 项按全部 `16` 块 | `docs/56b` §7 | **读数之前** |

### 4.2 本次判定前新固定的口径（X3–X9，**均在 v7 读数产生之后**）

| 编号 | 裁定结果 | 影响判据 |
|---|---|---|
| X3 | 单元 = 块 1 的 **`1,125` 个分布**（节点 × 风格 × 手模式）；「整体」= 该 1,125 分布上的比例；**强制并列**按 `125` 节点聚合的第二读数 | L3 第 1 项（并适用于 X6 的额度项单元） |
| X4 | 三分域**自然分区**：翻前 = `street=preflop`（7 类）／面对下注 = `price_context=facing-bet`（8 类）／免费行动 = `unopened` 且非翻前（1 类，`free-check`） | L3 第 1 项分域报告 |
| X5 | 两个机会域：**域一 = `unopened-open`**；**域二 = 翻牌且 `facing-bet`（`flop-draw`）** | L3 第 4 项 |
| X5b | 「至少两档」= **三个风格配对中至少两对**在两域上各 ≥10 个百分点 | L3 第 4 项 |
| X6 | **无需单独裁定**：单元随 X3；金额条件熵均值取**非 `None` 行**的均值（由既有 `_mean` 口径唯一确定） | L3 第 3 项 |
| X7 | 「方向稳定」= 三档风格两两配对的差在**全部 16 个种子块**上符号一致 | L3 第 5 项 |
| X8 | 短段 = 每（对手族 × 风格）内按（种子块清单序、人数、轮换）排序的**前 20 对**；长段 = 其余；增益 = 配对差 bb/100 的 `短段 − 长段` | L5 第 2 项 |
| X8b | 尺度 = 面对的 `bet/raise` 额 ÷ **街起始底池**（= 上一街结束底池；翻前 = `1.5 BB`）；三档 `≤50% / 50–100% / >100%`（**修订项**，见 §4.10） | L5 第 2 项尺度桶 |
| X9 | L4「最差族」= **逐人数各自取最差族**（沿用 `docs/55g` §7 先例）；配对差按手池化，块均值 = 每块内该族全部配对差异的均值 | L4 |

### 4.3 口径固定的方法论后果（必须一并记录）

1. X3–X9 的固定时点**在读数产生之后**，因此**弱于**预注册固定。**不得**把本次判定表述为「预注册口径上的判定」。
2. 为降低事后自由度，全部选择遵循同一条规则：**取由冻结原文与字段语义自然导出、且不依赖读数的那一项**。逐项依据见 §4.2。
3. **未**按「哪一选项更容易达标」挑选任何一项：本轮两项未满足（L3 第 4 项、L4）在任何已列出的备选读法下都不达标——L4 的两种区间口径、L3 第 4 项的三种「至少两档」读法、X3 的两种单元均给出同一方向。
4. 若某项被认为无法在不看读数的情况下固定，则应记为**不可判定**；本轮**不存在**此类项（见 §11）。

### 4.4 计算口径与自洽核验

- 配对差 = 同一（人数、风格、对手族、种子块、轮换）下 `mixed-focus` 与 `legacy-focus` 的净筹码之差；
- 配对完整性：`16,896` 对，**配对异常 0**；缺 `seed_block`/`rotation` 的行 **`0`**；
- 币值换算：`33,792` 行全部为单手行，`bb_per_100 == net_chips × 10` **零不符** ⇒ `1 BB = 10` 筹码在本样本上自洽；
- 判定结构：按 `docs/55` §10.4.2 第 3 条，**逐人数各自独立判定、禁止跨人数归因**（L4 在 §8 逐人数给出；跨人数数字仅作报告）；
- 非强制节点分母：块 1 的 `1,125` 行中 `action_units` 键数为 `2` 者 `252` 行、为 `3` 者 `873` 行 ⇒ **全部 ≥2**，分母在全集上成立。

## 5. L1 机制与正确性（必要条件）

| 项 | 读数 | 解释性状态 |
|---|---|---|
| 违规数 | 阶段 A `violations=[]`、阶段 B `violations=[]` | 读数 `0` |
| 截断数 | 阶段 B `truncated_hands=0`；补算 `aggregates` 截断合计 `0` | 读数 `0` |
| 摘要一致 | `config_digest` / `manifest_digest` 在阶段 A、阶段 B 与两级补算**四处一致**；补算不重算摘要 | 读数一致 |
| 随机可重放 | 阶段 A 补算（`128` 手）与阶段 B 补算（`33,792` 手）`net_chips_exact=true`、`behavior_exact=true`；**补算同时含观测工件自洽核对**（`128` + `33,792` 手） | 读数一致 |
| 信息边界 | **本轮取得直接读数**：`tests/test_info_boundary.py` → **`7 passed`**；工件层面仍无专门字段，如实标注为**契约测试口径**而非工件字段读数 | 读数通过（口径受限） |

L1 五项**均有读数**，未发现违反。信息边界项较 v6 前进一步（v6 记为「未直接读数」），但其证据形式是契约测试，**不是**评测工件字段。

## 6. L2 成本（必要条件）

| 项 | 读数 | 解释性状态 |
|---|---|---|
| 硬预算 `DECISION_BUDGET_MS=100` | `decision_path` 超 `100 ms` **`0`**（样本 `80,000`）；`bot_step` / `engine_apply` / `public_summary_update` 各 `80,000` 亦均为 `0`；补充场景 `240` 样本为 `0` | 未超硬预算 |
| 指导值 | p50 `0.2768` / p95 `0.3670` / p99 `0.5151` / max `13.9234` ms | p95 <50、p99 <80 读数满足 |
| 逐人数分组 | 人数 2–9 共 `8` 组，各 `10,000` 样本，各组超时 `0`；p95 区间 `0.3462–0.4602` | 已逐人数报告，未以合并均值代替 |
| 样本量 | `80,000` ≥ 建议 `10,000` | 读数满足 |
| 口径要求 | 查询延迟与端到端决策延迟**分列**；`coverage.executed_nodes=125` 已如实标注为**清单库存**口径（`planned=128`、`not_run=0`、`structural=3`） | 满足 |
| 包络（**人工比对**，代码不据此改 `status`） | 阶段 B wall `511.948 s` / 峰值 RSS `493.22 MiB`；阶段 B 补算 wall `602.846 s` / 峰值 RSS `917.38 MiB`；保留文件合计 `62.78 MiB` | 未触及 `≤1800 s` / `≤1 GiB` / `≤100 MiB`；**补算路径无声明包络**，其 RSS 余量约 `10%` 且**未归因** |

## 7. L3 不可预测性（D7 六项，估值块 = `2518403890`）

| # | 判据（门槛） | 读数 | 解释性状态 |
|---|---|---|---|
| 1 | 实质动作混合覆盖率 **≥25%**，并分域报告翻前 / 面对下注 / 免费行动 | 整体 **`39.82%`**（`448/1,125`）；分域 翻前 `56.38%` / 面对下注 `30.69%` / 免费行动 `0.00%`；并列按 `125` 节点聚合 `50.40%` | 读数高于门槛 |
| 2 | 平均类型熵 **≥0.15 bit** | `0.3181`（tight） / `0.4221`（aggressive） / `0.3838`（calling），各 `375` 分布 | 读数高于门槛 |
| 3 | 至少两档锚点**各 ≥15%**；金额条件熵 **≥0.5 bit** | 锚点满足率 **`237/237 = 100.00%`**；金额条件熵（非 `None`，`n=237`）**`1.3999`**（分风格 `1.3610` / `1.5395` / `1.2955`） | 读数高于门槛 |
| 4 | **至少两档**在两个预先指定机会域上**各 ≥10 个百分点** | 域一差 `20.22` / `33.91` / `13.69` pp（均 ≥10）；**域二差 `0.35` / `0.03` / `0.38` pp（均 ≪10）** ⇒ **`0/3` 对满足** | **不满足** |
| 4b | 风格间 JS 距离 **≥0.15**（沿用，不重标定） | `0.1730`（tight–aggressive） / `0.2956`（tight–calling） / `0.1641`（aggressive–calling），各 `125` 节点 | 读数高于门槛 |
| 5 | 风格稳定性：**方向稳定** | `6/6` 个（配对 × 域）组合在 **`16/16` 块**同号 | 方向稳定 |

**必须一并标注的四条**：

1. **D7 第 4 条**：第 4b 项最薄余量为 `aggressive–calling` 的 `0.1641`（余量 `+0.0141`）；按该条，**邻近阈值的达标不得表述为稳健达标**。
2. **域二的读数是决定性的**：三档风格在 `flop-draw`（翻牌面对下注）节点上的主动率（bet / raise）均在 `0.2–0.6%` 量级；按冻结定义（`docs/55a` §7.5 域二 = 主动概率）就是该口径。**不得**以「该域天然难有主动」为由在读数后更换域定义。
3. **`docs/55a` §7.3 + D7 第 5 条**：结构恒等节点**照实计入**；本次**未剔除**任何节点后再报告。
4. **D7 第 6 条**：不得以「门槛原为旧口径提出、可能过紧」为由在读数后放宽；该风险已在本判定前如实披露，属**已被接受的风险**。

**不得用于替代判定的相邻读数（防止换分母）**：`no_active_candidate` = `299 / 294 / 295`（各 / `375`）。它是回执字段，但**不是**覆盖率读数，**不得**用它替代第 1 项、也**不得**反向推断覆盖率。

## 8. L4 不劣化（D4，逐人数独立判定）

**统计量**：相对 `heuristic@1` 的最差族**配对**收益差 95% 区间**下界 ≥ −5 BB/100**；区间按 X1（块均值 t 区间，`t(0.975,15)=2.131`）；最差族按 X9（逐人数各自取）。

| 人数 | 最差族 | 均值 BB/100 | t95 下界 | 块 min | 块 max | 状态 |
|---|---|---|---|---|---|---|
| 2 | `marginal-call-pressure-probe` | `-522.92` | `-1556.41` | `-5160.00` | `3216.67` | 未达门槛 |
| 3 | `marginal-call-pressure-probe` | `-681.94` | `-2885.57` | `-13400.00` | `4246.67` | 未达门槛 |
| 4 | `aggression-rate-probe` | `-1364.22` | `-2792.43` | `-7054.17` | `308.33` | 未达门槛 |
| 5 | `random@1` | `-191.92` | `-872.09` | `-2470.00` | `2086.67` | 未达门槛 |
| 6 | `aggression-rate-probe` | `-842.19` | `-2043.33` | `-8288.89` | `69.44` | 未达门槛 |
| 7 | `aggression-rate-probe` | `-1604.76` | `-3307.63` | `-8580.95` | `480.95` | 未达门槛 |
| 8 | `aggression-rate-probe` | `-625.05` | `-2122.45` | `-11127.92` | `591.67` | 未达门槛 |
| 9 | `marginal-call-pressure-probe` | `-948.06` | `-2045.19` | `-4777.41` | `3789.26` | 未达门槛 |

**逐人数判定结果：`0 / 8` 人数达门槛**；且 **t 区间与块极值两口径均为 `0/8`**（口径选择不改变结论）。故 L4 记为**不满足**。

**双向报告（D4 第 3 条，不得只报有利一侧）**

| 人数 | 最大正偏离族 | 均值 |
|---|---|---|
| 2 | `size-signal-probe` | `+225.00` |
| 3 | `heuristic@1` | `+189.93` |
| 4 | `fixed-pressure-probe` | `+807.29` |
| 5 | `aggression-rate-probe` | `+222.08` |
| 6 | `fixed-pressure-probe` | `+757.99` |
| 7 | `fixed-pressure-probe` | `+191.82` |
| 8 | `fixed-pressure-probe` | `+568.57` |
| 9 | `fixed-pressure-probe` | `-1.50` |

（人数 9 的「最大正偏离族」均值仍为负，说明该人数下 8 族均值**全为负**。）

**必须并列呈现的已知方向性风险（D4 第 5 条强制）**

| 来源 | 读数 |
|---|---|
| `docs/53c` §4 第 1 条（旧记录） | 旧启发式 arm 在 4 个主种子块中的 **3 个**高于新 Bot；总计 新 `+47,007` 对旧 `+136,878` |
| 本次 v7 对应读数（跨人数合计，仅作报告） | `mixed-focus` 净筹码 `+491,669` 对 `legacy-focus` `+1,032,216`（各 `16,896` 手）→ 方向与旧记录**一致**；**`16` 块中 `12` 块** legacy 更高 |

**该读数不是强度证据**：机械探针不是 best response、逐格手数有限、`16` 个固定种子块即便给出区间也只是粗区间。它**只**说明方向不利于新 Bot，与 L4 的判定结果**并列**呈现，不得单独当作结论。

## 9. L5 抗针对（无数值门槛，属报告层）

### 9.1 逐族读数（第 1 项）

| 对手族 | `mixed-focus` 净筹码 | 均值 BB/100 | 块 min | 块 max |
|---|---|---|---|---|
| `aggression-rate-probe` | `+44,493` | `+210.67` | `-199.77` | `+1571.97` |
| `fixed-pressure-probe` | `+114,981` | `+544.42` | `-1506.06` | `+3064.47` |
| `heuristic@1` | `-19,893` | `-94.19` | `-451.67` | `+314.17` |
| `marginal-call-pressure-probe` | `+165,536` | `+783.79` | `-202.27` | `+2920.30` |
| `passive-call-probe` | `+22,975` | `+108.78` | `-174.62` | `+1131.74` |
| `position-pressure-probe` | `+28,964` | `+137.14` | `-336.44` | `+872.12` |
| `random@1` | `+120,520` | `+570.64` | `-1018.71` | `+2808.64` |
| `size-signal-probe` | `+14,093` | `+66.73` | `-23.03` | `+202.73` |

逐人数分解可由阶段 B 回执的 `matches.rows` 复算；本节跨人数汇总**仅作报告**，按 §10.4.2 第 3 条**不得**据此归因到具体人数。

### 9.2 针对性诊断（第 2 项，**本轮首次可报告**）

**口径（X8 + X8b 修订）**：短段 = 每（对手族 × 风格）内按（种子块清单序、人数、轮换）排序的前 `20` 对（合计 `60` 对/族）；长段 = 其余 `2,052` 对/族；增益 = 配对差 bb/100 的 `短段 − 长段`；尺度 = 面对的 `bet/raise` 额 ÷ **街起始底池**（三档 `≤50% / 50–100% / >100%`）。

**逐族整体**

| 族 | 短段 bb/100 | 长段 bb/100 | 差（短 − 长） |
|---|---|---|---|
| `aggression-rate-probe` | `-189.00` | `-788.39` | `+599.39` |
| `fixed-pressure-probe` | `+626.67` | `+281.33` | `+345.34` |
| `heuristic@1` | `-770.17` | `-50.67` | `-719.50` |
| `marginal-call-pressure-probe` | `-2520.17` | `-320.79` | `-2199.38` |
| `passive-call-probe` | `-121.50` | `-595.97` | `+474.47` |
| `position-pressure-probe` | `-1682.50` | `-577.87` | `-1104.63` |
| `random@1` | `-201.67` | `+25.19` | `-226.85` |
| `size-signal-probe` | `-338.33` | `-455.11` | `+116.78` |

**池化（8 族合并）逐桶**

| 维度 | 桶 | n（短/长） | 短段 | 长段 | 差 |
|---|---|---|---|---|---|
| 尺度 | `≤50%` | `5 / 173` | `+132.00` | `-2568.32` | `+2700.32` |
| 尺度 | `50–100%` | `0 / 130` | — | — | — |
| 尺度 | `>100%` | `37 / 1000` | `-2470.00` | `+770.31` | `-3240.31` |
| 尺度 | 无面对加注 | `438 / 15113` | `-504.73` | `-355.37` | `-149.35` |
| 牌力 | `weak` | `332 / 9575` | `-852.17` | `+184.11` | `-1036.28` |
| 牌力 | `medium` | `102 / 4857` | `-326.18` | `-561.49` | `+235.32` |
| 牌力 | `strong` | `0 / 764` | — | — | — |
| 牌力 | `None` | `46 / 1220` | `+95.43` | `-189.24` | `+284.67` |
| 街道 | `preflop` | `28 / 853` | `-1800.36` | `+483.49` | `-2283.85` |
| 街道 | `flop` | `10 / 305` | `-2645.00` | `+133.02` | `-2778.02` |
| 街道 | `turn` | `0 / 66` | — | — | — |
| 街道 | `river` | `4 / 79` | `-3467.50` | `-1323.04` | `-2144.46` |

**必须一并披露的稀疏性（限制该诊断的可解释力）**：`15,551 / 16,896`（**`92.0%`**）被测手**整手没有面对任何 `bet/raise`**，故尺度桶 `92%` 落在「无面对加注」，三个真实尺度桶合计仅 `215` 对（`1.3%`）；`strong` 牌力档与 `turn` 街在短段**样本为 0**。因此该项**只能**支持「本次样本集与本次探针池下未观察到 X」的边界表述，**不得**据此作强度或剥削性结论，**更不得**表述为「探针随样本增长而变准」。

### 9.3 结论边界（D3 第 4 条）

因本次**不做** best response（D3 已裁定），L5 只能表述为「**本次样本集与本次探针池下未观察到 X**」；**不得**表述为不可剥削，**不得**作为 exploitability / NashConv / best response 的界。

## 10. 汇总判定

| 层 | 项数 | 读数高于/满足门槛 | 未满足 | 不可判定 |
|---|---|---|---|---|
| L1 机制与正确性 | 5 | 5 | 0 | **0** |
| L2 成本 | 6 | 6 | 0 | 0 |
| L3 不可预测性（D7） | 6 | 5（第 1、2、3、4b、5 项） | **1（第 4 项，`0/3` 配对）** | **0** |
| L4 不劣化（D4） | 1（逐人数 `8` 项） | 0 | **`8/8` 人数** | 0 |
| L5 抗针对 | 2（报告层） | 第 1 项已报告；第 2 项**本轮首次可报告** | 0（无数值门槛） | **0** |

**存在未满足项** ⇒ 按 **D7 第 3 条**（全部绑定 ⇒ 任一项不过即「质量未获支持」）、**D4 第 4 条**、**§10.4.3**：

> **本次结论：质量未获支持；默认策略保持 `heuristic@1`。**

## 11. 不支持的原因必须分开陈述（与 v6 的关键差别）

| 类别 | 本轮涉及项 | 性质 |
|---|---|---|
| **(a) 已计算且未达** | L4（`0/8`，两口径一致）；L3 第 4 项（`0/3` 配对，域二主动率三档风格均在 `0.2–0.6%`） | **读数明确低于门槛** |
| **(b) 在冻结口径下不可判定** | **无** | v6 的 (b) 类（L3 第 1/3/4/5 项与 L5 第 2 项）经本次诊断性改造后**全部可计算** |

**三条必须写明的限定**：

1. **(b) 类归零是改造达成的结果，不是质量改善**。诊断性改造只解决了「能不能判」，**不改变**被测策略或分布；v7 的读数建立在**全新节点集与全新种子集**上，与 v6 的数值**不可直接比较**。
2. **不得**把 (a) 类说成「口径问题」或「工程问题」；也**不得**把 v6 的 (b) 类说成「已证不合格」（v6 的该项表述维持不变）。
3. 本轮**不存在**「无读数即记未满足」的项，因此 v6 §11 中关于「不得用点估计声称达标」的处置本轮**未被触发**；该纪律本身**继续有效**。

## 12. 用户签收

**签收原文（由用户出具，逐字记录）**：

> 「认可本次判定结果与「质量未获支持」结论，保持 `heuristic@1` 默认；认可 X3–X9（含 X8b 修订）这些口径是在读数产生之后才固定的、弱于预注册；同意不再就该冻结集合做任何补算、重跑或口径改换。」

**原文来源（如实披露）**：该原文由用户从本轮提供的**三个参考措辞**中选定 A 项（完整版）；**参考措辞由助手起草，选择与签收均由用户作出**。用户的答复原文为：

> 「A.」（签收问项）与「A.」（提交问项）

- 签收形式：由用户在参考措辞中选定（`docs/55g` §12 的同一形式）；
- 签收**不新增、不修改任何判据**（D2 §10.4.2 第 2 条）；
- 本节只记录**用户表态**，不改动 §10 的机械判定结果。

## 13. 契约、API 与默认策略

| 对象 | 状态 |
|---|---|
| 公开 API 字段 / 数据库列 / `history_json` | **零改动** |
| `mixed-validation.v1` / `v2` / `mixed-observations.v1` / `mixed-recheck.v1` | **零改动** |
| 清单模型 / 冻结清单 | **零改动**（摘要逐字未变） |
| 新策略身份 / 新清单 | **均未新增**；被测身份仍为 `mixed-local@5` |
| `docs/37` §3.1 | **不触发** |
| 默认策略 | **仍为 `heuristic@1`** |
| v1–v6 外部证据 | **零触碰**（清单与全部回执 sha256 逐条复核未变） |
| v7 七个工件 | **零改动、零新增**（本篇不写工件） |

## 14. 未做事项与仍未证

- **未做**：best response 与任何 BR 构造（D3 已排除）；跨人数归因；用更多 seed 估计判定反转频率（`docs/39` §3.7 的同一精神）；任何参数、门槛或口径改动；任何重跑或补算；默认切换。
- **仍未证**：任何强度、可剥削性或不可剥削结论；跨机器与云端成本；总体（非 `16` 固定种子块）覆盖；L5 第 2 项因**桶内样本极小**（部分格 `2–5` 对）尚不足以支撑任何分层结论。
- **即便判据全部满足**，结论也只能表述为「本次独立验证下判据满足」（§10.4.2 第 5 条）；本次判据未全满足，该限制不改变结论方向，但必须一并记录。
- **未消耗**：本轮**未消耗**任何 authorization，也未触发 `docs/37` §3.1；判定期间仅额外运行了一次定向契约测试（`test_info_boundary.py`）。
- **永久已消耗的 A9 授权**（`a9-calibration-i10` 等）一律**不得重试**，与本轮无关。

## 15. 不得声称

- 不得把本次部分达标读数（覆盖率 `39.82%`、类型熵 `0.3181`/`0.4221`/`0.3838`、锚点 `100.00%`、金额熵 `1.3999`、JS `0.1730`/`0.2956`/`0.1641`、稳定性 `16/16`）表述为质量已证明、稳健达标或「基本通过」。
- 不得把 L4 的「已计算未达」说成「口径问题」；不得把 L3 第 4 项的未满足说成「域定义偏严」。
- 不得说成「v7 比 v6 差」或「v7 比 v6 好」——两者建立在**不同节点集与不同种子集**上，数值不可直接比较。
- 不得把「(b) 类归零」说成质量改善；它只是**可判定性**的改造结果。
- 不得把 L5 的探针读数表述为不可剥削、exploitability 上界或 best response 结果；不得把第 2 项的极小样本格当作发现。
- 不得把本次判定表述为「验证通过」「验收通过」或「已达生产门槛」。
- 不得把 X3–X9 的口径表述为「预注册口径」或「读数前锁定」；其固定时点在读数之后（§4.3）。
- 不得删除或改写 v6 的失败阶段 A 回执，也不得把它说成「未发生过」。
- 不得把本次结论表述为对 `docs/53c` 或 `docs/55g` 的**反转**（本次是**再次维持**）。
- 不得据此修改 `docs/20` 至 `docs/56g` 与任何已落盘工件；不得改写 `@1`–`@5` 任一身份的历史含义。

## 16. 本篇唯一下一步

判据闭环已完成，结论为**质量未获支持**，默认保持 `heuristic@1`。

按 D4 第 4 条 / §10.4.3，若需再试，必须**新开策略身份或新样本集并重新预注册**（全新清单、全新种子与**单独运行授权**）；**不得**改判据、**不得**重跑 v7 冻结集合、**不得**把同一集合反复调参后称为独立验证（`docs/53` §13.2）。

**建议的唯一下一步（需用户单独授权，本篇不自行执行）**：先就 L4 的负差做一轮**只读定向诊断**（只分析 v7 已落盘工件，不新增样本、不重跑、不改代码），把负配对差定位到人数 / 风格 / 对手族的哪些格子与哪些尺度 / 牌力 / 街道分桶，用于回答「是否值得新开身份重做」；诊断结论**不得**用于修改任何判据。
