# 54m M8：`mixed-local@5`（跟注型翻后被动偏移）标定运行回执

> 日期：2026-09-22。
>
> 本文件是 `docs/54k` §7 标定运行的实施回执，沿用 `docs/30` D6 的编号族约定。`docs/20` 至 `docs/54l` 未被改写。
>
> 本文件记录一次已授权的实测运行与其读数。**这些读数是开发证据，不是独立质量验证，也不是质量签收**：按 `docs/53` §13.2，已看过的读数不得反复调参后当作独立验证，独立质量验证须另立预注册设计。

## 1. 授权原文与边界

用户答复原文：

> 「A.」

对应选项原文：

> 「A. 授权执行完整标定包（清单 + 阶段 A + 阶段 B + 补算），按上表常量与包络」

| 项 | 内容 |
|---|---|
| 被授权 | 为 `mixed-local@5` 新建工作树外目录与冻结清单；执行阶段 A、阶段 B 与确定性补算；读取并报告 T1 与目标带读数、PFR 分工与成本读数 |
| 明确排除 | 调整任何参数；重跑；`commit` / `push`；默认切换；M2-3 |

## 2. 身份、清单与目录

| 项 | 值 |
|---|---|
| 策略身份 | `mixed-local@5`（累积版：前四版口径 + 跟注型翻后被动偏移 100） |
| `code_identity` | `afa237a9414d1e21c8cdd58c7555aaf730b3d667` |
| `config_digest` | `8896013bbcd873cd493c6319756c353365b49cc7b4e846e927c4216d6d1f82f2` |
| `manifest_digest` | `d68125e2dfc4600f2e23a80bb18b23f997812f73732812ddf685d38e112124e6` |
| 主种子 | `1215 / 20260918 / 3311 / 7926`（沿用，未新增） |
| 节点 | 125（清单库存口径） |
| 输出目录 | `/Users/bryanylliu/holdem-mixed-bot-validation-v5/`（v1–v4 未被触碰） |

`manifest_digest` 与授权时给出的**预承诺值逐字一致**，说明实际使用的清单就是被审查的那一份。

## 3. 判定口径一：D3 = T1（三对两两平均 JS ≥ 0.15）

读数口径：与运行报告同源——同一 4,224 手设计、同一四个主种子、同一清单排布下的 125 节点平均 JS（含结构恒等节点）。

| 风格对 | `@4` | `@5` 实测 | 冻结节点预测（实施回执） |
|---|---|---|---|
| 紧密–松凶 | 0.2136 | 0.2136 | 0.2136 |
| 紧密–跟注 | 0.2444 | **0.3132** | 0.3132 |
| 松凶–跟注 | 0.1102 | **0.1765** | 0.1765 |
| 最小对 | 0.1102（不满足） | **0.1765（满足）** | — |

**判定：T1 满足。** 运行读数与实施回执里的冻结节点预测**逐位一致**，印证实施严格按同一数学落地。

## 4. 判定口径二：`@4` 的目标带不回落

| 档 | `@4` | `@5` 实测 | 下限 ≥0.12 | 上限 ≤0.35 |
|---|---|---|---|---|
| 紧密 | 0.1264 | 0.1264 | ✓（余 0.0064） | ✓（余 0.2236） |
| 松凶 | 0.2372 | 0.2372 | ✓ | ✓（余 0.1128） |
| 跟注 | 0.3281 | 0.3281 | ✓ | ✓（余 0.0219） |

两两差：紧–松 `0.1108`、松–跟 `0.0909`、紧–跟 `0.2017`，全部 ≥0.05。

**判定：目标带不回落**，且三档 VPIP 与 `@4` **逐字相同**——符合设计（本次只动翻后被动分支，翻前一分未改）。

| 读数 | `@4` | `@5` |
|---|---|---|
| PFR 紧 / 松 / 跟 | 0.0099 / 0.0128 / 0.0000 | 0.0099 / 0.0128 / 0.0000（逐项不变） |
| 主动率 紧 / 松 / 跟 | 0.0194 / 0.0362 / 0.0152 | 0.0194 / 0.0362 / 0.0143 |
| 旧 arm（参照） | 0.2429 / 0.0170 / 0.0649 | 逐字相同 |

跟注型 PFR 仍为 0、松凶 PFR 与主动率仍最高，符合 `docs/54k` §8 的回归风险清单。

## 5. 运行读数与资源

| 阶段 | 实测 |
|---|---|
| 阶段 A | 状态 `completed`；对抗对照 64 手；wall `0.937s`；输出 52,292 B；违规 0 |
| 阶段 B | 状态 `completed`；wall `125.32s`、CPU `124.58s`；RSS `72.2 MiB`；输出 1,282,312 B；违规 0、截断 0 |
| 覆盖 | 成本格 288/288、成本样本 80,000/80,000、补充场景 240/240、对抗对照 4,224/4,224 |
| 决策路径 | 样本 80,000；p50 `0.277` / p95 `0.362` / p99 `0.531` / max `8.904` ms；超 100 ms **0**；18 个分组逐组 0 |
| 补算 | 状态 `completed`；wall `70.14s`；RSS `52.8 MiB`；**4,224 手逐手净筹码与行为指标完全一致** |

包络（≤1800 s / ≤1 GiB / ≤100 MiB、单进程不并行）**均未触发**。

**如实记账**：阶段 B 的 RSS 由 `@4` 的 `60.3 MiB` 升到 `72.2 MiB`（约 +19%）；仍在包络内，但本轮**未做归因分析**，因此不得推断原因。

## 6. 证据落盘（工作树外）

| 文件 | sha256 前 8 位 |
|---|---|
| `input/frozen-fixture-manifest.json` | `a8615297` |
| `runs/stage-a/mixed-validation-stage-A.json` | `e253ffa7` |
| `runs/stage-b/mixed-validation-stage-B.json` | `3f3bf7d8` |
| `runs/stage-b-recheck/mixed-recheck-stage-B.json` | `cedf97fe` |

补算报告的来源回执摘要为 `3f3bf7d8…`，与阶段 B 文件本身的摘要逐字一致。v1–v4 的证据目录在本次运行前后分别为 5 / 4 / 4 / 4 个文件，未被增删或改写。

## 7. 本次运行**未**改变的事项

| 对象 | 状态 |
|---|---|
| 公开 API 字段 / 数据库列 / `history_json` / artifact schema | 零改动 |
| 仓库内文件 | 零改动（本文件为回执，另计） |
| 新建默认策略 | **仍为 `heuristic@1`** |
| `docs/53c` 的质量签收结论 | **未被覆盖**：本次命中不构成质量结论 |
| `docs/37` §3.1 | 不触发 |

## 8. 未做事项与仍未证

- 未调参、未重跑；M2-3 未做。
- 未做任何 best-response 或剥削性检验；抗针对只用机械探针，不是 best response。
- 单格手数少（8–16 手量级），bb/100 的量级由单手全下主导，**其排序不可当结论**。
- `coverage.executed_nodes` 为清单库存口径（125），不是本轮执行数。
- 跟注型动作熵在冻结节点上由 `0.376` 降到 `0.303`（副作用的定性记录）；按 `docs/53` §13.2 **不得**表述为改进目标。
- 仍未证：长期稳定性、2–9 人之外的外推、**任何质量结论**；RSS 上升的原因未归因。
- 各次运行只获得过包络级预算批准，不构成对更大规模运行的授权。

## 9. 不得声称

- 不得把本文件表述为独立质量验证、质量签收或"质量已通过"。
- 不得把 T1 命中或目标带命中表述为对手强度认证：两者都是自定的标定口径。
- 不得把紧密型 `0.1264` 的下限余量 `0.0064` 表述为稳健达标。
- 不得把确定性补算当作新样本或新证据。
- 不得把三档 VPIP 或 JS 的实测值表述为真实扑克频率、GTO 或均衡结论。
- 不得把本次读数用于反复调参后声称同一集合构成独立验证。
- 不得把本文件表述为已授权默认切换；**默认策略仍为 `heuristic@1`**。
- 不得据此改写 `docs/20` 至 `docs/54l` 与任何已落盘证据。

## 10. 唯一下一步

**建议先裁定一件事：提交本次回执**（是否需要推送，请在答复中一并说明）。提交之后，`@5` 的参数取值即被 v5 清单与三份回执固定，此后任何参数改动都须**新开 `mixed-local@6`**，不得静默改写 `@5`。

再往后可选的方向（每一项都需要单独一轮裁定，不在本轮范围内）：默认策略切换（前置条件是**质量签收**，而质量签收需要独立预注册验证，不是本次标定能提供的）、独立质量验证的预注册设计（可另立新的编号族）、抗针对与剥削性检验、M2-3。
