# 54g M8：翻前门槛细化（M2b）标定运行回执

> 日期：2026-09-22。
>
> 本文件是 `docs/54d` §7 M2b 的**标定运行回执**，沿用 `docs/30` D6 的编号族约定。`docs/20` 至 `docs/54f` 未被改写。
>
> 本文件记录一次已授权的实测运行与其读数。**这些读数是开发证据，不是独立质量验证，也不是质量签收**：按 `docs/53` §13.2，已看过的读数不得反复调参后当作独立验证，独立质量验证须另立预注册设计。

## 1. 授权原文与边界

用户答复原文：

> 「A.」

对应选项原文：

> 「A. 授权执行完整 M2b 包（清单 + 阶段 A + 阶段 B + 补算），按上表常量与包络」

| 项 | 内容 |
|---|---|
| 被授权 | 为 `mixed-local@3` 新建工作树外目录与冻结清单；执行阶段 A、阶段 B 与确定性补算；读取并报告三档实测 VPIP 与成本/延迟读数 |
| 明确排除 | 调整任何参数；重跑；`commit` / `push`；默认切换；M2-3；M3 / D3 |

## 2. 身份、清单与目录

| 项 | 值 |
|---|---|
| 策略身份 | `mixed-local@3`（累积版：首版 + 分池口径 + 翻前门槛细化） |
| `code_identity` | `3cefb2763f14b9d6fb0e372750eacdc9dbf0a21a` |
| `config_digest` | `5ed7992b36de67cc97b4035f075d2c563f989300b448c5878f31847eb001643f` |
| `manifest_digest` | `f8ad8c2e47e35833e315efc6add989a21e091adf1197d520bd70c7eddfb46541` |
| 主种子 | `1215 / 20260918 / 3311 / 7926`（沿用，未新增） |
| 节点 | 125（清单库存口径） |
| 输出目录 | `/Users/bryanylliu/holdem-mixed-bot-validation-v3/`（v1 与 v2 未被触碰） |

`manifest_digest` 与授权时给出的**预承诺值逐字一致**，说明实际使用的清单就是被审查的那一份。

## 3. 运行读数

| 阶段 | 实测 |
|---|---|
| 阶段 A | 状态 `completed`；对抗对照 64 手；wall `0.923s`；输出 52,256 B；违规 0 |
| 阶段 B | 状态 `completed`；wall `128.40s`、CPU `126.71s`；RSS `62.2 MiB`；输出 1,282,288 B；违规 0、截断 0 |
| 覆盖 | 成本格 288/288、成本样本 80,000/80,000、补充场景 240/240、对抗对照 4,224/4,224 |
| 决策路径 | 样本 80,000；p50 `0.279` / p95 `0.375` / p99 `0.587` / max `23.611` ms；超 100 ms **0**；18 个分组逐组 0 |
| 补算 | 状态 `completed`；wall `71.34s`；RSS `50.7 MiB`；**4,224 手逐手净筹码与行为指标完全一致**；未出现差异，因此报告正常写出 |

补算报告的来源回执摘要为 `cf287a7a…204d3f0c`，与阶段 B 文件本身的摘要逐字一致。

包络（≤1800 s / ≤1 GiB / ≤100 MiB、单进程不并行）**均未触发**。

## 4. D2 目标带判定（判定读数口径）

读数口径：在混合 arm 上，把同一清单排期（4 个主种子 × 4 个对手族 × 2–9 人）的全部对抗对照手数**按手数加权合并**，得到每档 VPIP。该口径可由第二版复核：用同一算法算 v2 得到 `0.0455 / 0.0881 / 0.0795`，与 `docs/54c` R2 记录的 `0.045 / 0.088 / 0.080` 吻合。

| 档 | 实测 VPIP | 下限 ≥0.12 | 上限 ≤0.35 | 手数 |
|---|---|---|---|---|
| 紧密 | **0.1080** | ✗ **差 0.0120** | ✓（余 0.2420） | 704 |
| 松凶 | 0.2372 | ✓ | ✓（余 0.1128） | 704 |
| 跟注 | 0.3281 | ✓ | ✓（余 0.0219） | 704 |

两两差：紧–松 `0.1292` ✓｜松–跟 `0.0909` ✓｜紧–跟 `0.2201` ✓（阈值 0.05）

**判定：目标带未命中。** 仅一项未达标：紧密型低于下限 `0.0120`。上限未被越过，但跟注型距上限仅余 `0.0219`。

## 5. 与第二版逐项对照（同口径）

| 读数 | @2 | @3 | 观察 |
|---|---|---|---|
| VPIP 紧 / 松 / 跟 | 0.0455 / 0.0881 / 0.0795 | 0.1080 / 0.2372 / 0.3281 | 三档同时抬离，且彼此分开 |
| PFR 紧 / 松 / 跟 | 0.0156 / 0.0128 / 0.0000 | 0.0099 / 0.0128 / 0.0000 | 跟注型仍为 0；松凶仍最高 |
| 主动率 紧 / 松 / 跟 | 0.0206 / 0.0326 / 0.0145 | 0.0183 / 0.0362 / 0.0152 | 松凶仍最高 |
| 旧 arm（参照） | 0.2429 / 0.0170 / 0.0649 | 逐字相同 | 旧 arm 口径未受影响 |
| 风格间 JS 紧–松 / 紧–跟 / 松–跟 | 0.2028 / 0.1862 / 0.0690 | 0.2388 / 0.2711 / 0.1102 | 三对全部上升；松–跟仍为最小的一对 |

按 `docs/54d` §8 的回归风险清单核对：非翻前节点两版逐位一致（由既有回归锁定）、跟注型 PFR 继续为 0、松凶 PFR 与主动率仍最高、未读取对手底牌、未引入新的随机维度。**松–跟 的 JS 距离上升不等于 M3 / D3 已裁定或已解决**。

## 6. 筛选模型偏差的验证

`docs/54e` §4 记录运行前估算为 `0.178 / 0.363 / 0.492`（紧 / 松 / 跟），并已披露该模型**系统性高估 30–40%**。实测 `0.1080 / 0.2372 / 0.3281`，即高估 **39% / 35% / 33%**，与该披露一致。估算值只用于选参，不是读数。

## 7. 证据落盘（工作树外）

| 文件 | sha256 前 8 位 |
|---|---|
| `input/frozen-fixture-manifest.json` | `38692f01` |
| `runs/stage-a/mixed-validation-stage-A.json` | `b2f7b343` |
| `runs/stage-b/mixed-validation-stage-B.json` | `cf287a7a` |
| `runs/stage-b-recheck/mixed-recheck-stage-B.json` | `9e741bf1` |

v1 与 v2 的证据目录在本次运行前后均为 5 / 4 个文件，未被增删或改写。

## 8. 本次运行**未**改变的事项

| 对象 | 状态 |
|---|---|
| 公开 API 字段 / 数据库列 / `history_json` / artifact schema | 零改动 |
| 仓库内文件 | 零改动（本文件为回执，另计） |
| 新建默认策略 | **仍为 `heuristic@1`** |
| `docs/37` §3.1 | 不触发 |

## 9. 未做事项与仍未证

- 未调参、未重跑；M2-3 未做；M3 / D3 未裁定。
- 未做任何 best-response 或剥削性检验；抗针对只用机械探针，不是 best response。
- 单格手数少（8–16 手量级），bb/100 的量级由单手全下主导，**其排序不可当结论**。
- `coverage.executed_nodes` 为清单库存口径（125），不是本轮执行数。
- 仍未证：第三版的长期稳定性、2–9 人之外的外推、任何质量结论。
- 各次运行只获得过包络级预算批准，不构成对更大规模运行的授权。

## 10. 不得声称

- 不得把本文件表述为独立质量验证、质量签收或"已达标"。
- 不得把确定性补算当作新样本或新证据。
- 不得把三档 VPIP 的实测值表述为真实扑克频率、GTO 或均衡结论。
- 不得把本次读数用于反复调参后声称同一集合构成独立验证。
- 不得据此改写 `docs/20` 至 `docs/54f` 与任何已落盘证据。

## 11. 唯一下一步

**建议先裁定一件事：如何处置"目标带未命中"**。两条必须先说明的边界：

1. 本次运行只是开发证据；若调参重跑，每次重跑仍是开发证据，独立质量验证须另立预注册设计。
2. `mixed-local@3` 的三项翻前取值已被 v3 清单与三份回执落盘，且已进入其配置摘要。因此**改动 @3 的参数等于静默改写已落盘身份**——按纪律不可行；下一轮迭代必须**新开身份 `mixed-local@4`**，并配新清单与新目录。

读数的含义（不是方案）：唯一未达标项是紧密型，缺口 `0.0120`；跟注型距上限只剩 `0.0219`。因此"整体抬门槛"会先顶到上限，可动的是**只作用于紧密型的翻前跟注偏移**（现取值 0），但步长受"估算系统性高估 33–39%"制约，须由下一轮授权运行实测确认。
