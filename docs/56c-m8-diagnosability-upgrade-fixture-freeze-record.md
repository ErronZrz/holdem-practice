# 56c M8：可诊断性改造后的 v7 清单冻结回执

> 日期：2026-09-23。
>
> 本文件接在 `docs/56b` 之后，沿用 `docs/30` §10.1 D6 的编号族约定。`docs/20` 至 `docs/56b` 未被改写。
>
> 本文件记录：第三套冻结清单（v7）的**落盘对象**、**落盘后的只读核验**，以及**未落位到清单 JSON 的口径**（如实记账）。
>
> **本次冻结不产生任何读数**：未运行任何评测路径；`<v7>/runs/` 目录尚未创建。

## 1. 授权原文与边界

用户答复原文：

> 「A.」

指向的选项原文：

> 「A. 授权 I2：冻结 v7 清单」

| 项 | 内容 |
|---|---|
| 被授权 | 生成并落盘 v7 冻结清单到新外部证据目录 |
| 明确排除 | R1（运行）；任何评测路径实跑；改判据 / 门槛 / 机会分母 / 估值块 / 区间方法；策略身份切换；推送 |
| 保留 | v1–v6 全部工件**原样保留**；v7 之外不写入任何目录 |

## 2. 只读基线核验（冻结前）

```text
$ git status --short --branch
## master...origin/master [ahead 7]

$ git rev-parse HEAD
0ed11d9e50c30855d6296e3e5eab1faada1cd0b8

$ find /Users/bryanylliu/holdem-mixed-bot-validation-v7 -type f | wc -l
0
```

v6 五份工件 sha256（前 8 位）在冻结前后**均未变**：清单 `0cb1dad4`、失败阶段 A `86e84fed`、重跑阶段 A `332607ca`、阶段 B `7ad4318b`、补算 `5597d221`。

## 3. 落盘对象

| 项 | 值 |
|---|---|
| 路径 | `<v7>/input/frozen-fixture-manifest.json` |
| 文件 sha256 | `a68e9776c180400b80fccecd9698b0d12a32e7b95ddf650246f87c2c487cae61` |
| 文件大小 | `215,370 B` |
| `manifest_digest` | `68ab0ca3f73eeb566a08d78542cd9624c644b476ad59cd936033f3fc5afe4547` |
| `strategy_id` | `mixed-local@5`（**沿用既有身份**，未新开版本） |
| `config_digest` | `8896013bbcd873cd493c6319756c353365b49cc7b4e846e927c4216d6d1f82f2` |
| 可行动节点 | `125`（16 类；`node_id` 全部以 `-iqv2` 结尾） |
| 主种子 | `16` 个（与 `MIXED_IQV2_MAIN_SEEDS` 逐字一致） |
| `output_dir` | `<v7>/runs`（**不是** `<v7>`；复算摘要时用错口径会导致全部 MISMATCH） |
| `report_format` | 声明为 `mixed-validation.v2`（逐节点明细随报告产出） |

## 4. 落盘后的只读核验

| 核验项 | 结果 |
|---|---|
| 回读并复算 `manifest_digest` | `68ab0ca3…4547`，**与落盘前逐字一致** |
| 节点数与标识 | `125`；全部以 `-iqv2` 结尾 |
| 种子一致性 | 回读种子与 `MIXED_IQV2_MAIN_SEEDS` **逐字一致** |
| 重复落盘 | **被拒绝**：`清单文件已存在，不得覆盖` |
| 目录状态 | `<v7>` 文件数 `1`（仅清单）；`<v7>/runs/` **尚未创建** |
| v6 证据 | 五份 sha256 **全部未变** |

## 5. 身份差异（必须持续披露）

| 项 | 值 |
|---|---|
| 清单内 `code_identity`（**冻结时的 HEAD**） | `0ed11d9e50c30855d6296e3e5eab1faada1cd0b8`（`docs:` 提交） |
| 改造实际所在的代码提交 | `1ca5ddb360a0ec4a2cd138552ac2b62f8da151c0`、`7f3d76d7802feec8b9a913e4bf61052f983b2d1c`（两条 `feat:` 提交） |
| 差异内容 | 冻结时的 HEAD 与代码提交之间**只有文档差异**（代码内容与 `7f3d76d…` 相同） |

**结论口径**：清单里的 `code_identity` 是**冻结时的预承诺值**，不等于将来执行代码的提交。回执必须**如实标注**二者差异，**不得**把将来的报告表述为「由 `0ed11d9e` 代码产生」。

## 6. 未落位到清单 JSON 的口径（如实记账）

清单模型**不得加字段**（加字段会改变既有 v1–v6 清单的 `manifest_digest`）。因此下列口径**只存在于代码与文档层**，清单内没有对应字段：

| 口径 | 落在何处 |
|---|---|
| 机会分母（非强制节点等） | 文档层（沿用既有冻结定义） |
| 风格可分度的两个预先指定机会域 | 文档层 |
| X1：L4 区间方法与保守处置 | 代码常量与文档层 |
| X2：第 1/2/3/4 项的**固定估值种子块** = 第 1 个种子块 | `docs/56b` §7 + 实现层 |
| 观测工件的字段闭集与落盘格式 | 代码层 + `docs/56a` §4.3 / `docs/56b` §6 |

**这意味着：只读核对清单本身无法复原判定口径**；口径必须由 `docs/55`、`docs/55a`、`docs/56`、`docs/56a`、`docs/56b` 与实现常量共同确定。此点是既有结构的固有性质，本文件如实记账，不改动清单模型。

## 7. 契约、API 与默认策略

| 对象 | 状态 |
|---|---|
| 公开 API 字段 / 数据库列 / `history_json` | **零改动** |
| `mixed-validation.v1` / `v2` | **零改动**（本阶段未改代码） |
| `mixed-observations.v1` | **零改动**（本阶段未改代码） |
| `experiment` / `measurement` / 候选 A artifact schema | **零改动** |
| 策略身份 | **未新增**；仍为 `mixed-local@5` |
| 新增清单 | **是**：v7 清单（本文件的落盘对象） |
| `docs/37` §3.1 | **不触发** |
| 默认策略 | **仍为 `heuristic@1`** |
| v1–v6 外部证据 | **零触碰** |

## 8. 未做事项与仍未证

- **未做**：R1（运行 `A0` / `A` / 补算 / `B`）；任何评测路径实跑；任何阶段耗时实测；推送。
- **仍未证**：改造后判据是否**可判定**与是否**满足**；L4 是否可修复；任何质量结论——`docs/53c` 与 `docs/55g` 的结论**均未被改变**。
- **未消耗**：本阶段未消耗任何运行授权；也未触发 `docs/37` §3.1。

## 9. 不得声称

- 不得把本次冻结说成验证已开始、判据已可判定或结论已可预期。
- 不得把清单的 `manifest_digest` 说成质量证据或成本验收。
- 不得把清单内的 `code_identity` 说成将来执行代码的提交（§5）。
- 不得在冻结之后按任何读数修改种子、节点或字段；**不得**改判据、门槛、机会分母、估值块或区间方法。
- 不得用错 `output_dir` 口径（应为 `<v7>/runs`）复算摘要并据此声称不一致。
- 不得据此修改 `docs/20` 至 `docs/56b` 与任何已落盘工件；不得重跑 v6 冻结集合。

## 10. 本篇唯一下一步

v7 清单已冻结，运行前置（`docs/56a` §4.7）已全部就绪。**下一步是 R1：运行授权**，且顺序不可颠倒、不可合并：

1. **阶段 `A0`（可选的有界冒烟）**；
2. **阶段 `A`**（含逐节点明细与观测工件）；
3. **确定性补算**（`--allow-recheck`），与运行**各自单独一轮**；
4. **阶段 `B`**（须引用阶段 `A` 的 `completed` 回执通过门禁）。

**运行前必须先给实测估时（C2）**：估算不是读数；`docs/54h` 已有「估算偏差 41%」的先例，且本次新增的逐节点明细与观测工件**都没有先例可依托**。

**建议**：先只授权 **R1 的第 1 步（阶段 `A`）**，用它的真实 wall/RSS/输出体积读数去校准后续阶段的估时，再逐阶段授权。
