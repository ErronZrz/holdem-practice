# 57f M8：第四套样本集的清单冻结回执

> 日期：2026-09-23。
>
> 本文件接在 `docs/57e` 之后，沿用 `docs/30` §10.1 D6 的编号族约定。`docs/20` 至 `docs/57e` 未被改写。
>
> 本文件记录：第四套冻结清单（`strategy_id=mixed-local@6` + `-iqv3` 节点集 + `16` 个新主种子）的**落盘对象**、**落盘后的只读核验**，以及**未落位到清单 JSON 的口径**（如实记账）。
>
> **本次冻结不产生任何读数**：未运行任何评测路径；`<v8>/runs/` 目录**尚未创建**。

## 1. 授权原文与边界

用户答复原文：

> 「A. 授权 J2：先落 S1 口径前置文档并提交，再生成并落盘 v8 清单」

| 项 | 内容 |
|---|---|
| 被授权 | 落盘 `docs/57e` 并提交；随后生成并落盘第四套冻结清单到新的外部证据目录 |
| 明确排除 | 任何运行或补算（`J3`）；改判据 / 门槛 / 机会分母 / 估值块 / 区间方法；默认切换；推送之外的任何分支操作 |
| 保留 | v1–v7 全部工件**原样保留**；除 `<v8>/input/` 外不写入任何目录 |
| 未消耗 | 未消耗任何运行授权；`docs/37` §3.1 **未触发** |

## 2. 只读基线核验（冻结前）

```text
$ git status --short --branch
## master...origin/master

$ git rev-parse HEAD
22e669a879acbf862aa8eb8953ca6fa90c29bf2c

$ find /Users/bryanylliu/holdem-mixed-bot-validation-v8 -type f | wc -l
0（目录尚不存在）
```

工作区**零改动**。v7 七份工件与 v6 五份（含失败阶段 A `86e84fed`）sha256 在冻结前后**逐字未变**。

**冻结顺序已满足**：口径冻结（`docs/57e`，提交 `22e669a`）**先于**本次清单冻结完成，符合 `docs/57b` §5.6 的第 3 → 4 步顺序，未颠倒、未合并。

## 3. 落盘对象

| 项 | 值 |
|---|---|
| 路径 | `/Users/bryanylliu/holdem-mixed-bot-validation-v8/input/frozen-fixture-manifest.json` |
| 文件 sha256 | `484a6ce83687be436a2676081c6f9c35a7deed5c4e867b9bb95eec868141a225` |
| 文件大小 | `215,371 B` |
| `manifest_digest` | `0a1b1ea8985dfbd0f9d5ddeeedbca53e348c97ca0f5c31d9f2273e87f912e8f6` |
| `strategy_id` | `mixed-local@6` |
| `config_digest` | `b6678f65069c19779f683bd6a8df31f7bc3567cf813654d2758278c13151b4a1` |
| 可行动节点 | `125`（`16` 类；`node_id` 全部以 `-iqv3` 结尾） |
| 主种子 | `16` 个（与 `MIXED_IQV3_MAIN_SEEDS` 逐字一致） |
| `output_dir` | `<v8>/runs`（**不是** `<v8>`；复算摘要时用错口径会导致全部 MISMATCH） |
| `report_format` | 声明为 `mixed-validation.v2`（逐节点明细随报告产出） |
| `code_identity` | `22e669a879acbf862aa8eb8953ca6fa90c29bf2c`（**冻结时的 HEAD**，见 §5） |

## 4. 落盘后的只读核验

| 核验项 | 结果 |
|---|---|
| 回读并复算 `manifest_digest` | `0a1b1ea8…e8f6`，**与落盘前逐字一致** |
| 节点数与标识 | `125`；全部以 `-iqv3` 结尾 |
| 种子一致性 | 回读种子与 `MIXED_IQV3_MAIN_SEEDS` **逐字一致** |
| 重复落盘 | **被拒绝**：`清单文件已存在，不得覆盖` |
| 目录状态 | `<v8>` 文件数 `1`（仅清单）；`<v8>/runs/` **尚未创建** |
| v1–v7 证据 | 七套目录文件数 `5 / 4 / 4 / 4 / 4 / 5 / 7` **未变**；v7 七份 sha256 前 8 位 `a68e9776 / 980a62f6 / 464daefe / dd89e5fb / f2b83bc1 / 04a5acb2 / 215835e5` **逐字未变** |

## 5. 身份差异（必须持续披露）

| 项 | 值 |
|---|---|
| 清单内 `code_identity`（**冻结时的 HEAD**） | `22e669a879acbf862aa8eb8953ca6fa90c29bf2c`（`docs:` 提交） |
| 执行本次验证时将使用的代码提交 | **尚未确定**：`@6` 的实现提交为 `4fbb983…`（`feat:`），`-iqv3` 与种子的实现提交为 `720ee2d…`（`feat:`），之间与之后只有文档差异 |
| 差异内容 | 冻结时的 HEAD 与实际执行代码之间**只有文档差异**，代码内容与 `720ee2d…` 相同 |

**结论口径**：清单里的 `code_identity` 是**冻结时的预承诺值**，**不等于**将来执行代码的提交。回执必须**如实标注**二者差异，**不得**把将来的报告表述为「由 `22e669a` 代码产生」。

## 6. 未落位到清单 JSON 的口径（如实记账）

清单模型**不得加字段**（加字段会改变既有 v1–v7 清单的 `manifest_digest`）。因此下列口径**只存在于代码与文档层**：

| 口径 | 落在何处 |
|---|---|
| 机会分母（非强制节点等） | `docs/57e` §7.1（沿用既有冻结定义） |
| 风格可分度的两个预先指定机会域 | `docs/57e` §7.2 与 §9 的 `X5` |
| L4 区间方法与保守处置 | `docs/57e` §8.1（`docs/56a` §6 原样沿用） |
| 固定估值种子块 | `docs/57e` §8.2 与 §9 的 `X2`（本轮取值 = 第 `1` 个主种子块 `2992123152`） |
| 上一轮事后固定的口径（`X3`–`X9`） | `docs/57e` §9（**本轮首次在读数前成文**） |
| 三分域自然分区 | `docs/57e` §9 的 `X4` |
| 观测工件的字段闭集与落盘格式 | 代码层 + `docs/56a` §4.3 / `docs/56b` §6 |

**与上一轮的关键差别（如实记账）**：v7 的 `X3`–`X9` **只在判定完成后**才成文（`docs/56h` §4.2）；本轮它们**在任何读数之前**即已写入 `docs/57e`。**只读核对清单本身仍无法复原判定口径**——此点是既有结构的固有性质，本文件如实记账，不改动清单模型。

## 7. 契约、API 与默认策略

| 对象 | 状态 |
|---|---|
| 公开 API 字段 / 数据库列 / `history_json` | **零改动** |
| `mixed-validation.v2` / `mixed-observations.v1` / `mixed-recheck.v1` | **零改动**（本次未改代码） |
| `experiment` / `measurement` / 候选 A artifact schema | **零改动** |
| 清单模型 / v1–v7 冻结清单 | **零改动**（摘要逐字未变） |
| 策略身份 | **未新增**（仍为 `mixed-local@6`） |
| 新增清单 | **是**：第四套清单（本文件的落盘对象） |
| `docs/37` §3.1 | **不触发** |
| 默认策略 | **仍为 `heuristic@1`** |
| v1–v7 外部证据 | **零触碰** |

## 8. 未做事项与仍未证

- **未做**：`J3`（运行 `A0` / `A` / 补算 / `B`）；任何评测路径实跑；任何阶段耗时实测；完整标定；默认切换。
- **仍未证**：第四套样本集上的**任何**判据读数与成本读数；`@6` 的强度、可剥削性、抗针对性；域二与 L4 是否改善；根因 C 是否成立。
- **不得把本清单的摘要说成质量证据**；不得把「口径已在读数前成文」说成判据会满足。
- **未消耗**：本阶段未消耗任何运行授权。

## 9. 不得声称

- 不得把本次冻结说成验证已开始、判据已可判定或结论已可预期。
- 不得把清单的 `manifest_digest` 说成质量证据或成本验收。
- 不得把清单内的 `code_identity` 说成将来执行代码的提交（§5）。
- 不得在冻结之后按任何读数修改种子、节点或字段；**不得**改判据、门槛、机会分母、估值块或区间方法。
- 不得用错 `output_dir` 口径（应为 `<v8>/runs`）复算摘要并据此声称不一致。
- 不得把 `docs/57e` 的锁定说成对 `docs/56h` 的追溯改写，也**不得**说成质量改善。
- 不得据此修改 `docs/20` 至 `docs/57e` 与任何已落盘工件；不得重跑 v6 / v7 冻结集合。

## 10. 本篇唯一下一步

第四套清单已冻结；`docs/57b` §5.6 的**运行前置已全部就绪**。**下一步是 `J3`：运行授权**，且顺序不可颠倒、不可合并：

1. **阶段 `A0`（可选的有界冒烟）**；
2. **阶段 `A`**（含逐节点明细与观测工件）；
3. **确定性补算**（`--allow-recheck`），与运行**各自单独一轮**；
4. **阶段 `B`**（须引用阶段 `A` 的 `completed` 回执通过门禁）。

**运行前必须先给实测支撑的估时（`R3`）**：估算不是读数；`docs/54h` 已有「估算偏差 `41%`」先例，而**峰值 RSS 从来没有过可用估算**（v7 阶段 A 相对 v6 `+77%`、阶段 B 补算 `917.38 MiB` 均**未归因**）。

**建议**：先只授权 **`J3` 的第 1 步（阶段 `A`）**，用它的真实 wall / RSS / 输出体积读数去校准后续阶段的估时，再逐阶段授权。
