# 56g M8：v7 阶段 B 运行与确定性补算回执

> 日期：2026-09-23。
>
> 本文件接在 `docs/56f` 之后，沿用 `docs/30` §10.1 D6 的编号族约定。`docs/20` 至 `docs/56f` 未被改写。
>
> 本文件记录：v7 **阶段 B 运行**的实测读数、**阶段 B 确定性补算**的实测读数、观测工件覆盖在满规模上的首次验证，以及包络对照与估算偏差的如实披露。
>
> **本篇不给出任何质量结论**：阶段 B 的 `completed` 与补算的 `completed` 都是**机制与可复现性**结论，**不是**质量证据，也不构成任何签收。

## 1. 授权原文与边界

用户答复原文依次为：

> 「请运行阶段 B.」——授权**阶段 B 运行**（引用阶段 A 的 `completed` 回执，`--family-set=iqv --emit-observations`，写入 `<v7>/runs/stage-b/`）。
>
> 「A.」——授权**阶段 B 的确定性补算**，写入 `<v7>/runs/stage-b-recheck/`。
>
> 「A.」——授权写本回执并**提交推送**。

| 项 | 内容 |
|---|---|
| 被授权 | 跑阶段 B；跑阶段 B 补算；写本回执；提交 `docs:` 并推送 |
| 明确排除 | L1–L5 判据判定与签收；抗针对与剥削性检验；改判据 / 门槛 / 机会分母 / 估值块 / 区间方法；改冻结清单；改默认策略；给报告加字段 |
| 保留 | 冻结清单（v7）与 v1–v6 全部工件**原样保留**；v6 失败阶段 A 回执 `86e84fed…` **原样保留** |

## 2. 只读基线核验（两次运行之前）

```text
$ git status --short --branch
## master...origin/master

$ git rev-parse HEAD
53ceefcd4f4f139a72505379c2e59a0cf7436a10
```

两次运行期间 HEAD **未变**（均为 `53ceefc`）。目标目录 `<v7>/runs/stage-b/` 与 `<v7>/runs/stage-b-recheck/` 在各自运行前**均不存在**。

**运行入口（原样命令）**：

```text
阶段 B：
  python -m tests.mixed_bot_validation <v7清单> B <v7>/runs/stage-b \
    --allow-stage=B --stage-a-receipt=<v7>/runs/stage-a/mixed-validation-stage-A.json \
    --family-set=iqv --emit-observations

阶段 B 补算：
  python -m tests.mixed_bot_recheck <v7清单> <v7>/runs/stage-b/mixed-validation-stage-B.json \
    <v7>/runs/stage-b-recheck --allow-recheck
```

两次均以**后台执行 + 轮询**方式完成（不中断进程、不丢样本）。

## 3. 阶段 B 运行：实测读数（真实读数）

| 项 | 实测 |
|---|---|
| 报告路径 | `<v7>/runs/stage-b/mixed-validation-stage-B.json` |
| 报告 sha256 | `f2b83bc1a7cbd0f451c72efec3e818ffdc38ef8521ada00f0d7350035ae97c4c` |
| 报告大小 | `20,300,681 B`（`19.36 MiB`）；`output_bytes` **自洽** |
| 观测路径 | `<v7>/runs/stage-b/mixed-observations-stage-B.json` |
| 观测 sha256 | `04a5acb2e57058f9929f3f39e0c0e64d2d1b490032f3bff3de2ea300a5043b13` |
| 观测大小 / 手数 | `45,525,667 B`（`43.42 MiB`）/ `33,792` 手；`output_bytes` **自洽** |
| `schema_version` / `stage` / `status` / `violations` | `mixed-validation.v2` / `B` / **`completed`** / **`[]`** |
| `manifest_digest` | `68ab0ca3f73eeb566a08d78542cd9624c644b476ad59cd936033f3fc5afe4547`（与冻结值**逐字一致**） |
| `config_digest` | `8896013bbcd873cd493c6319756c353365b49cc7b4e846e927c4216d6d1f82f2`（与冻结值**逐字一致**） |
| 覆盖 | 成本格 `288/288`、成本样本 `80,000/80,000`、补充 `240/240`、对抗 `33,792/33,792`、`not_run 0` |
| 决策延迟 | `80,000` 样本；p50 `0.277` / p95 `0.367` / p99 `0.515` / max `13.923` ms；**超 100 ms 计数 `0`** |
| 逐节点明细 | `18,000` 行（`125 × 3 × 3 × 16`）；标识全部以 `-iqv2` 结尾；出现 `16` 个不同种子块 |
| 对抗对照 | 计划/完成/截断 = `33,792 / 33,792 / 0`；逐手行 `33,792`；种子块 `16` |
| 行为聚合 | `direct_distributions=1125`、`styles=3`、`categories=48`、`style_js_distances=3` |
| `limitations` | `5` 条；含「十六个固定主种子块…」，**未出现**四块或八块措辞 |
| 资源 | wall **`511.948 s`**、CPU `506.303 s`、峰值 RSS `517,177,344 B` = **`493.22 MiB`**、单进程不并行 |

**退出码未捕获**（两次运行均为后台执行，启动 shell 已退出）：据实记为「stderr 为空 + 工件齐备 + `status=completed`」。

## 4. 阶段 B 确定性补算：实测读数（真实读数）

| 项 | 实测 |
|---|---|
| 报告路径 | `<v7>/runs/stage-b-recheck/mixed-recheck-stage-B.json` |
| 报告 sha256 | `215835e5c89eed73d58c3cda60d809b88d5a016a732822eefa13eb8a8bbcbb6d` |
| 报告大小 | `324,853 B`；`output_bytes` **自洽** |
| `schema_version` / `source_stage` / `status` | `mixed-recheck.v1` / `B` / **`completed`** |
| `consistency.source_receipt_sha256` | `f2b83bc1…7c4c`（与阶段 B 回执**逐字一致**） |
| `consistency.hands_compared` | **`33,792`**（与阶段 B 回执完成手数一致） |
| `consistency.net_chips_exact` / `behavior_exact` | **`True`** / **`True`** |
| `manifest_digest` / `config_digest` | 与冻结值**逐字一致** |
| 顶层字段 / `consistency` 字段 | `14` 个 / `6` 个，与既有补算报告**同集合**（无新增字段） |
| 汇总 | `aggregates` `384` 行、`worst_opponents` `6` 行 |
| 行为 | `direct_distributions=1125`、`style_js_distances=3`、`categories=48`（与阶段 B 回执一致） |
| `limitations` | `4` 条，含「按主种子块统计只有**十六个**固定块…」 |
| 资源 | wall **`602.846 s`**、CPU `577.154 s`、峰值 RSS `961,937,408 B` = **`917.38 MiB`**、单进程不并行 |

## 5. 观测工件覆盖：满规模首次验证

阶段 A 与阶段 B 的补算执行代码**均已包含**观测工件核对（`docs/56e` 的 F4）。阶段 B 的只读复核：

| 核验项 | 结果 |
|---|---|
| 由回执目录推导的观测路径 | `<v7>/runs/stage-b/mixed-observations-stage-B.json` |
| 该路径是否存在 | **是** |
| 载入手数 | **`33,792`** |
| 工件的 `manifest_digest` 与回执一致 | **是** |
| 逐手比对（手数/顺序/分组键/净筹码） | **`33,792` 手，未抛错** |

因此：阶段 A 与阶段 B 的补算**均包含观测工件核对且未报不一致**。

**边界（沿用 `docs/56e` §6，不因规模变大而放宽）**：补算报告**不带**「观测已核对」标记；工件**缺失**时该项静默跳过；**未**核对工件的 `output_bytes` 自洽性，**未**比对动作序列 / 各街底池 / 牌力档。

## 6. 包络对照与估算偏差（**估算不是读数**）

代码只把包络写进局限说明文本，**不据此改 `status`**，因此包络对照**必须人工做**：

| 包络项 | 限值 | 实读 | 结论 |
|---|---|---|---|
| 阶段 B wall | ≤ `1800 s` | `511.948 s` | 包络内 |
| 阶段 B 峰值 RSS | ≤ `1 GiB` | `493.22 MiB` | 包络内 |
| 保留文件合计 | ≤ `100 MiB` | `20,300,681 + 45,525,667 = 65,826,348 B` = `62.78 MiB` | 包络内 |
| 单进程 / 不并行 | 要求 | `single_process=True`、`parallel=False` | 符合 |

**补算路径没有声明包络**，故其读数**无包络可对照**；其峰值 RSS `917.38 MiB` 与 `1 GiB` 之间余量仅约 `10%`，**未归因**（`docs/56f` 的阶段 A 补算为 `140.09 MiB`，两者不是同一负载，**不得**据此推断趋势或外推到其他规模）。

| 项 | 估算（`docs/56d` §6，估算） | 实读 | 偏差 |
|---|---|---|---|
| 观测工件 | ≈ `42.4 MiB` | `43.42 MiB` | `+2.4%` |
| 主报告 | ≈ `15.2 MB` | `20.30 MB` | **`+33.6%`** |
| 合计 | ≈ `55–57 MiB` | `62.78 MiB` | `+10% ~ +14%` |
| 阶段 B wall | `480–650 s` | `511.948 s` | 落在区间内 |
| 阶段 B 峰值 RSS | 无外推依据 | `493.22 MiB` | — |
| 阶段 B 补算 wall | 本轮运行前估「≈`500 s` 量级」 | `602.846 s` | **`+20.6%`** |

**采样不是权威口径**：轮询期间以 `ps` 采样到的峰值（阶段 B `449.5 MiB`、补算 `136 MiB`）**均低于**报告口径的进程内自测峰值（`493.22 MiB`、`917.38 MiB`）；以**进程内自测**为准。

## 7. 身份差异（必须持续披露）

| 项 | 值 |
|---|---|
| 两份 v7 阶段 B 工件中的 `code_identity` | `0ed11d9e50c30855d6296e3e5eab1faada1cd0b8`（**冻结时的 HEAD**） |
| 阶段 B 运行与补算**实际执行的代码** | `53ceefcd4f4f139a72505379c2e59a0cf7436a10` |
| 冻结清单是否改动 | **零改动**：sha256 仍为 `a68e9776…ae61`，`manifest_digest` 仍为 `68ab0ca3…4547` |

**不得**把本轮任何工件说成由 `0ed11d9e` 产生。

## 8. 契约、API 与默认策略

| 对象 | 状态 |
|---|---|
| 公开 API 字段 / 数据库列 / `history_json` | **零改动** |
| `mixed-validation.v1` / `v2` / `mixed-observations.v1` / `mixed-recheck.v1` | **零改动**（无新增字段、无文本改动） |
| 清单模型 / 冻结清单 | **零改动**（摘要逐字未变） |
| 策略身份 | **未新增**；仍为 `mixed-local@5` |
| `docs/37` §3.1 | **不触发** |
| 默认策略 | **仍为 `heuristic@1`** |
| v1–v6 外部证据 | **零触碰**（清单与全部回执 sha256 逐条复核未变） |

## 9. 未做事项与仍未证

- **未做**：L1–L5 判据判定；任何签收；抗针对与剥削性检验；默认切换。
- **仍未证**：本质量结论——`docs/53c` 与 `docs/55g` 的「质量未获支持」**均未被本轮改变**；L3 六项与 L4 是否可判定、是否满足，**均未判定**。
- **补算不是新样本、不是新证据**：`33,792` 手与阶段 B 是**同一批**手，**不增加**任何样本量。
- **不得**把 `917.38 MiB` 当作阶段 B 的 RSS 结论，也**不得**把它当作任何规模的容量承诺。
- 本轮**未消耗**任何已登记的 A9 授权；阶段 A 与阶段 B 的运行授权已分别使用完毕。

## 10. 不得声称

- 不得把阶段 B 的 `completed` / `violations=[]` / `0` 次超 `100 ms` 说成质量证据或验证通过。
- 不得把补算的 `completed` 说成质量证据；它只证明**同一批对局可确定性复现**。
- 不得把「包络内」说成成本验收或预算保证；包络对照是**人工比对**，代码不据此改判 `status`。
- 不得把 §6 的任何估算说成读数，也不得据实读反推估算方法的可靠性。
- 不得把 §5 的覆盖扩大到动作序列、各街底池或牌力档。
- 不得删除或改写 v6 的失败阶段 A 回执，也不得把它说成「未发生过」。
- 不得据此修改 `docs/20` 至 `docs/56f` 与任何已落盘工件；不得重跑 v1–v6 的冻结集合。

## 11. 本篇唯一下一步

**按 `docs/55` §7 的 L1–L5 判据出具判定与签收**（**数值照抄、不重标定**；X1 区间方法与 X2 估值块口径已锁定，**产生读数后不得改**），并交用户签收。

判定落地**之前**，须先把「将要照抄的 L1–L5 数值与门槛清单」原样列出供核对；**不得**自行重标定、不得更换估值块、不得改用别的区间方法。
