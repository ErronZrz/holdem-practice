# 57i M8：第四次验证阶段 B 的运行回执

> 日期：2026-09-23。
>
> 本文件接在 `docs/57h` 之后，沿用 `docs/30` §10.1 D6 的编号族约定。`docs/20` 至 `docs/57h` 未被改写。
>
> 本文件记录：`J3` 第 3 步（阶段 `B`）的运行命令、门禁引用、实测读数与估时对照。
>
> **本篇不给出任何质量结论**：阶段 `B` 的 `completed` 与 `violations=[]` 是**机制与门禁**结论，**不是**质量证据。本篇**不做任何判据计算**（L1–L5 的判定属判定轮次）。

## 1. 授权原文与边界

用户答复原文：

> 「授权下一步。」

其唯一指向即本篇对象：按 `docs/57f` §10 / `docs/57b` §5.6 的顺序，阶段 `A` 补算之后的下一步**只能是阶段 `B`**。

**运行前已先给出实测支撑的估时（`R3`）**：wall `≈450–550 s`（点估 `468 s`，依据 v7 阶段 `B` 的 `511.948 s` 乘本轮阶段 `A` 相对 v7 阶段 `A` 的 `0.914`）、峰值 RSS `≈500 MiB 量级`（**无可靠外推依据**）、输出 `≈67 MiB`。

| 项 | 内容 |
|---|---|
| 被授权 | 单独一轮运行阶段 `B`（清单、阶段 `A` 回执均已冻结） |
| 明确排除 | 阶段 `B` 的确定性补算；任何判据计算；改判据 / 门槛 / 机会分母 / 估值块 / 区间方法；改清单或种子；重跑阶段 `A` 或其补算；重跑 v6 / v7 冻结集合；默认切换 |
| 包络 | wall ≤1800 s；RSS ≤1 GiB；输出 ≤100 MiB；单进程不并行 |
| 未消耗 | 未消耗阶段 `B` 补算与判定轮次的授权；`docs/37` §3.1 **未触发** |

## 2. 只读基线核验（运行前）

```text
$ git status --short --branch
## master...origin/master

$ git status --porcelain=v1 -uall | wc -l
0

$ git rev-parse HEAD
9c7c77f7e97265543685822b7c49db81a5793fe3
（与 origin/master 逐字一致）

$ find /Users/bryanylliu/holdem-mixed-bot-validation-v8 -type f | wc -l
4（清单 + 阶段 A 报告与观测 + 阶段 A 补算）
```

工作区**零改动**；v8 四份既有工件与 v1–v7 全部证据在运行前后**逐字未变**。

## 3. 运行命令、门禁与工件

```text
$ uv run python -m tests.mixed_bot_validation \
    /Users/bryanylliu/holdem-mixed-bot-validation-v8/input/frozen-fixture-manifest.json \
    B /Users/bryanylliu/holdem-mixed-bot-validation-v8/runs/stage-b \
    --allow-stage=B --family-set=iqv --emit-observations \
    --stage-a-receipt=/Users/bryanylliu/holdem-mixed-bot-validation-v8/runs/stage-a/mixed-validation-stage-A.json
（退出码 0；单进程不并行）
```

**门禁（实读代码后如实记录）**：入口在运行前校验阶段 `A` 回执**存在**、`stage == "A"` 且 `status == "completed"`，否则**不启动**（缺失即抛「阶段 B 需要阶段 A 的实测回执」）。本轮传入的正是 sha 为 `b9642098…3319` 的阶段 `A` 报告。

**如实记账**：报告 schema **不记录**阶段 `A` 回执的路径与摘要（口径**不新增字段**），因此「阶段 `B` 由哪一份阶段 `A` 回执放行」这一链接**只由本篇的命令记录承载**，无法从阶段 `B` 工件本身复原。

| 工件 | sha256 | 大小 |
|---|---|---|
| `<v8>/input/frozen-fixture-manifest.json`（**未改动**） | `484a6ce83687be436a2676081c6f9c35a7deed5c4e867b9bb95eec868141a225` | `215,371 B` |
| `<v8>/runs/stage-a/*`（**三份均未改动**，含补算） | `b9642098…3319` / `f776ab7c…8c0a` / `974a019c…11b5` | — |
| `<v8>/runs/stage-b/mixed-validation-stage-B.json`（**本轮新增**） | `6d3838fb09d9b419bb4d9f16b510a692989428c2064c7ef4716fa8b682fff27b` | `20,371,657 B` |
| `<v8>/runs/stage-b/mixed-observations-stage-B.json`（**本轮新增**） | `087e0d024658c7e75038977f82c5ab392754c3688fe5f56cce3990fc09425132` | `46,037,249 B` |

- **首跑成功**：阶段 `B` **一次通过**，未发生失败重跑；`<v8>` 文件数由 `4` 变为 `6`，既有工件**零改写**。

## 4. 实测读数

### 4.1 身份与自洽

| 项 | 读数 |
|---|---|
| `schema_version` / `stage` / `status` | `mixed-validation.v2` / `B` / `completed` |
| `violations` | `[]` |
| `strategy_id` | `mixed-local@6` |
| `config_digest` | `b6678f65069c19779f683bd6a8df31f7bc3567cf813654d2758278c13151b4a1` |
| `manifest_digest` | `0a1b1ea8985dfbd0f9d5ddeeedbca53e348c97ca0f5c31d9f2273e87f912e8f6`（与冻结清单一致） |
| `code_identity` | `22e669a879acbf862aa8eb8953ca6fa90c29bf2c`（**冻结时的预承诺值**，见 §6） |
| 观测工件同源 | `mixed-observations.v1`、`strategy_id=mixed-local@6`、`manifest_digest` 与报告一致 |

### 4.2 覆盖（`executed_nodes` 仍是**清单库存口径**）

| 项 | 读数 |
|---|---|
| 成本格 / 成本样本 | `288 / 288`、`80,000 / 80,000` |
| 补充场景 | `240 / 240` |
| 对抗对照 | `33,792 / 33,792` 手 |
| `executed_nodes` / `planned_nodes` / `not_run_nodes` | `125` / `128` / `0`（`125` 为清单库存口径，**不是**本轮执行数） |
| 结构性不适用 | `3` |
| 截断手数 | `0`（报告与观测两处一致） |

### 4.3 决策路径（样本 `80,000`）

| 项 | 读数 |
|---|---|
| 超 `100 ms` 次数 | **`0`** |
| p50 / p95 / p99 / max | `0.2767` / `0.3598` / `0.4818` / `15.7714` ms |
| 分组维度 | 人数（`2–9` 各 `10,000`）、街（`4` 组）、风格（`3` 组）、深度（`3` 组），共 `18` 组，各组超时 **`0`** |

### 4.4 对抗对照与行为分区

| 项 | 读数 |
|---|---|
| 对抗手数 | `completed_hands = 33,792`、`planned_hands = 33,792`、`truncated_hands = 0` |
| 种子块 | `16` 个，与清单 `seeds` **集合相等**（实读比对为 `True`） |
| 区间口径 | `interval_status = not-estimated-small-fixed-seed-set`（原样落盘） |
| 观测工件 | `33,792` 手，与对抗手数一致 |
| 行为分区条目数 | `direct_distributions 1,125`、`style_js_distances 3`、`categories 48`、`nodes 18,000` |

- 上表只证明**覆盖齐全、条目数自洽**；**取值未在本篇解读**——风格可分度（域一 / 域二）与逐人数不劣化的判定**留待判定轮次**。
- `matches_note` 原样落盘：「机械探针不是 best response；胜过随机或旧启发式不等于强度认证。」

### 4.5 资源（包络为**人工比对**，代码不据此改 `status`）

| 项 | 读数 | 包络 | 余量 |
|---|---|---|---|
| wall | **`500.907 s`**（≈`8.35 min`） | ≤1800 s | 约 `28%` 已用 |
| CPU | `496.457 s` | — | — |
| 峰值 RSS | **`465.64 MiB`**（`488,259,584 B`） | ≤1 GiB | 约 `55%` 余量 |
| 输出 | 报告 `20,371,657 B` + 观测 `46,037,249 B` = **`66,408,906 B`**（≈`63.33 MiB`） | ≤100 MiB | 约 `37%` 余量 |
| 并发 | `single_process=true`、`parallel=false` | 单进程不并行 | 满足 |

**未归因项（如实记账）**：观测工件实测 `1,362 B/手`，与本轮阶段 `A` 的 `1,403 B/手` **不同负载**（阶段 `B` 含 `33,792` 手）；两者差值**未有解释**，本文件**不声称已归因**。

## 5. 估时对照（估算 vs 实测）

| 项 | 运行前估算（`R3`） | 实测 | 偏差 |
|---|---|---|---|
| 阶段 `B` wall | `450–550 s`（点估 `468 s`） | `500.907 s` | 落在区间内；对点估 **`+7.0%`** |
| 阶段 `B` 峰值 RSS | `≈500 MiB` 量级（**无可靠外推依据**） | `465.64 MiB` | **`-6.9%`**，同一量级 |
| 输出合计 | `≈67 MiB` | `63.33 MiB` | **`-5.5%`** |

**本轮结论**：三项均落在估算区间或量级内，但**不得**据此把估算表述为读数，也**不得**据此为任何后续规模承诺耗时或内存——`docs/54h` 的「估算偏差 `41%`」先例、`docs/56f` 的「补算 RSS 不得外推」、以及 v7 阶段 `B` 补算 `917.38 MiB`（**未归因**）的现状**均未改变**。

## 6. 身份差异（必须持续披露）

| 项 | 值 |
|---|---|
| 报告中的 `code_identity`（**冻结时的 HEAD**） | `22e669a879acbf862aa8eb8953ca6fa90c29bf2c`（`docs:` 提交） |
| `@6` 与 `-iqv3` 的**实际实现提交** | `4fbb9833425475c593916969ba0605fc3525a2c9`（`feat:`）、`720ee2d57d69e5e1af75013456331e6413484ac6`（`feat:`） |
| 差异内容 | 冻结时的 HEAD 与实际执行代码之间**只有文档差异**，代码内容与 `720ee2d…` 相同 |

**不得**把本次报告表述为「由 `22e669a` 代码产生」。

## 7. 契约、API 与默认策略

| 对象 | 状态 |
|---|---|
| 公开 API 字段 / 数据库列 / `history_json` | **零改动** |
| `mixed-validation.v2` / `mixed-observations.v1` / `mixed-recheck.v1` | **零改动**（本次无代码改动，无新增字段） |
| `MixedFixtureManifest` / 冻结清单 | **零改动**（摘要逐字未变） |
| 策略身份 | **未新增**（仍为 `mixed-local@6`） |
| 新增清单 | **否** |
| `experiment` / `measurement` / 候选 A artifact schema | **零改动** |
| `docs/37` §3.1 | **不触发** |
| 默认策略 | **仍为 `heuristic@1`**（未获质量签收，必须保持） |
| v1–v7 外部证据 / 封存 campaign 工件 | **零触碰** |

## 8. 未做事项与仍未证

- **未做**：阶段 `B` 的确定性补算；**任何判据计算**；任何质量结论；完整标定；默认切换。
- **仍未证**：L1–L5 的**任何判据读数**（阶段 `B` 已是主来源，但**判定未做**）；`@6` 的强度、可剥削性、抗针对性；域二与 L4 是否改善；根因 C 是否成立；阶段 `B` 补算的 wall / RSS。
- **阶段 `B` 的 `completed` 不是质量证据**：覆盖 `288/288`、`80,000` 样本、毫秒级延迟、`violations=[]` 均只证明**机制与门禁**。
- **抗针对与剥削性检验仍未做**：探针族只是机械对照，**不是** best response；`shared-board` 的读数**只能是**「读数存在、可利用性未检验」。
- **不得把 v8 与 v6 / v7 的读数直接比较**（不同节点集、不同种子集、不同策略身份）。
- **未消耗**：本轮未消耗阶段 `B` 补算与判定轮次的授权；全程只获**包络级**预算批准。

## 9. 不得声称

- 不得把阶段 `B` 的 `completed`、`violations=[]`、覆盖 `288/288` 或毫秒级延迟读数说成**质量证据**、判据达标或「已达生产门槛」。
- 不得把阶段 `B` 跑完说成验证通过或结论可预期；**判定轮次尚未开始**。
- 不得把本次估时对照说成「估算已被验证」；不得为补算或任何后续规模承诺耗时或内存。
- 不得把本次报告说成由 `22e669a` 代码产生（§6）。
- 不得声称阶段 `B` 工件本身能证明它由哪一份阶段 `A` 回执放行（§3）。
- 不得把 `docs/57e` 的锁定说成 v7 的预注册口径或质量改善。
- 不得据此修改 `docs/20` 至 `docs/57h` 与任何已落盘工件；不得重跑本次阶段 `B`；不得重跑阶段 `A` 或其补算；不得重跑 v6 / v7 冻结集合；不得改写 v6 的失败阶段 A 回执。

## 10. 本篇唯一下一步

`J3` 第 3 步（阶段 `B` 运行）已完成并如实记账。按 `docs/57f` §10 与 `docs/57b` §5.6 的顺序，**下一步是阶段 `B` 的确定性补算**（`--allow-recheck`，须单独授权；它与运行**各自单独一轮**，不得合并）：

```bash
uv run python -m tests.mixed_bot_recheck \
  <v8>/input/frozen-fixture-manifest.json \
  <v8>/runs/stage-b/mixed-validation-stage-B.json \
  <v8>/runs/stage-b-recheck --allow-recheck
```

**运行前须先给实测支撑的估时（`R3`）**：可依托 v7 阶段 `B` 补算的 `602.846 s` / 峰值 RSS `917.38 MiB`（**未归因**，余量仅约 `10%`），但该路径**无自己的声明包络**，`R4(a)` 只是**补声明**，**不得**外推到其他规模。补算完成之后才是**判据判定与用户签收**（按 `docs/57e` 锁定的判据与口径）。
