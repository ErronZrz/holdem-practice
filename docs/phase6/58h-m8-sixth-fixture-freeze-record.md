# 58h M8：第六套冻结清单的落盘回执（`J2`）

> 日期：2026-09-24。
>
> 本文件接在 `docs/phase6/58g-m8-rss-envelope-caliber-extension-record.md` 之后，沿用 `docs/30` §10.1 D6 的编号族约定。归档路径按 `docs/README.md` 的维护约定：本文件属第四次验证族的**共用族号 `58`**（先例 `58a`–`58g`），故取 **`58h`**。
>
> 本文件是 `docs/phase6/58c` §13 冻结顺序的**第 4 步**（`J2`）的回执：按已冻结的判据与口径（`docs/phase6/58f`）、已实施的身份（`docs/phase6/58d`）与样本集（`docs/phase6/58e`），在**新的外部证据目录**落盘第六套冻结清单。
>
> **本文件不授权任何运行**：`J3`（阶段 `A` / `B` + 确定性补算）仍需单独授权；本文件也**不产生**任何读数。

## 1. 授权原文与边界

用户裁定（选项照录）：

> 「A. 授权 `J2`，目录用 `-v10`」

该选项随附理由由助手在选项中提出、随选项被选定，此处照录以免与用户原话混淆：

> 「与既有 `-v6`…`-v9` 命名链一致，第五次验证自然为 `-v10`；只创建 `input/`，`runs/` 留给 `J3`；清单由代码生成、不手写不修剪，已存在则拒绝覆盖；回执含文件 sha256 / `manifest_digest` / 节点与种子一致性核验 / `code_identity` 差异披露。不消耗运行授权、不运行任何阶段。」

| 项 | 内容 |
|---|---|
| 被授权 | **工作树外写入**：新建外部证据目录并用 `-v10` 命名；只创建 `input/` 并落盘第六套冻结清单；随后出冻结回执 |
| 明确排除 | 任何运行或补算（`J3`）；创建 `runs/`；改任何代码；改判据 / 门槛 / 机会分母 / 估值块 / 区间方法 / 包络常量；改任一判定钉子；默认策略切换；推送 |
| 转写纪律 | 清单由代码生成，**不手写、不修剪、不排序**；目标文件已存在则**拒绝覆盖** |
| 未消耗 | **未消耗**任何运行授权；`docs/phase4/37` §3.1 **未触发**（未触及参考实现、保守判据、`equity`、采样数与固定种子） |

## 2. 只读基线核验（冻结前）

```text
$ git status --short --branch
## master...origin/master

$ git status --porcelain=v1 -uall | wc -l
0

$ git rev-parse HEAD
d46c780a3c962bf17dcbe62443e0bde926caf372

$ git rev-parse origin/master
d46c780a3c962bf17dcbe62443e0bde926caf372
```

落盘前工作区**零改动**，HEAD 与 `origin/master` **一致**（无领先/落后）。目标目录 `/Users/bryanylliu/holdem-mixed-bot-validation-v10` 在落盘前**不存在**（`ls -d` 返回 `No such file or directory`）；既有九套证据目录的文件数为 `5 / 4 / 4 / 4 / 4 / 5 / 7 / 7 / 8`，与 `docs/phase6/58f` §2 记录的数值一致。

回归基线（落盘前实跑，用于确认冻结基线未漂移）：`uv run ruff check .` → `All checks passed!`；`cd backend && uv run pytest -q` → `1271 passed, 3 skipped`。

## 3. 落盘内容

| 项 | 值 |
|---|---|
| 路径 | `/Users/bryanylliu/holdem-mixed-bot-validation-v10/input/frozen-fixture-manifest.json` |
| 字节数 | `215375` |
| `sha256` | `7cf406fe59bd64a7f381f52a6d01f489f4374dd0630dd83a5a857c09e49a9190` |
| `manifest_digest` | `296af53055c8a2e2f8c5310e2d93cf252290fcfc4cfef5511574397d47a5782f` |
| `config_digest` | `393ca127fc16ef47d22cab29d287f43dde5aa94ee4a5cbb34ed15b4a12f627a2` |
| `strategy_id` | `mixed-local@8` |
| `code_identity` | `d46c780a3c962bf17dcbe62443e0bde926caf372` |
| `schema_version` | `mixed-bot-fixtures.v1` |
| `report_format` | `mixed-validation.v2：字段闭集，输出目录只创建不覆盖，存在同名文件即拒绝运行` |
| 节点 | `125` 个，`16` 个类别，标识一律带 `-iqv5` 后缀 |
| 种子 | `16` 个主种子，清单序第 `1` 块 = `3008262424` |
| 落盘方式 | `write_frozen_manifest(frozen_iqv5_manifest(...), input_dir)`；目录按需创建，目标文件不存在则创建，存在则**拒绝覆盖** |

清单序主种子（照录，顺序即清单序）：

```text
3008262424, 1713517322, 1076959820, 2076521261, 990749861, 4010473074,
4073374213, 3841665689, 1227494703, 1241820418, 2584526233, 623151548,
3456817561, 1667776298, 2892543175, 1056690037
```

**清单文件本身不含 `manifest_digest` 字段**：该值是 `MixedFixtureManifest.digest()` 的产物，由回执与阶段报告引用。这一点在引用该值时必须注意，**不得**声称清单里写有它。

落盘范围：仅创建 `input/` 与其中一份清单文件；`-v10` 目录下**只有 `input/`**，**未创建 `runs/`**（留给 `J3`）。

**首次调用的失败记账**：首调用因导入路径错误（相对导入无父包）在**写入任何文件之前**终止，未创建目录、未创建文件；改用包导入路径重试成功。该失败**不产生**任何工件，也**不构成**本轮的读数。

## 4. 一致性核验（落盘后重新读盘）

| # | 核验 | 结果 |
|---|---|---|
| 1 | 重新读盘 `sha256` 与写入时一致 | 一致（`7cf406fe…`） |
| 2 | 磁盘文本与由代码重建的文本**逐字一致** | 一致 |
| 3 | 清单中的节点集合与 `build_frozen_iqv5_nodes()` 逐项一致 | 一致（`125` 项） |
| 4 | 清单中的种子序与 `MIXED_IQV5_MAIN_SEEDS` 逐项一致 | 一致（`16` 项，顺序按清单序） |
| 5 | 节点标识一律带第六套后缀 `-iqv5` | 一致（`125/125`） |
| 6 | 节点标识唯一 | 一致（`125` 个互异标识） |
| 7 | 类别数 | `16` |
| 8 | 字段集合与第五套清单**完全相同**（未加字段、未删字段） | 一致（`13` 个顶层字段） |
| 9 | `resource_envelope` / `stop_conditions` / `stage_plan` / `seed_derivation` / `schema_version` / `report_format` 文本与第五套**逐字相同** | 一致 |
| 10 | 清单中不含 `manifest_digest` 字段 | 确认不含 |
| 11 | 目标目录下只有 `input/`，`runs/` 不存在 | 确认 |
| 12 | 既有的九套样本集配方、种子与清单文本未被改动 | 一致（本轮零代码改动，工作区 `0` 项变更） |
| 13 | 九套既有证据目录的文件数与工件 sha 未变 | 一致（`5/4/4/4/4/5/7/7/8`；`-v9` 八份 sha 与 `docs/phase6/67` §3 所记逐字一致，含失败阶段 A 两份；v6 失败阶段 A `86e84fed…` 保留） |

与第五套清单的**逐字段差异**（机械代入的结果，**不是**新的裁定）：`strategy_id`、`code_identity`、`config_digest`、`seeds`、`nodes`、`scenario_order`、`output_dir` 七项不同；其余六项**逐字相同**。

## 5. `code_identity` 差异披露（必须逐次如实标注）

- 本次落盘的 `code_identity` = `d46c780a3c962bf17dcbe62443e0bde926caf372`，即**冻结时的仓库 HEAD**；
- 本记录自身的提交会**再移动 HEAD**，因此「本记录提交后的 HEAD」与清单中的 `code_identity` **必然不同**——这是「先冻结、后落回执」的固有结果，**不是**清单缺陷；
- 将来执行运行（`J3`）时的实际代码若与 `code_identity` 不同（既有先例见 `docs/phase6/67` §5：清单记 `22e669a…`，实际执行代码为 `4fbb983…` / `720ee2d…`），**每一次回执都必须如实标注**该差异；
- **不得**把本清单说成由「本记录提交后的代码」产生。

## 6. 包络文本与判定上界的差异披露

- 本清单的 `resource_envelope` 由**共享常量**生成，文本仍写作 `RSS ≤ 1073741824 字节`（`1 GiB`）；该常量**本轮未获授权改动**，也**未改动**；
- 而峰值 RSS 的**判定上界**自 `docs/phase6/58g` 起为 **`1.5 GiB`（`1,610,612,736 B`）**，对本轮（第五次独立验证的 `J3`）生效；
- ⇒ 二者之间**存在已披露的差异**：清单文本 `1 GiB`，`J3` 判定上界 `1.5 GiB`。**不得**为了让二者一致而重新生成或覆盖清单，也**不得**声称清单文本已更新；
- `wall`（阶段 `A` `120 s` / 累计 `1800 s`）与输出（`100 MiB`）**不变**；并发约定（单进程、不并行）**不变**；比对口径**不变**（整个调用的最大常驻集为准，模块自记 `peak_rss_bytes` 只作**下界**并列报告）。

## 7. 冻结后的不可回退边界

- 本清单**不得**按任何读数修改：不得改节点、改种子、改种子序、改场景顺序、改阶段计划文本、改报告版本；
- **不得**重跑或补算 v6 / v7 / v8 / v9 的冻结集合；**不得**在 `-v10` 目录内覆盖或替换已落盘文件；
- `-v10` 目录内的**既有文件只读**；`runs/` 尚未创建，属于 `J3` 的动作；
- 判据与门槛（`docs/phase6/58f`）与判定钉子（`backend/tests/mixed_bot_judgment.py` 的 `11` 条）在读数之后**不得**改动；
- 若将来另开验证：必须**新开身份或新样本集并重新预注册**（全新清单、全新种子、单独运行授权）。

## 8. 未做事项

- **未做**：任何阶段 `A` / `B` 运行；任何确定性补算；创建 `runs/` 或任何输出目录；任何标定；任何质量读数计算；任何代码改动；任何判据 / 门槛 / 分母 / 估值块 / 区间 / 包络常量改动；默认策略切换；推送。
- **仍未证**：第八版在第六套样本集上的任何质量表现；第六套样本集上**没有任何质量读数**（覆盖率、类型熵、金额熵、锚点份额、三对 JS 距离、域一 / 域二差值、跨人数净值**全部未算**）；成本与耗时（`docs/phase6/58` §13 的估时仍是**估算**，不是读数）；峰值 RSS 无可靠外推依据。

## 9. 契约、API 与默认策略

- **未改**任何代码；API 字段、数据库列、内部契约、报告与观测 schema、artifact schema **零改动**；`MixedFixtureManifest` **未加字段**；
- **未新增策略身份**（第八版身份已在 `58d` 落地）；**新增一份冻结清单**（第六套，`mixed-local@8` 配对）；
- 默认策略仍为 **`heuristic@1`**；四份「质量未获支持」签收（`docs/phase5/53c`、`docs/phase6/55g`、`56h`、`57k`）**未被反转**；`docs/phase4/37` §3.1 **未触发**。

## 10. 不得声称

- 不得把本记录说成运行已授权、阶段已开始或结果已知；本文件**不含任何读数**。
- 不得把「清单已冻结」说成「验证通过」「已达生产门槛」或「质量获支持」。
- 不得把第六套样本集与 v6 / v7 / v8 / v9 的**质量**读数并列比较（不同节点集、不同种子集、不同策略身份）；机械量（手数、`wall`、RSS）对照可作成本外推但**须标注**。
- 不得声称清单内写有 `manifest_digest`；不得声称清单文本已与 `docs/phase6/58g` 的判定上界一致。
- 不得把 `docs/phase6/58f` 说成运行授权或清单已冻结；不得把 `docs/phase6/58g` 说成 `wall` / 输出 / 并发上限的变更。
- 不得把 `docs/phase6/58c` §7 的覆盖面性质、§8 的风险评估、`docs/phase6/58d` §4 / §6 的内存核对说成**读数**或新证据。
- 不得把本记录说成对 `docs/53c` / `55g` / `56h` / `57k` 任一签收结论的反转、修正或追认。
- 不得据此修改 `docs/` 下任何已归档文档与任何已落盘工件；不得改写 `@1`–`@8` 任一身份的历史含义。

## 11. 本篇唯一下一步

据 `docs/phase6/58c` §13，冻结顺序的第 5 步是 **`J3`：运行阶段 `A` / `B` 与确定性补算**（各自单独一轮，不得合并）。该动作**尚未授权**，且需要：

1. 单独的运行授权（包络：`wall ≤1800 s` / **峰值 RSS `≤1.5 GiB`（整调用口径）** / 输出 `≤100 MiB`）；
2. 运行前给出**有实测支撑的估时**（`docs/phase6/58` §13 的估算可作起点，须声明其薄弱处）；
3. 在 `-v10` 目录内创建 `runs/` 并落盘阶段产物；每个阶段完成后单独出回执。

**建议**：先单独授权 `J3` 的**阶段 `A`**（成本最低、可最先暴露清单或入口链问题），阶段 `B` 与补算在其回执之后再分别授权。
