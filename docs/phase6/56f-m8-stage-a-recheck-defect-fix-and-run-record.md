# 56f M8：v7 阶段 A 补算的缺陷修复与运行回执

> 日期：2026-09-23。
>
> 本文件接在 `docs/56e` 之后，沿用 `docs/30` §10.1 D6 的编号族约定。`docs/20` 至 `docs/56e` 未被改写。
>
> 本文件记录：阶段 A 确定性补算的**首次失败**（缺登记）、由此定位的缺陷与修复、修复后的补算**实测读数**，以及补算**首次覆盖观测工件**的读数与边界。
>
> **本篇不给出任何质量结论**：补算通过是**可复现性**结论，**不是**质量证据。

## 1. 授权原文与边界

用户答复原文依次为：

> 「请继续下一步」——读作**当轮批准**：运行 v7 阶段 A 的确定性补算，写入工作树外新目录 `<v7>/runs/stage-a-recheck/`（该目录当时**不存在**；只创建、不覆盖）。
>
> 「A.」——授权 **F5 实施**：补登记十六块措辞 + 补一条十六块补算入口链回归 + 实跑三项校验。
>
> 「A.」——授权**完整闭环**：提交 `fix:` → 重跑补算 → 用真实读数写 `docs/56f` → 提交 `docs:` → 推送。

| 项 | 内容 |
|---|---|
| 被授权 | 跑阶段 A 补算；定位并修复其失败原因；补回归并提交；重跑补算；写回执并提交；推送 |
| 明确排除 | 阶段 B；任何质量判定或签收；改判据 / 门槛 / 机会分母 / 估值块 / 区间方法；改冻结清单；改默认策略；给报告加字段 |
| 保留 | 冻结清单（v7）与 v1–v6 全部工件**原样保留**；v6 失败阶段 A 回执 `86e84fed…` **原样保留** |

## 2. 只读基线核验（本轮开始前）

```text
$ git status --short --branch
## master...origin/master

$ git rev-parse HEAD
e494ad52c75ae18f82e91f80087c15aaef061f05
```

七套外部证据的清单与全部既有回执 sha256 逐条实读，与上一轮记录**逐项相符**；v7 当时只有 3 个文件（清单、阶段 A 回执、观测工件），`runs/` 下只有 `stage-a`。

## 3. 首次补算：失败（缺登记，未产出任何报告）

| 项 | 值 |
|---|---|
| 命令 | 提示词原样命令（清单 + 阶段 A 回执 + `<v7>/runs/stage-a-recheck` + `--allow-recheck`） |
| 结果 | **`exit=1`**；进程总耗时 `4.787 s`（含 `uv` 启动，**不是补算读数**） |
| 失败点 | 报告**构造期**：`recheck_limitations(len(receipt.matches.seed_blocks))` → **`MixedRecheckError: 未登记的种子块个数：16`** |
| 产出 | **无报告**：`<v7>/runs/stage-a-recheck/` **仅为空目录**；stdout `0 B` |
| 既往工件 | 全部**逐字未变** |
| 门禁性质 | **fail-closed 生效**：中止发生在落盘之前，**未留下任何半成品工件** |

**失败前已执行、且未报不一致的核对项**（按代码顺序）：行为指标 + 逐节点行；逐手净筹码 `128` 手；**F4 的观测工件核对**。它们**只说明未触发中止**，**不构成**任何验收结论——本轮**没有产出报告**。

**根因**：补算的块数措辞登记表 `RECHECK_BLOCK_COUNT_WORDS` 只登记了 `4` 与 `8`，**缺 `16`**。这与 `docs/56d` §4 的 **F1** 同形（当时修的是**验证报告**侧的登记表，**补算侧一直没补**）。该缺陷自 `docs/56b` 引入 16 个主种子起**潜伏**：此前**从未**有 16 块回执被真正补算过。

**回归缺口（必须记账）**：补算的默认测试只覆盖 `4` 块（端到端）与 `4/8` 块（单点），**没有任何一条真正走补算入口链的 16 块用例**。这是接手提示词已记录的「**默认测试套件不覆盖 opt-in 路径**」这一流程教训的**第四次**出现。

## 4. 修复（`fix:` 提交）

| 项 | 内容 |
|---|---|
| 提交 | `a0ad271fe3bb8f758031e3e3e0bf332fd2ff5644` — `fix: register the sixteen-block wording for the deterministic recheck` |
| 规模 | 2 文件，`+48 / -1` |
| F5-1 | `RECHECK_BLOCK_COUNT_WORDS` 补 `16: "十六"`；`4` / `8` 的措辞**零改动** |
| F5-2 | 单点回归增一条 `16` 断言（既有 `4` / `8` / 未登记即失败三类断言**未放松**） |
| F5-3 | 入口链回归 `test_recheck_entry_path_accepts_the_sixteen_block_receipt`：`validation.main` → `mixed_bot_recheck.main`；断言逐节点行 `1 × 3 × 3 × 16 = 144`、**十六个种子块齐现**、补算报告 `status=completed`、`limitations` 含「十六个固定块」、`net_chips_exact` / `behavior_exact` 为真、`hands_compared` 与回执完成手数一致 |

**反证（回归不是空转）**：临时撤回 `16` 的登记后，两条用例**同时转红**并复现与真实运行**同一条**错误信息；还原后复绿。

修复后校验：`ruff check .` → `All checks passed!`；`pytest -q` → **`1131 passed, 3 skipped`**（原 `1130`，`+1`）；`npm run build` → `✓ built`。

## 5. 重跑补算：实测读数（真实读数）

| 项 | 实测 |
|---|---|
| 命令 | 与 §3 同一条命令；输出目录复用 §3 留下的**空目录** |
| 退出码 / stderr | **`0`** / **空** |
| 报告路径 | `<v7>/runs/stage-a-recheck/mixed-recheck-stage-A.json` |
| 报告 sha256 | `dd89e5fb114b1074178ecdb8d8dd40e38fbe1962baa72d70eff494e055482104` |
| 报告大小 | `77,244 B`；`output_bytes` 与文件大小**自洽**（`77,244`） |
| `schema_version` / `source_stage` / `status` | `mixed-recheck.v1` / `A` / **`completed`** |
| `strategy_id` | `mixed-local@5` |
| `manifest_digest` | `68ab0ca3f73eeb566a08d78542cd9624c644b476ad59cd936033f3fc5afe4547`（与冻结值**逐字一致**） |
| `config_digest` | `8896013bbcd873cd493c6319756c353365b49cc7b4e846e927c4216d6d1f82f2`（与冻结值**逐字一致**） |
| `consistency.source_receipt` | 阶段 A 回执路径 |
| `consistency.source_receipt_sha256` | `980a62f6503c533501c7640b136bd2e71e77819132ecb7d6e89833c3b0a34571`（与阶段 A 回执**逐字一致**） |
| `consistency.hands_compared` | **`128`**（与阶段 A 回执的完成手数一致） |
| `consistency.net_chips_exact` / `behavior_exact` | **`True`** / **`True`** |
| 资源 | wall **`4.231 s`**、CPU `4.179 s`、峰值 RSS `146,898,944 B` = **`140.09 MiB`**、输出 `77,244 B`、单进程不并行 |
| 顶层字段 | **`14` 个**，与既有补算报告**同集合**（无新增字段） |
| `consistency` 字段 | **`6` 个**，与既有补算报告**同集合** |
| 汇总 | `aggregates` **`128`** 行、`worst_opponents` **`2`** 行（阶段 A 只取单一风格，故按 `风格 × arm` 得 2 行） |
| 行为 | `direct_distributions=1125`、`style_js_distances=3`、`categories=48` |
| `limitations` | **`4` 条**，第 3 条为「按主种子块统计只有**十六个**固定块…」 |

**关于 RSS 与 wall**：`140.09 MiB` 是**补算路径**的读数，与阶段 A 的 `72.72 MiB` **不是同一负载**，两者差值**未归因**；**不得**据本行外推阶段 B 的 RSS（阶段 B 的匹配行数是阶段 A 的 `132` 倍）。补算 wall `4.231 s` **不得**当作阶段 B 耗时的外推依据。

## 6. 补算首次覆盖观测工件（读数与边界）

本次补算的执行代码（`a0ad271`）**已包含** F4 的核对路径。只读复核确认：

| 核验项 | 结果 |
|---|---|
| 由回执目录推导的观测路径 | `<v7>/runs/stage-a/mixed-observations-stage-A.json` |
| 该路径是否存在 | **是** |
| 载入手数 | `128` |
| 工件的 `manifest_digest` 与回执一致 | **是** |
| 逐手比对（手数/顺序/分组键/净筹码） | **`128` 手，未报不一致** |

因此：**本次补算包含观测工件核对，且未报不一致**——这是「补算通过」首次**同时**覆盖新增的观测产物。

**边界（必须记账）**：补算报告**不带**「观测已核对」标记（本轮未给报告加字段）；工件**缺失**时该项静默跳过；**未**核对工件的 `output_bytes` 自洽性，**未**比对动作序列 / 各街底池 / 牌力档。上述「已覆盖」只及于手数、顺序、分组键与逐手净筹码。

## 7. 身份差异（必须持续披露）

| 项 | 值 |
|---|---|
| 补算报告中的 `code_identity` | `0ed11d9e50c30855d6296e3e5eab1faada1cd0b8`（**沿用回执字段**，补算不改写身份） |
| 阶段 A 回执的实际执行代码 | `eb01fcfd8936e8b93b0611fa29b6f23321c4e72c` |
| **本次补算实际执行的代码** | `a0ad271fe3bb8f758031e3e3e0bf332fd2ff5644`（`fix:` 提交） |
| 冻结清单是否改动 | **零改动**：sha256 仍为 `a68e9776…ae61`，`manifest_digest` 仍为 `68ab0ca3…4547` |

**不得**把补算报告说成由 `0ed11d9e` 产生，也**不得**把阶段 A 回执说成由 `0ed11d9e` 产生。

## 8. 契约、API 与默认策略

| 对象 | 状态 |
|---|---|
| 公开 API 字段 / 数据库列 / `history_json` | **零改动** |
| `mixed-recheck.v1` / `mixed-validation.v1` / `v2` / `mixed-observations.v1` | **零改动**（无新增字段、无文本改动） |
| 清单模型 / 冻结清单 | **零改动**（摘要逐字未变） |
| 策略身份 | **未新增**；仍为 `mixed-local@5` |
| `docs/37` §3.1 | **不触发** |
| 默认策略 | **仍为 `heuristic@1`** |
| v1–v6 外部证据 | **零触碰**（清单与全部回执 sha256 逐条复核未变） |

## 9. 未做事项与仍未证

- **未做**：阶段 B；任何判据判定（L1–L5）；签收；默认切换。
- **仍未证**：阶段 B 的 wall / 峰值 RSS / 输出体积；抗针对与剥削性检验；`docs/53c` 与 `docs/55g` 的结论**均未被本轮改变**。
- **补算不是新样本、不是新证据**：它只是同一批对局的确定性重放；`128` 手与阶段 A 是**同一批**手，**不增加**任何样本量。
- 本轮的 wall / RSS 只覆盖**阶段 A 补算**，**不构成**阶段 B 的成本验收或预算保证。
- 阶段 B **仍需单独一轮授权**；本轮未消耗任何已登记的 A9 授权。

## 10. 不得声称

- 不得把补算的 `completed` 说成质量证据、验收结果或质量签收。
- 不得把 `1131 passed`、确定性重放或毫秒级延迟读数说成质量证据。
- 不得把「F5 已修复」说成「流程已无缺口」——同类回归是**事后补上**的，且已**第四次**出现。
- 不得把 §5 的 `140.09 MiB` 当作阶段 B 的 RSS 依据，也不得把 `4.231 s` 当作阶段 B 的耗时依据。
- 不得把 §6 的覆盖扩大到动作序列、各街底池或牌力档。
- 不得删除或改写 v6 的失败阶段 A 回执，也不得把它说成「未发生过」。
- 不得据此修改 `docs/20` 至 `docs/56e` 与任何已落盘工件。

## 11. 本篇唯一下一步

**阶段 B 的运行**（`--allow-stage=B --family-set=iqv --emit-observations`，引用阶段 A 的 `completed` 回执），写入 `<v7>/runs/stage-b/`，**须单独一轮授权**。

阶段 B 完成并另跑其确定性补算之后，才谈按 `docs/55` §7 的 L1–L5 判据（**数值照抄、不重标定**）出具结论与签收；X1 / X2 口径已锁定，**产生读数后不得改**。
