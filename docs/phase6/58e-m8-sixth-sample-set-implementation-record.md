# 58e M8：第六套样本集的实施记录（`J1b`）

> 日期：2026-09-23。
>
> 本文件接在 `docs/phase6/58d-m8-eighth-identity-implementation-record.md` 之后，沿用 `docs/30` §10.1 D6 的编号族约定。归档路径按 `docs/README.md` 的维护约定：本文件属第四次验证族，**共用族号 `58`**，故取 **`58e`**。
>
> 本文件记录 `J1b` 的实际结果：按 `docs/phase6/58c` §11 / §13 实施**第六套样本集**（配方、节点、种子盐、主种子、清单装配与回归）。
>
> **本文件不构成冻结或运行授权**：样本集**未冻结**、**未落盘**到任何外部证据目录、**未运行**任何标定或验证。`J2` / `J3` 仍**未授权**。
>
> **本文件不改变任何既有结论**：`-v9` 的「质量未获支持」签收（`docs/phase6/58b` §12）**原样有效**；默认策略**保持 `heuristic@1`**。

## 1. 授权原文与边界

用户裁定原文：

> 「裁定 **A：授权 `J1b`**，实施第六套独立样本集。」

随附声明确认（要点照录）：

> 1. 「`J1b` 仅按 `docs/phase6/58c` §11 实施 `-iqv5` 样本集：使用盐 **`mixed-local-iqv-v5`**、16 个新主种子、清单装配与同构回归。不得参考 `@1`–`@8`、v6–v9 的任何读数来挑选牌面、行动形状、节点或种子。」
> 2. 「必须核验新盐派生结果与既有四套样本集、四个 A9 种子均为零交集，并在回执中如实记录。」
> 3. 「`J1b` 完成后单独形成实施回执，并按 `feat:` 与 `docs:` 两条提交提交本轮内容；**不推送**。」
> 4. 「本次不授权修改判据、门槛、机会分母、包络、策略规则常量或 `X-范围`。」
> 5. 「不授权 `J2` 清单冻结或 `J3` 运行；完成 `J1b` 后必须先汇报实施、回归与零交集核验结果，再单独请求下一项授权。」
> 6. 「`B-4` 保持为并列报告层，不恢复为逐分布硬门槛；`calling` 权重在部分指数下取整为零及已披露的单档化副作用，按既有冻结取值如实保留，不在本轮回调。」

| 项 | 内容 |
|---|---|
| 被授权 | 实施第六套样本集：`16` 条配方与主题牌面、节点构造器、新种子盐与 `16` 个新主种子、场景顺序与阶段计划文本、清单装配函数、深度重建派发、同构回归 |
| 明确排除 | 冻结清单；创建外部证据目录；运行标定 / 阶段 `A` / `B` / 补算；改判据 / 门槛 / 机会分母 / 估值块 / 区间方法 / 包络 / 策略规则常量；默认策略切换；推送 |
| 未授权 | `J2`（清单冻结）、`J3`（运行）；工作树外写入 |
| 未消耗 | **未消耗**任何运行授权；`docs/37` §3.1 **未触发** |

## 2. 只读基线核验（实施前）

```text
$ git status --short --branch
## master...origin/master [ahead 5]

$ git status --porcelain=v1 -uall | wc -l
0

$ git rev-parse HEAD
d9a0b172bc9459968ca0abfbef761f7f8d094426
```

实施前工作区**零改动**；`-v9` 八个工件与 v1–v8 九套证据目录**未被触碰**；前五套配方常量**未被改动**。

## 3. 实施内容

| 文件 | 改动 |
|---|---|
| `backend/tests/mixed_bot_states.py` | 新增 `MIXED_IQV5_NODE_ID_SUFFIX = "-iqv5"`、`MIXED_IQV5_RECIPES`（`16` 条）、配方索引与类别顺序、`build_iqv5_node()`、`build_frozen_iqv5_nodes()` |
| `backend/tests/mixed_bot_validation.py` | 新增 `MIXED_IQV5_SEED_PREFIX`、`MIXED_IQV5_MAIN_SEED_COUNT`、`derive_iqv5_main_seeds()`、`MIXED_IQV5_MAIN_SEEDS`、`MIXED_IQV5_SCENARIO_ORDER`、`MIXED_IQV5_STAGE_PLAN`、`frozen_iqv5_manifest()`；深度重建派发新增第六套分支 |
| `backend/tests/test_mixed_iqv5_sample_set.py` | **新增**：节点正交性、类别语义、深度重建、种子派生与零交集、清单装配、opt-in 入口链 |

**不得改动**：前五套（含首套）的配方常量、节点构造器、种子盐与种子取值；`docs/` 下全部已归档文档；九套外部证据目录；`mixed_bot_judgment.py` 的 `11` 条钉子与 `mixed_bot_behavior_diagnostic.py` 的分桶钉子。

## 4. 节点集

| 项 | 值 |
|---|---|
| 类别框架 | 沿用 `docs/phase6/55` §10.3：`16` 类，`104 + 21 = 125` |
| `node_id` 后缀 | **`-iqv5`**（与 `-iqv1`–`-iqv4` 均不重复） |
| 节点数 | **`125`**（`16` 类别 × 各自适用人数） |
| 配方来源 | 类别、形状、角色与开注设置**沿用既有定义**；主题牌面**全部重写** |

**逐类别主题牌面（新配方）**：

| 类别 | 底牌（行动座位） | 公共牌 |
|---|---|---|
| `hu-blind-position` | `Qc Tc` | — |
| `unopened-open` | `Kh Jh` | — |
| `open-after-limp` | `Kd Jd` | — |
| `facing-first-raise` | `Kc Qh` | — |
| `facing-reraise` | `9h 9s` | — |
| `short-stack-call` | `4h 4c` | — |
| `incomplete-raise` | `Kh Qh` | — |
| `free-check` | `Ac 9c` | `Ts 6d 3h` |
| `flop-draw` | `Kc Qc` | `Jc 9c 4h` |
| `top-pair-weak-kicker` | `Kd 6c` | `Kh 9c 4d 2s` |
| `turn-combo-draw` | `Qh Jh` | `9s 8s 3d 2c` |
| `one-side-all-in` | `Qh Th` | `9c 6d 3s 2h` |
| `overpair-on-high-board` | `8c 8d` | `Ah Ks Qd 6c 3h` |
| `missed-draw-river` | `Jd Td` | `Ac 9c 5h 3s 2d` |
| `shared-board` | `2d 3c` | `Jh Ts 9d 8c 7h` |
| `sizes-merged` | `6d 2c` | `6h Ts 8d 4c 3s` |

**正交性核验（回归实跑通过）**：与**首套及第二至第五套**逐套比对 ——

1. `node_id` 集合两两无交集；
2. 逐（类别 × 人数）的**主题牌面**（公共牌 + 行动座位底牌）均不同；
3. 「类别 + 人数 + 公共牌 + 底牌 + 行动线」的**组合**两两无交集。

（行动线本身由形状决定，跨套相同属正常；判定的是组合不重合。）

**类别语义核验（回归实跑通过）**：`short-stack-call` 确实构成不足额跟注、`incomplete-raise` 无可加注、`one-side-all-in` 恰有一家全下且仍有可行动对手、`free-check` 可过牌且待跟注为 `0`、`shared-board` 焦点手牌与公共牌同值；`flop-draw` / `turn-combo-draw` 的听牌强度 `> 0`；`top-pair-weak-kicker` 与 `sizes-merged` 为**一对**；`overpair-on-high-board` 为口袋对；`missed-draw-river` 为**高牌**。全部**只断言性质，不含任何门槛数值**。

## 5. 主种子

**派生规则（冻结，逐字引用）**：

```text
seed(i) = 大端解释的无符号 32 位整数，取 SHA-256("mixed-local-iqv-v5:seed:" + str(i)) 的前 4 字节
          i = 1..16
```

**取值（已实算，供逐字引用）**：

| i | 取值 | i | 取值 |
|---|---|---|---|
| 1 | `3008262424` | 9 | `1227494703` |
| 2 | `1713517322` | 10 | `1241820418` |
| 3 | `1076959820` | 11 | `2584526233` |
| 4 | `2076521261` | 12 | `623151548` |
| 5 | `990749861` | 13 | `3456817561` |
| 6 | `4010473074` | 14 | `1667776298` |
| 7 | `4073374213` | 15 | `2892543175` |
| 8 | `3841665689` | 16 | `1056690037` |

**零交集核验（回归实跑通过）**：

| 对照对象 | 交集 |
|---|---|
| 首套 `MIXED_MAIN_SEEDS` | **空** |
| 第二套 `MIXED_IQV_MAIN_SEEDS` | **空** |
| 第三套 `MIXED_IQV2_MAIN_SEEDS` | **空** |
| 第四套 `MIXED_IQV3_MAIN_SEEDS` | **空** |
| 第五套 `MIXED_IQV4_MAIN_SEEDS` | **空** |
| 已永久消耗的标定种子 `1215 / 20260918 / 3311 / 7926` | **空** |

`16` 个取值**互不重复**；派生函数 `derive_iqv5_main_seeds()` 可复算并逐字一致。

**必须一并声明**：`16` 块**仍是小固定种子集**，仍**不出具**总体覆盖承诺，`interval_status=not-estimated-small-fixed-seed-set` 的口径**不变**。

## 6. 清单装配

`frozen_iqv5_manifest()` 已实现：**只返回清单对象，不写任何文件、不创建目录**。落盘属于**单独授权**的动作（`J2`）。

| 项 | 处置 |
|---|---|
| 报告版本 | 沿用 `mixed-validation.v2`（字段闭集不变） |
| 机械明细文本 | 场景顺序与阶段计划**按第六套重写**（节点与种子更换）；资源包络与停止条件沿用已登记文本 |
| 机会分母与两个机会域 | **不进入清单**（清单模型不得加字段）→ 落在代码与文档层；口径沿用 `docs/phase6/66` §7 |
| `config_digest` | 由 `strategy_id` 决定（第八版为 `393ca127…`）；装配时机械代入，**不是**新裁定 |

## 7. 实测三项检查（实施后实跑）

```text
$ uv run ruff check .
All checks passed!

$ uv run pytest -q
1271 passed, 3 skipped, 2 warnings in 41.34s

$ npm run build
✓ 24 modules transformed.
✓ built in 544ms
```

用例数由 `1256` 增至 **`1271`**（新增第六套回归 `15` 条）。

## 8. 读数纪律（本轮的要点）

- 本轮**没有**在第六套样本集上计算或记录任何质量读数（不存在三档主动率、熵、JS 距离、跨人数净值之类的数字），以保持它作为**未来独立验证样本**的可用性；
- 唯一触及节点行为的是 opt-in 入口链回归：它在**临时目录**对第六套的**一个**节点跑一次阶段 `A` 与一次补算，只断言**结构与身份一致性**、**不读取也不记录任何质量指标值**（沿用第四套 / 第五套同款回归的既有做法）；
- 本轮**未运行**任何标定、阶段 `A` / `B`（真实样本集口径）或补算；
- §4 / §5 的正交性与零交集核验是**结构与确定性核验**，**不是**读数，也**不是**新证据。

## 9. 契约、API 与默认策略

| 对象 | 状态 |
|---|---|
| 公开 API 字段 / 数据库列 / `history_json` | **零改动** |
| `mixed-validation.v2` / `mixed-observations.v1` / `mixed-recheck.v1` / `mixed-judgment-payload.v1` | **零改动** |
| `MixedFixtureManifest` | **不加字段**（机会分母与机会域仍落在代码与文档层） |
| 策略身份 | **未新增**（仍为 `mixed-local@8`，`J1` 已落地） |
| 产品代码 | **零改动**（本轮只动测试侧） |
| `docs/phase4/37` §3.1 | **未触发** |
| 默认策略 | **仍为 `heuristic@1`** |
| 前五套配方、种子与清单文本 | **逐字未变** |
| 九套外部证据目录 / `-v9` 八份工件 | **零触碰** |
| 包络数值 | **零改动** |

## 10. 未做事项与仍未证

- **未做**：清单冻结（`J2`）；任何外部目录创建；任何标定或验证运行（`J3`）；任何判据 / 门槛 / 分母 / 区间 / 包络改动；任何产品代码改动；按观测回调任何取值；默认策略切换；推送。
- **仍未证**：第八版在第六套样本集上的任何表现；第六套的成本与耗时（估时仍为估算）；判据与口径尚未在读数之前按本轮冻结（按 `docs/phase6/58c` §13，这一步必须早于任何读数）；方向① 对 L3 各项与 L4 的实际影响。
- **未消耗**：任何运行授权。

## 11. 不得声称

- 不得把本记录说成清单已冻结、验证已开始或结果已知；本文件**不含任何读数**。
- 不得把「样本集已实施」说成「验证通过」「已达生产门槛」或「质量获支持」。
- 不得把第六套样本集与 v6 / v7 / v8 / v9 的**质量**读数并列比较（不同节点集、不同种子集、不同身份）；机械量（手数、wall、RSS）对照可作成本外推但须标注。
- 不得把 §4 / §5 的正交性与零交集核验说成读数或新证据。
- 不得把本记录说成对 `docs/53c` / `55g` / `56h` / `57k` 任一签收结论的反转、修正或追认；也不得说成对 `docs/phase6/58b`「质量未获支持」结论的改动。
- 不得据此修改 `docs/` 下任何已归档文档与任何已落盘工件；不得改动前五套配方或种子；不得重跑 `-v9` 的任何阶段或补算。
- 不得据 `docs/phase6/58d` §6 的副作用回调 `_ACTIVE_GATE_WEIGHT` 或任何取值（用户已再次确认）。

## 12. 本篇唯一下一步

`J1b` 已完成：第六套样本集落地、回归通过、三项检查全绿。据 `docs/phase6/58c` §13 的冻结顺序，下一步依次为：

1. **冻结判据与口径**（照抄 `docs/phase6/66` 的判据与全部口径，**须在任何读数之前完成**）；
2. **`J2` 清单冻结**：生成并落盘第六套清单到**新的外部证据目录**（工作树外写入**须当轮批准**）；
3. **`J3` 运行**：阶段 `A` / `B` + 确定性补算，**各自单独一轮**。

**三者均尚未授权**，各自需**单独授权**。**推送**仍**未授权**（本地领先 `7` 个提交）。

**建议**：先单独授权**冻结判据与口径**——它是 `J2` 与任何读数的前置，且**不消耗运行授权、不写工作树外、不改任何取值**。
