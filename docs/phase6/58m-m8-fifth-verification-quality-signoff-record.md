# 58m M8：第五次独立验证的质量签收回执（`-v10`）

> 日期：2026-09-24。
>
> 本文件接在 `docs/phase6/58l-m8-fifth-verification-criteria-judgment-readings-record.md` 之后，沿用 `docs/30` §10.1 D6 的编号族约定。归档路径按 `docs/README.md` 的维护约定：本文件属第四次验证族的**共用族号 `58`**（先例 `58a`–`58l`），故取 **`58m`**。
>
> 本文件记录：按 `docs/phase6/58f` 冻结的 L1–L5 判据与全部判定口径，在**已冻结样本**（`-v10`）上作出的判定读数所**唯一确定**的签收结论，以及**用户签收原文**。
>
> **本轮为签收轮**：判定读数已在 `docs/phase6/58l` 单独落盘（**与第四次验证的单篇合并不同**，本次判定与签收**分两轮**进行）；本文件**不重算**任何读数，也**不新增**任何判据。
>
> **措辞约束**：按 `docs/55` §7.5 第 1 条，判定只输出**解释性状态**，不使用「合格 / 不合格 / 通过 / 达标」。文中「满足 / 未满足」仅作为 D4 与 D7 原文所用的**绑定术语**出现。

## 1. 授权原文与边界

用户答复原文依次为：

> 「授权判据判定。」——授权**判定轮次**（只读运行判定器 `tests.mixed_bot_judgment`，按 `docs/phase6/58f` 冻结判据出读数；结果见 `docs/phase6/58l`）。

> 「授权质量签收。」——授权**签收轮次**（出具本回执并提交）。

> 签收问项的选定原文见 §6。

| 项 | 内容 |
|---|---|
| 被授权 | 出具质量签收回执；**单条 `docs:` 提交**（**不含推送**） |
| 计算口径 | 只**引用**已落盘读数（`docs/phase6/58l`）；**不重算、不新增判据、不改口径**；不写任何工件 |
| 明确排除 | 修改任何判据、门槛、机会分母、估值块或区间方法；改判定器任一钉子；重跑任何阶段或补算；删除任何样本；默认策略的自主切换；任何包络数值的修改；**推送** |
| 保留 | v1–v9 全部工件、`-v10` 全部 `6` 个工件**原样保留**（含 v6 失败阶段 A `86e84fed…`） |
| 未消耗 | 未消耗任何新的运行授权；`docs/phase4/37` §3.1 **未触发** |

## 2. 只读基线核验（签收前）

```text
$ git status --short --branch
## master...origin/master [ahead 4]

$ git status --porcelain=v1 -uall | wc -l
0

$ git rev-parse HEAD
8587fe3a5e2b19c95f84e515efc633dc9dd873bd

$ git rev-parse origin/master
256f0dc62cdb55678b689d783df0bb6fff1bdcdd
```

工作区**零改动**。`-v10` 六份工件签收前 sha 逐字复核（判定前后、签收前后均**未变**）：

| 工件 | `sha256` |
|---|---|
| `input/frozen-fixture-manifest.json` | `7cf406fe59bd64a7f381f52a6d01f489f4374dd0630dd83a5a857c09e49a9190` |
| `runs/stage-a/mixed-validation-stage-A.json` | `f44758ed8ea8679f3fbc40cb652a71a775ca7157d6759c0567662b145df2b8db` |
| `runs/stage-a/mixed-observations-stage-A.json` | `e4d81f29d20d4847fac75b360fce908a7c22a337c53d276a693ed7244f1931d7` |
| `runs/stage-b/mixed-validation-stage-B.json` | `4bc2ea116661e75ddcdf311a5d51b85df8020c930ee90c38e3cd88d190977f1d` |
| `runs/stage-b/mixed-observations-stage-B.json` | `2fa45fa8b2591228b80c9b392abec2b7363fe591555eeb744596b000038ee19f` |
| `runs/stage-b-recheck/mixed-recheck-stage-B.json` | `34ba8899c6063608b4301f16c31b334e82f74853594c6d4ed0fa6175bbf3e27c` |

既有九套证据目录文件数 `5 / 4 / 4 / 4 / 4 / 5 / 7 / 7 / 8` **未变**。

**身份与 `code_identity` 差异（必须与结论并列）**：`strategy_id=mixed-local@8`、`config_digest=393ca127fc16ef47d22cab29d287f43dde5aa94ee4a5cbb34ed15b4a12f627a2`、`manifest_digest=296af53055c8a2e2f8c5310e2d93cf252290fcfc4cfef5511574397d47a5782f`。清单与各报告内的 `code_identity = d46c780a3c962bf17dcbe62443e0bde926caf372`（**清单冻结时的 HEAD**）；判定执行时的实际 HEAD = `f128c8c…`，本回执的提交还将前移 HEAD。**不得**把本轮结论说成由其中任一 HEAD 的代码产生。

## 3. 汇总判定（**引用** `docs/phase6/58l`，不重算）

| 层 | 项数 | 满足 | 未满足 | 不可判定 |
|---|---|---|---|---|
| L1 机制与正确性 | 5 | 5 | 0 | 0 |
| L2 成本 | 6 | 6 | 0 | 0 |
| L3 不可预测性（D7） | 6 | **6**（判定器 `unsatisfied_items = []`） | 0 | 0 |
| L4 不劣化（D4） | 1（逐人数 `8` 项） | 0 | **`8/8` 人数** | 0 |
| L5 抗针对 | 2（报告层） | 第 1 项已报告；第 2 项短/长段与 `12` 桶已报告 | 0（无数值门槛） | 0 |

**存在未满足项** ⇒ 按 `docs/phase6/58f` §11 第 1 条（判据在冻结样本上的判定结果**直接决定**本次质量签收结论）与第 5 条（判据未满足时的**对称处置**）：

> **本次结论：质量未获支持；默认策略保持 `heuristic@1`。**

**与上一轮（第四次验证）的差别（必须并列，不得择一呈现）**：L3 在**本轮六项全部满足**（上一轮为 `4/6`：第 3 项按保守处置记不满足、第 4 项不满足）；本轮**唯一**未满足项是 **L4**。

## 4. 不支持的原因必须分开陈述

| 类别 | 本轮涉及项 | 性质 |
|---|---|---|
| **(a) 已计算且未达** | **L4 不劣化**：逐人数最差族配对收益差 95% 区间下界 `−999.92` 至 `−3365.44` BB/100，`0/8` 人数满足；且 **t 区间与块极值两口径一致**（口径选择不改变结论）；配对 `16,896` 对、异常 `0` | **读数明确低于门槛 `−5 BB/100`** |
| **(b) 在冻结口径下不可判定** | **无** | — |
| **(c) 报告层事项（非判据，如实记账）** | 判定载荷**未单独落盘**（以 stdout 产出并逐字转录于 `58l`，靠确定性复算保真）；L3 第 4 项域二**恰好 `2/3`** 对达标、第 4b 项最薄 JS 距离 `0.2028` 属**邻近达标**；L3 第 5 项在本样本上为**结构性必然**（`blocks_vary = false`） | **报告层**，**不改变**判定 |

**三条必须写明的限定**：

1. **(c) 类必须与 (a) 类分开陈述**：`(c)` 类各项**既不是** L4 未满足的原因，**也不得**被用来稀释其结论；
2. **不得**把 `(a)` 类说成「口径问题」或「工程问题」；**不得**把 L4 的未满足说成「门槛过严」「机会分母不当」或「探针不公平」；
3. 本轮**不存在**「无读数即记未满足」的项。

**两条必须一并声明的纪律后果**（`docs/phase6/58f` §5）：

1. 邻近阈值的达标（域二 `2/3`、最薄 JS 距离 `0.2028`）**不得**表述为稳健达标；
2. 门槛一经批准即锁定：**不得**在读数之后修改、放宽或按结果更换机会分母。

**包络与清单文本的差异（与结论并列）**：三阶段的**整调用**峰值 RSS 分别为 `183.57 MiB`（阶段 `A`）/ `1201.00 MiB`（阶段 `B`）/ `950.94 MiB`（补算），**均在 `1.5 GiB` 判定上界内**；但清单内 `resource_envelope` 文本仍写 `RSS ≤ 1073741824 字节`（`1 GiB`），该常量**未获授权改动**。**不得**声称清单文本已更新，也**不得**把历史轮次（v1–v8 未记录整调用口径）说成「均未超界」。

## 5. 用户签收

**签收原文（逐字记录）**：

> 「A. 签收「质量未获支持」结论」

**原文来源（如实披露）**：该原文取自助手在签收问项中提出的**选项 `A` 的文本**；**参考措辞由助手起草，选择与签收均由用户作出**。

**签收的绑定范围与限定**：

- 绑定范围**仅限本次独立验证**（`-v10` / `mixed-local@8` / 第六套样本集 / 盐 `mixed-local-iqv-v5`）；
- 签收**不新增、不修改任何判据**（`docs/phase6/58f` §11 第 2 条）；本节只记录**用户表态**，不改动 §3 的机械判定结果；
- 签收**不反转、不修正、不追认** `docs/phase5/53c`、`docs/phase6/55g`、`56h`、`57k` 四份既有「质量未获支持」结论（本次是**再次维持**，为第五份）；
- 按 `docs/phase6/58f` §11 第 5 条：结论为「质量未获支持」，默认策略**保持 `heuristic@1`**；**不得**改判据、**不得**重跑同一集合、**不得**把同一集合反复调参后称为独立验证；若需再试，须**新开身份或新样本集并重新预注册**，且运行需**单独授权**。

## 6. 契约、API 与默认策略

| 对象 | 状态 |
|---|---|
| 公开 API 字段 / 数据库列 / 内部契约 | **零改动** |
| `mixed-validation.v2` / `mixed-observations.v1` / `mixed-recheck.v1` / `mixed-judgment-payload.v1` | **零改动**（判定器与 `11` 条钉子逐字未改） |
| `MixedFixtureManifest` / `-v10` 冻结清单 | **零改动**（摘要逐字未变） |
| 新策略身份 / 新清单 | **均未新增**；被测身份仍为 `mixed-local@8` |
| `docs/phase4/37` §3.1 | **不触发** |
| 默认策略 | **仍为 `heuristic@1`**（本次未获质量支持，必须保持） |
| v1–v9 外部证据 | **零触碰**（目录文件数与关键 `sha256` 逐条复核未变） |
| `-v10` 六个工件 | **零改动、零新增**（本篇不写工件） |
| 包络数值 | **零改动**（`≤1800 s` / `≤1.5 GiB` / `≤100 MiB` 维持 `docs/phase6/58g` 的值；清单文本差异见 §4） |

## 7. 未做事项与仍未证

- **未做**：最佳反应（best response）与任何 BR 构造；跨人数归因；用更多 seed 估计判定反转频率；任何参数、门槛或口径改动；任何重跑或补算；默认策略切换；RSS 包络的任何变更；推送。
- **仍未证**：任何强度、可剥削性或不可剥削结论；跨机器与云端成本；总体（非 `16` 固定种子块）覆盖；L5 第 2 项分桶的桶级结论（多数桶的长段样本有限）；`-v10` 之外任何样本集上的推广。
- **未消耗**：本轮**未消耗**任何运行授权；判定轮次仅额外运行了一次定向契约测试（`tests/test_info_boundary.py`）。
- **永久已消耗的 A9 授权**（`a9-calibration-i10` 等）一律**不得重试**，与本轮无关。

## 8. 不得声称

- 不得把本次部分达标读数（覆盖率 `60.71%`、类型熵 `0.6079`/`0.6667`/`0.4454`、锚点 `382/382`、金额熵 `1.4005`、JS `0.2593`/`0.3551`/`0.2028`、稳定性 `16/16`）表述为质量已证明、稳健达标或「基本通过」。
- 不得把 L4 的「已计算未达」说成「口径问题」；不得把 L4 的未满足归因于门槛、分母或探针设计。
- 不得把 `-v10` 与 `-v9` 或 v6 / v7 / v8 的**质量读数**直接比较，或说成「更好 / 更差」（**不同节点集、不同种子集、不同策略身份**）；机械量（手数、`wall`、RSS）对照可作成本外推，但须标注且不得当作质量。
- 不得把 L5 的探针读数表述为不可剥削、exploitability / NashConv 上界或 best response 结果；不得把 L5 第 2 项的有限样本桶当作发现。
- 不得把「方向稳定」说成独立证据。不得把邻近阈值的达标说成稳健达标。
- 不得把本次判定表述为「验证通过」「验收通过」或「已达生产门槛」；不得把 L1 / L2 达标、`completed`、`violations=[]`、确定性重放通过说成质量证据。
- 不得只报一种 RSS 口径；不得声称 `-v10` 清单文本已与判定上界一致。
- 不得据此修改 `docs/` 下任何已归档文档与任何已落盘工件；不得重跑 `-v10` 已完成的阶段 `A` / `B` / 补算 / 判定；不得改写 `@1`–`@8` 任一身份的历史含义。
- 不得把本次结论表述为对 `docs/phase5/53c`、`docs/phase6/55g`、`56h`、`57k` 的**反转**（本次是**再次维持**）。

## 9. 本篇唯一下一步

第五次独立验证**已闭环**：`J1`（第八版身份）→ `J1b`（第六套样本集）→ 判据与口径冻结（`58f`）→ `J2`（清单冻结，`58h`）→ `J3` 三轮（阶段 `A` `58i` / 阶段 `B` `58j` / 补算 `58k`）→ 判定读数（`58l`）→ 签收（本篇）。结论为**质量未获支持**，默认保持 `heuristic@1`。

按 `docs/phase6/58f` §11 第 5 条，若需再试，必须**新开策略身份或新样本集并重新预注册**（全新清单、全新种子与**单独运行授权**）；**不得**改判据、**不得**重跑 `-v10` 冻结集合、**不得**把同一集合反复调参后称为独立验证。

**本篇的唯一下一步建议（需单独授权，本篇不自行执行）**：就 L4 的负差在 `-v10` 已落盘工件上做一轮**只读定向诊断**——把负配对差定位到人数 / 风格 / 对手族与尺度 / 牌力 / 街道分桶（**不新增样本、不重跑、不改代码**），为「下一轮是否新开身份」提供可复核的依据。该诊断结论**不得**用于修改任何判据或口径。

本地提交（`c518fee` / `ee97bd8` / `f128c8c` / `8587fe3` 及本篇）**均未推送**，推送须单独授权。
