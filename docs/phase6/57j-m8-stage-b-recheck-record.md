# 57j M8：第四次验证阶段 B 的确定性补算回执

> 日期：2026-09-23。
>
> 本文件接在 `docs/57i` 之后，沿用 `docs/30` §10.1 D6 的编号族约定。`docs/20` 至 `docs/57i` 未被改写。
>
> 本文件记录：`J3` 第 4 步（阶段 `B` 的确定性补算）的运行命令、实测读数、估时对照与**包络余量**。
>
> **本篇不给出任何质量结论，也不做任何判据计算**：补算**不产生新对局、不产生新样本**；行为分区只做**结构与自洽核对**，L1–L5 的判定**留待判定轮次**。

## 1. 授权原文与边界

用户答复原文：

> 「授权确定性补算。」

其唯一指向即本篇对象：按 `docs/57f` §10 / `docs/57b` §5.6 的顺序，阶段 `B` 运行之后的下一步**只能是阶段 `B` 的确定性补算**。

**运行前已先给出实测支撑的估时（`R3`）**：wall `≈550–700 s`（点估 `590 s`，依据 v7 阶段 `B` 补算 `602.846 s` × 阶段 `B` 运行比 `0.978`）、峰值 RSS `≈870 MiB 量级`（**无可靠外推依据**，并明示余量或仅约 `15%`）、输出 `≈326 KB`。

| 项 | 内容 |
|---|---|
| 被授权 | 单独一轮运行阶段 `B` 的确定性补算（清单、阶段 `B` 回执均已冻结） |
| 明确排除 | 任何判据计算；任何质量结论；改判据 / 门槛 / 机会分母 / 估值块 / 区间方法；改清单或种子；重跑阶段 `A` / 阶段 `B` 及其补算；重跑 v6 / v7 冻结集合；默认切换 |
| 包络 | 沿用既有 `≤1 GiB`（`R4(a)`：**补声明、不改数值**）；本路径**仍无自己的声明包络** |
| 未消耗 | 未消耗判定轮次的授权；`docs/37` §3.1 **未触发** |

## 2. 只读基线核验（补算前）

```text
$ git status --short --branch
## master...origin/master

$ git status --porcelain=v1 -uall | wc -l
0

$ git rev-parse HEAD
d347333afc1d32303b6402e0feb67bfc34018555
（与 origin/master 逐字一致）

$ find /Users/bryanylliu/holdem-mixed-bot-validation-v8 -type f | wc -l
6（清单 + 阶段 A 报告/观测/补算 + 阶段 B 报告/观测）
```

工作区**零改动**。补算前逐字复核：v8 六份既有工件 sha256 = `484a6ce8…a225` / `b9642098…3319` / `f776ab7c…8c0a` / `974a019c…11b5` / `6d3838fb…f27b` / `087e0d02…5132`；v1–v7 七套目录文件数 `5 / 4 / 4 / 4 / 4 / 5 / 7`（含 v6 失败阶段 A `86e84fed`，**原样保留**）——**均与 `docs/57i` §3 一致**。

## 3. 运行命令与工件

```text
$ uv run python -m tests.mixed_bot_recheck \
    /Users/bryanylliu/holdem-mixed-bot-validation-v8/input/frozen-fixture-manifest.json \
    /Users/bryanylliu/holdem-mixed-bot-validation-v8/runs/stage-b/mixed-validation-stage-B.json \
    /Users/bryanylliu/holdem-mixed-bot-validation-v8/runs/stage-b-recheck \
    --allow-recheck
（退出码 0；单进程不并行）
```

| 工件 | sha256 | 大小 |
|---|---|---|
| `<v8>/input/frozen-fixture-manifest.json`（**未改动**） | `484a6ce83687be436a2676081c6f9c35a7deed5c4e867b9bb95eec868141a225` | `215,371 B` |
| `<v8>/runs/stage-a/*`（**三份均未改动**） | `b9642098…3319` / `f776ab7c…8c0a` / `974a019c…11b5` | — |
| `<v8>/runs/stage-b/mixed-validation-stage-B.json`（**未改动**） | `6d3838fb09d9b419bb4d9f16b510a692989428c2064c7ef4716fa8b682fff27b` | `20,371,657 B` |
| `<v8>/runs/stage-b/mixed-observations-stage-B.json`（**未改动**） | `087e0d024658c7e75038977f82c5ab392754c3688fe5f56cce3990fc09425132` | `46,037,249 B` |
| `<v8>/runs/stage-b-recheck/mixed-recheck-stage-B.json`（**本轮新增**） | `a3c945906ba87de675b1c463540aac929ba559d05d397160422582f8088dda54` | `324,904 B` |

- **首跑成功**：补算**一次通过**，未发生失败重跑；`<v8>` 文件数由 `6` 变为 `7`，既有工件**零改写**。

## 4. 实测读数

### 4.1 身份与结论字段

| 项 | 读数 |
|---|---|
| `schema_version` / `source_stage` / `status` | `mixed-recheck.v1` / `B` / `completed` |
| `strategy_id` | `mixed-local@6` |
| `config_digest` | `b6678f65069c19779f683bd6a8df31f7bc3567cf813654d2758278c13151b4a1` |
| `manifest_digest` | `0a1b1ea8985dfbd0f9d5ddeeedbca53e348c97ca0f5c31d9f2273e87f912e8f6` |
| `code_identity` | `22e669a879acbf862aa8eb8953ca6fa90c29bf2c`（**冻结时的预承诺值**，见 §7） |
| `environment` | `macOS-26.6.2-arm64-arm-64bit-Mach-O / python 3.13.8` |

### 4.2 一致性核对（回执自带字段）

| 项 | 读数 |
|---|---|
| `source_receipt` | `<v8>/runs/stage-b/mixed-validation-stage-B.json` |
| `source_receipt_sha256` | `6d3838fb09d9b419bb4d9f16b510a692989428c2064c7ef4716fa8b682fff27b`（**与阶段 B 报告逐字一致**） |
| `hands_compared` | `33,792`（与阶段 B 的计划/完成对抗手数一致） |
| `net_chips_exact` | `true`（逐手净筹码按排期顺序**完全一致**） |
| `behavior_exact` | `true`（行为指标**完全一致**） |

- **任何差异都会中止且不写报告**：两处 `true` 目前即为「核对通过」的唯一证据形式；本轮回执文件的产出本身也意味着未触发中止路径。
- **观测工件一致性核对**：本轮回执旁**存在**观测工件（`087e0d02…5132`，`46,037,249 B`），因此该项核对**参与并已通过**。该结论**不带报告字段标记**；**只有工件存在时该结论才成立**，后续引用必须连同「工件存在」一并陈述。

### 4.3 结构自洽（**不做判据计算**）

| 项 | 读数 |
|---|---|
| `aggregates` 行数 | `384` |
| `worst_opponents` 条目数 | `6` |
| `behavior.direct_distributions` / `style_js_distances` / `categories` | `1,125` / `3` / `48` |
| `limitations` 条目数 | `4`（原样落盘，未改写） |

- 上表只证明**分区齐全、条目数自洽**；三分域与机会分母**均未在本轮触碰**，取值**未解读**。

### 4.4 资源与包络（包络为**人工比对**，代码不据此改 `status`）

| 项 | 读数 | 包络 | 余量 |
|---|---|---|---|
| wall | **`456.751 s`**（≈`7.61 min`） | ≤1800 s | 约 `75%` 余量 |
| CPU | `451.593 s` | — | — |
| 峰值 RSS | **`958.73 MiB`**（`1,005,305,856 B`） | `≤1 GiB`（`R4(a)` **补声明**） | **仅约 `6.37%`（`65.26 MiB`）** |
| 输出 | `324,904 B`（≈`317.3 KiB`） | ≤100 MiB | 远未触及 |
| 并发 | `single_process=true`、`parallel=false` | 单进程不并行 | 满足 |

- 外部 `/usr/bin/time -l` 交叉核对：`457.64 real`、`maximum resident set size = 1,028,489,216 B`（`980.84 MiB`，含解释器与运行时；相对 `1 GiB` 余量 `4.21%`）——**两项计量都低于包络，但余量都很薄**。
- **包络未触发，未中止运行**：本轮全程在 `≤1 GiB` 之内，无需触发「触及即停」路径。

### 4.5 包络余量与未归因（本篇重点，如实记账）

| 对照项 | v7 阶段 `B` 补算 | 本轮（v8 阶段 `B` 补算） |
|---|---|---|
| wall | `602.846 s` | `456.751 s`（**`-24.2%`**） |
| 峰值 RSS | `917.38 MiB`（余量 `10.41%`） | `958.73 MiB`（余量 **`6.37%`**） |
| 输出 | `324,853 B` | `324,904 B`（`+0.02%`） |
| 补算 / 运行 的 RSS 比 | `1.860×` | **`2.059×`** |

**未归因（明确不声称已解释）**：

- 补算峰值 RSS 相对同轮运行的比值由 `1.860×` 升到 `2.059×`，**差值未归因**；
- v8 阶段 `A` 补算与阶段 `B` 补算都出现「补算 RSS 明显高于运行 RSS」的现象，但**两个规模不是同一负载**，本文件**不据此推断趋势**；
- **补算路径仍无自己的声明包络**：`R4(a)` 只是**补声明**，本轮余量收窄**不构成**任何包络调整、容量承诺或对其他规模的授权依据。

**本篇补充的累计观察（非判据、非门槛，仅如实记账）**：`<v8>` 全部保留工件合计 `77,326,487 B`（≈`73.74 MiB`），仍低于 `≤100 MiB`；此为算术合计，**不新增任何验收口径**。

## 5. 估时对照（估算 vs 实测）

| 项 | 运行前估算（`R3`） | 实测 | 偏差 |
|---|---|---|---|
| 补算 wall | `550–700 s`（点估 `590 s`） | `456.751 s` | **低于区间**；对点估 **`-22.6%`** |
| 补算峰值 RSS | `≈870 MiB` 量级（已声明余量或仅约 `15%`） | `958.73 MiB` | **`+10.2%`，高于估算**；余量实为 `6.37%` |
| 补算输出 | `≈326 KB` | `324,904 B` | **`-0.4%`** |

**本轮结论（含估算偏差，如实记账）**：

- wall 估算**偏高 `22.6%`**，RSS 估算**偏低 `10.2%`**——两项偏差**方向相反**，**不得**据此把估算表述为读数，也**不得**据此为任何后续规模承诺耗时或内存。
- 本轮再次出现「估算与实测存在明显偏差」的现象；`docs/54h` 的「估算偏差 `41%`」先例与「峰值 RSS 无可靠外推依据」的现状**均未改变**，本轮偏差**不构成**估算方法已被校准的结论。

## 6. 抗针对与剥削性检验（仍未做）

- 补算只**重放**既有对抗对照与行为分区，**不新增对手、不新增样本**；探针族只是机械对照，**不是** best response。
- `shared-board` 等探针的读数**只能是**「读数存在、可利用性未检验」；**不得**把本轮补算当作抗针对或剥削性检验。

## 7. 身份差异（必须持续披露）

| 项 | 值 |
|---|---|
| 回执中的 `code_identity`（**冻结时的 HEAD**） | `22e669a879acbf862aa8eb8953ca6fa90c29bf2c`（`docs:` 提交） |
| `@6` 与 `-iqv3` 的**实际实现提交** | `4fbb9833425475c593916969ba0605fc3525a2c9`（`feat:`）、`720ee2d57d69e5e1af75013456331e6413484ac6`（`feat:`） |
| 差异内容 | 冻结时的 HEAD 与实际执行代码之间**只有文档差异**，代码内容与 `720ee2d…` 相同 |

**不得**把本次补算回执表述为「由 `22e669a` 代码产生」。

## 8. 契约、API 与默认策略

| 对象 | 状态 |
|---|---|
| 公开 API 字段 / 数据库列 / `history_json` | **零改动** |
| `mixed-validation.v2` / `mixed-observations.v1` / `mixed-recheck.v1` | **零改动**（本次无代码改动，无新增字段） |
| `MixedFixtureManifest` / 冻结清单 | **零改动**（摘要逐字未变） |
| 策略身份 | **未新增**（仍为 `mixed-local@6`） |
| 新增清单 | **否** |
| `experiment` / `measurement` / 候选 A artifact schema | **零改动** |
| `docs/37` §3.1 | **不触发** |
| 默认策略 | **仍为 `heuristic@1`**（未获质量签收，必须保持） |
| v1–v7 外部证据 / 封存 campaign 工件 | **零触碰** |

## 9. 未做事项与仍未证

- **未做**：`J3` 范围内的机械步骤已全部完成；**判据判定与用户签收尚未开始**；任何质量结论；完整标定；默认切换。
- **仍未证**：L1–L5 的**任何判据读数**与是否达标；`@6` 的强度、可剥削性、抗针对性；域二与 L4 是否改善；根因 C 是否成立。
- **补算**：**不产生新对局、不产生新样本、不追加强制种子**；`net_chips_exact` / `behavior_exact` 是**确定性重放**结果，**不得**当作新证据、新样本或质量证据。
- **仍未证（成本）**：补算路径的**声明包络**仍不存在；本轮余量 `6.37%` **未归因**，**不得**外推。
- **未消耗**：本轮未消耗判定轮次的授权；全程只获**包络级**预算批准。

## 10. 不得声称

- 不得把本次补算的 `completed`、`net_chips_exact=true`、`behavior_exact=true` 说成**质量证据**或判据达标。
- 不得把确定性补算当作**新样本**或**新证据**；不得把「补算通过」说成阶段 `B` 已被独立复核、或验证已通过。
- 不得在未同时声明「观测工件存在」的前提下声称观测工件已被确定性核对（§4.2）。
- 不得把 `2.059×` / `1.860×` 的比值说成已归因，也不得据此外推任何规模。
- 不得把本轮读数解释为「包络已被验证充足」：余量 `6.37%` 是**本轮读数的算术后果**，**不构成**容量承诺。
- 不得把本次估时对照说成「估算已被校准」；不得为判定轮次或任何后续运行承诺耗时或内存。
- 不得把本次回执说成由 `22e669a` 代码产生（§7）。
- 不得把 `docs/57e` 的锁定说成 v7 的预注册口径或质量改善。
- 不得据此修改 `docs/20` 至 `docs/57i` 与任何已落盘工件；不得重跑本次补算；不得改写 v6 的失败阶段 A 回执。

## 11. 本篇唯一下一步

`J3` 的第 4 步（阶段 `B` 的确定性补算）已完成并如实记账。`J3` 的机械步骤至此**全部结束**；按 `docs/57f` §10 与 `docs/57b` §5.6 的顺序，**下一步是判据判定与用户签收**：

- **必须按 `docs/57e` 锁定的判据与口径**（L1–L5、六项门槛、机会分母、两个机会域、区间方法 `X1`、估值种子块 `X2`，以及本轮读数之前即已锁定的 `X3`–`X9`）执行，**取值一字不改**；
- 判定所需的全部读数来源（阶段 `B` 报告与观测、阶段 `B` 补算、阶段 `A` 与其补算）**均已落盘**；
- 若判据不满足，则按既定顺序进入**只读定向诊断**，**不得**据此修改 `@6` 的任何常量、判据或口径。
