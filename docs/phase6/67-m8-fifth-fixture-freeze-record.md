# 67 M8：第五套冻结清单的落盘回执（`J2`）

> 日期：2026-09-23。
>
> 本文件接在 `docs/phase6/66-m8-fourth-verification-criteria-and-caliber-freeze.md` 之后，沿用 `docs/30` §10.1 D6 的编号族约定。归档路径按 `docs/README.md` 的维护约定：本文件属第四次验证族，归档于 `phase6/`。
>
> 本文件是 `docs/phase6/62` §12 冻结顺序的**第 4 步**（`J2`）的回执：按已冻结的判据与口径（`docs/phase6/66`）、已实施的身份（`docs/phase6/64`）与样本集（`docs/phase6/65`），在**新的外部证据目录**落盘第五套冻结清单。
>
> **本文件不授权任何运行**：`J3`（阶段 `A` / `B` + 确定性补算）仍需单独授权；本文件也**不产生**任何读数。

## 1. 授权原文与边界

用户裁定（选项照录）：

> 「A. 授权 `J2`，目录用 `-v9`」

该选项随附理由由助手在选项中提出、随选项被选定，此处照录以免与用户原话混淆：

> 「与既有命名链一致（`-v6`/`-v7`/`-v8` 分别对应三次验证），第四次验证自然为 `-v9`；只创建 `input/` 并落盘清单（`runs/` 留给 `J3`），写后给出冻结回执（sha256 / `manifest_digest` / 节点与种子一致性核验 / `code_identity` 差异披露）。」

| 项 | 内容 |
|---|---|
| 被授权 | **工作树外写入**：新建外部证据目录并用 `-v9` 命名；只创建 `input/` 并落盘第五套冻结清单；随后出冻结回执 |
| 明确排除 | 任何运行或补算（`J3`）；创建 `runs/`；改任何代码；改判据 / 门槛 / 机会分母 / 估值块 / 区间方法；改任一判定钉子；默认策略切换；推送 |
| 转写纪律 | 清单由代码生成，**不手写、不修剪、不排序**；目标文件已存在则**拒绝覆盖** |
| 未消耗 | **未消耗**任何运行授权；`docs/37` §3.1 **未触发** |

## 2. 只读基线核验（冻结前）

```text
$ git status --short --branch
## master...origin/master [ahead 6]

$ git status --porcelain=v1 -uall | wc -l
0

$ git rev-parse HEAD
ae3f74930f022a69a4b64a7fcb85188c47384a74

$ git rev-parse origin/master
06e9ecb2e50781a8c71d02bb6195f5e2453139fb
```

目标目录 `/Users/bryanylliu/holdem-mixed-bot-validation-v9` 在落盘前**不存在**（`ls -d` 返回 `No such file or directory`）；既有八套证据目录的文件数在落盘前后**未变**（`5 / 4 / 4 / 4 / 4 / 5 / 7 / 7`）。

## 3. 落盘内容

| 项 | 值 |
|---|---|
| 路径 | `/Users/bryanylliu/holdem-mixed-bot-validation-v9/input/frozen-fixture-manifest.json` |
| 字节数 | `215371` |
| `sha256` | `499258c993d1e59d5c2f2c218b79f2944059d216e05e3c20998675c20704a7c6` |
| `manifest_digest` | `319cbb967d66dabdd795c19d60ab91962cd9397926ea71349583d855edc6a918` |
| `config_digest` | `fd7433e35fc8260d03635774b34fbdc419ad5a04ba0448916b036e385a546665` |
| `strategy_id` | `mixed-local@7` |
| `code_identity` | `ae3f74930f022a69a4b64a7fcb85188c47384a74` |
| 节点 | `125` 个，`16` 个类别，标识一律带 `-iqv4` 后缀 |
| 种子 | `16` 个主种子，清单序第 `1` 块 = `1780719137` |
| 落盘方式 | `manifest_json(frozen_iqv4_manifest(...))`；目标文件不存在则创建，存在则**拒绝覆盖** |

**清单文件本身不含 `manifest_digest` 字段**：该值是 `MixedFixtureManifest.digest()` 的产物，由回执与阶段报告引用。这一点在引用该值时必须注意，**不得**声称清单里写有它。

所生成的文本文本与 `docs/phase6/66` §8.2 冻结的估值块代入**一致**：第 `1 / 2 / 3 / 4` 项的估值块即本清单种子序第 `1` 块 `1780719137`（`VALUATION_BLOCK_INDEX = 0`）。这是位置口径的机械结果，**不是**新的裁定。

## 4. 一致性核验（落盘后重新读盘）

| # | 核验 | 结果 |
|---|---|---|
| 1 | 重新读盘 `sha256` 与写入时一致 | 一致（`499258c9…`） |
| 2 | 磁盘文本与由代码重建的文本**逐字一致** | 一致 |
| 3 | 清单中的节点集合与 `build_frozen_iqv4_nodes()` 逐项一致 | 一致（`125` 项） |
| 4 | 清单中的种子序与 `MIXED_IQV4_MAIN_SEEDS` 逐项一致 | 一致（`16` 项，顺序按清单序） |
| 5 | 节点标识一律带第五套后缀 | 一致 |
| 6 | 缺失的包络机械明细为空 | `()` |
| 7 | 局限说明按块数 `16` 沿用已登记文本 | 沿用（`5` 条） |
| 8 | 既有的四套样本集配方、种子与清单文本未被改动 | 一致（本轮零代码改动） |
| 9 | 八套既有证据目录的文件数与工件 sha 未变 | 一致（含 v8 七份、v7 七份、v6 五份含失败阶段 A `86e84fed`） |

## 5. `code_identity` 差异披露（必须逐次如实标注）

- 本次落盘的 `code_identity` = `ae3f74930f022a69a4b64a7fcb85188c47384a74`，即**冻结时的仓库 HEAD**；
- 本记录自身的提交会**再移动 HEAD**，因此「本记录提交后的 HEAD」与清单中的 `code_identity` **必然不同**——这是「先冻结、后落回执」的固有结果，**不是**清单缺陷；
- 将来执行运行（`J3`）时的实际代码若与 `code_identity` 不同（v8 的先例：清单记 `22e669a…`，实际执行代码为 `4fbb983…` / `720ee2d…`），**每一次回执都必须如实标注**该差异；
- **不得**把本清单说成由「本记录提交后的代码」产生。

## 6. 冻结后的不可回退边界

- 本清单**不得**按任何读数修改：不得改节点、改种子、改种子序、改场景顺序、改阶段计划文本、改报告版本；
- **不得**重跑或补算 v6 / v7 / v8 的冻结集合；**不得**在 `-v9` 目录内覆盖或替换已落盘文件；
- `-v9` 目录内的**既有文件只读**；`runs/` 尚未创建，属于 `J3` 的动作；
- 判据与门槛（`docs/phase6/66`）与判定钉子（`backend/tests/mixed_bot_judgment.py` 的 `11` 条）在读数之后**不得**改动；
- 若将来另开验证：必须**新开身份或新样本集并重新预注册**（全新清单、全新种子、单独运行授权）。

## 7. 未做事项

- **未做**：任何阶段 `A` / `B` 运行；任何确定性补算；创建 `runs/` 或任何输出目录；任何标定；任何读数计算；任何代码改动；任何判据 / 门槛 / 分母 / 估值块 / 区间改动；默认策略切换；推送。
- **仍未证**：第七版在第五套样本集上的任何表现；成本与耗时（`docs/phase6/58` §13 的估时仍是**估算**，不是读数）；峰值 RSS 无可靠外推依据。

## 8. 契约、API 与默认策略

- **未改**任何代码；API 字段、数据库列、内部契约、报告与观测 schema、artifact schema **零改动**；`MixedFixtureManifest` **未加字段**；
- **未新增策略身份**（第七版身份已在 `J1` 落地）；**新增一份冻结清单**（第五套，`mixed-local@7` 配对）；
- 默认策略仍为 **`heuristic@1`**；`docs/37` §3.1 **未触发**。

## 9. 不得声称

- 不得把本记录说成运行已授权、阶段已开始或结果已知；本文件**不含任何读数**。
- 不得把「清单已冻结」说成「验证通过」「已达生产门槛」或「质量获支持」。
- 不得把第五套样本集与 v6 / v7 / v8 的读数并列比较（不同节点集、不同种子集、不同策略身份）。
- 不得声称清单内写有 `manifest_digest`。
- 不得把 `docs/phase6/66` 的判据与口径冻结说成对任何门槛的重新批准；也不得把本记录说成对 `docs/53c` / `55g` / `56h` / `57k` 任一签收结论的反转、修正或追认。
- 不得据此修改 `docs/` 下任何已归档文档与任何已落盘工件；不得改写 `@1`–`@7` 任一身份的历史含义。

## 10. 本篇唯一下一步

据 `docs/phase6/62` §12，冻结顺序的第 5 步是 **`J3`：运行阶段 `A` / `B` 与确定性补算**（各自单独一轮，不得合并）。该动作**尚未授权**，且需要：

1. 单独的运行授权（含包络：`wall ≤1800 s` / `RSS ≤1 GiB` / 输出 `≤100 MiB`）；
2. 运行前给出**有实测支撑的估时**（`docs/phase6/58` §13 的估算可作起点，须声明其薄弱处）；
3. 在 `-v9` 目录内创建 `runs/` 并落盘阶段产物；每个阶段完成后单独出回执。

**建议**：先单独授权 `J3` 的**阶段 `A`**（成本最低、可最先暴露清单或入口链问题），阶段 `B` 与补算在其回执之后再分别授权。
