# 58l M8：第五次独立验证的判据判定读数（`J4`，**不含签收**）

> 日期：2026-09-24。
>
> 本文件接在 `docs/phase6/58k-m8-fifth-verification-stage-b-recheck-record.md` 之后，沿用 `docs/30` §10.1 D6 的编号族约定。归档路径按 `docs/README.md` 的维护约定：本文件属第四次验证族的**共用族号 `58`**（先例 `58a`–`58k`），故取 **`58l`**。
>
> 本文件记录：按 `docs/phase6/58f` 冻结的 L1–L5 判据与全部判定口径，在**已冻结样本**（`-v10`）上作出的**判定读数**。
>
> **本篇只做读数，不做签收**：按本轮授权范围，质量签收结论**不在本篇**，须**另起一轮**单独出具。本篇**不得**被引用为签收文件。
>
> **措辞约束**：按 `docs/55` §7.5 第 1 条，判定只输出**解释性状态**，不使用「合格 / 不合格 / 通过 / 达标」。文中「满足 / 不满足」仅作为 D4 与 D7 原文所用的**绑定术语**出现。

## 1. 授权原文与边界

用户裁定（原话照录）：

> 「授权判据判定。」

助手在授权前给出的边界陈述（此处照录以免与用户原话混淆）：

> 「在已冻结样本上、按 `docs/phase6/58f` §4–§9 计算 L1–L5 读数并如实并列四条既有纪律……该轮**只做判定、不做签收**——判定结论将直接决定质量签收，签收另起一轮。」

| 项 | 内容 |
|---|---|
| 被授权 | 按已冻结口径做判定计算；出具**判定读数回执**；单条 `docs:` 提交 |
| 计算口径 | 只读**已落盘工件的现有字段**；判定器**不写任何文件、不产生对局、不使用随机数**；不改动代码、不重跑任何阶段或补算 |
| 明确排除 | 出具质量签收结论；修改任何判据、门槛、机会分母、估值块或区间方法；改判定器任一钉子；重跑任何阶段或补算；删除任何样本；默认策略的自主切换；任何包络数值的修改；**推送** |
| 保留 | v1–v9 全部工件、`-v10` 全部 `6` 个工件**原样保留**（含 v6 失败阶段 A `86e84fed…`） |
| 未消耗 | 未消耗任何新的运行授权；`docs/phase4/37` §3.1 **未触发** |

## 2. 只读基线核验（判定前）

```text
$ git status --short --branch
## master...origin/master [ahead 3]

$ git status --porcelain=v1 -uall | wc -l
0

$ git rev-parse HEAD
f128c8cabe359f9e3b8aa1eca29c84f10e6be357

$ git rev-parse origin/master
256f0dc62cdb55678b689d783df0bb6fff1bdcdd
```

判定计算**开始前**工作区**零改动**；判定器**未写出任何文件**。**判定载荷未单独落盘**（沿用既有先例）：它以 stdout 形式产出并**逐字转录于本文件**，可由同一命令在**同一批已冻结工件**上**确定性复算**（判定器不使用随机数）。因此本回执是判定**之后**的记录文件，其中不含计算代码。

**判定期间额外运行的只读命令（如实记账）**：为取得 L1「信息边界」项的**直接读数**，运行了一次定向契约测试 `uv run pytest tests/test_info_boundary.py -q` → **`7 passed`**。这是本轮唯一的额外命令；它不产生对局、不写工件、不改代码。

**判定输入工件（`-v10`，共 `6` 个，判定前后 sha 逐字未变）**：

| 工件 | `sha256` | 角色 |
|---|---|---|
| `input/frozen-fixture-manifest.json` | `7cf406fe59bd64a7f381f52a6d01f489f4374dd0630dd83a5a857c09e49a9190` | 身份与摘要基准、种子清单序 |
| `runs/stage-a/mixed-validation-stage-A.json` | `f44758ed8ea8679f3fbc40cb652a71a775ca7157d6759c0567662b145df2b8db` | 阶段 `A` 有效回执 |
| `runs/stage-a/mixed-observations-stage-A.json` | `e4d81f29d20d4847fac75b360fce908a7c22a337c53d276a693ed7244f1931d7` | 备用 |
| `runs/stage-b/mixed-validation-stage-B.json` | `4bc2ea116661e75ddcdf311a5d51b85df8020c930ee90c38e3cd88d190977f1d` | **主来源** |
| `runs/stage-b/mixed-observations-stage-B.json` | `2fa45fa8b2591228b80c9b392abec2b7363fe591555eeb744596b000038ee19f` | L5 第 2 项 |
| `runs/stage-b-recheck/mixed-recheck-stage-B.json` | `34ba8899c6063608b4301f16c31b334e82f74853594c6d4ed0fa6175bbf3e27c` | L1（`33,792` 手确定性重放） |

身份：`strategy_id=mixed-local@8`、`config_digest=393ca127fc16ef47d22cab29d287f43dde5aa94ee4a5cbb34ed15b4a12f627a2`、`manifest_digest=296af53055c8a2e2f8c5310e2d93cf252290fcfc4cfef5511574397d47a5782f`。

**与第四次验证（`-v9`）无关的并列说明（必须标注）**：`-v9` 的两份失败阶段 `A` 工件在 `-v10` 中**不存在**——第五次验证的阶段 `A` **首跑即有效**（`docs/phase6/58i`）。`-v10` 目录内**没有**任何失败轮产物，因此本轮的读数来源**不含**「失败轮不作读数来源」的排除项。

## 3. 判定命令与载荷

```text
$ cd backend && /usr/bin/time -l uv run python -m tests.mixed_bot_judgment \
    /Users/bryanylliu/holdem-mixed-bot-validation-v10/input/frozen-fixture-manifest.json \
    /Users/bryanylliu/holdem-mixed-bot-validation-v10/runs/stage-b/mixed-validation-stage-B.json \
    /Users/bryanylliu/holdem-mixed-bot-validation-v10/runs/stage-b/mixed-observations-stage-B.json \
    --allow-judgment \
    --recheck=阶段B补算=/Users/bryanylliu/holdem-mixed-bot-validation-v10/runs/stage-b-recheck/mixed-recheck-stage-B.json
exit=0
```

- `payload_version = mixed-judgment-payload.v1`；载荷 `18,965 B`；`stderr` 为空（`stderr` 仅有 `/usr/bin/time -l` 的进程级读数）；
- 进程级实测：`real 1.23 s`、`maximum resident set size 696,057,856 B`（`663.81 MiB`）——**这是判定器进程的开销，与包络判定无关**（包络判定只针对已完成的阶段产物）；
- 判定器**未写出任何文件**；`-v10` 六份工件 sha 判定前后**逐字未变**。

## 4. 判定计算口径与固定时点

### 4.1 读数前已锁定的口径（强）

| 编号 | 口径 | 锁定位置 | 时点 |
|---|---|---|---|
| `X1` | L4 区间唯一方法：块均值双侧 95% t 区间，`df=S−1`，`S=16` ⇒ `t(0.975,15)=2.131`；**强制并列**块均值 / 块标准差 / 块极值 / 逐人数分解；两口径不一致判**未满足** | `docs/phase6/58f` §9.1（转写自 `docs/phase6/56a` §6） | **读数之前** |
| `X2` | L3 第 1/2/3/4 项的固定估值块 = **清单种子序的第 `1` 个主种子块**（`VALUATION_BLOCK_INDEX = 0`）；第 5 项按全部 `16` 块 | `docs/phase6/58f` §9.2（转写自 `docs/phase6/56b` §7） | **读数之前** |
| `S1` | 判定口径**声明沿用**判定器 `backend/tests/mixed_bot_judgment.py` 的 `11` 条钉子 + 黄金样本；**本轮未改动任一钉子** | `docs/phase6/58f` §10（转写自 `docs/phase6/57l`） | **读数之前** |

**本轮的具体取值**（与 `docs/phase6/58f` §9.2 的机械代入**一致**）：第 1/2/3/4 项的估值块 = 第六套清单种子序第 `1` 块 **`3008262424`**（**清单序**，不是按数值排序取最小值）；判定载荷字段 `valuation_seed_block = 3008262424` 与此**吻合**。

**判定器的 `11` 条钉子（原样引用，未改）**：

1. 估值单元取清单种子序的第一个主种子块；短段排序同样使用清单序，不使用数值大小。
2. 实质动作混合 = 次高份额 ≥10%；分域按类别自然分区；节点读数先合并 units 再套同一规则。
3. 类型熵 = 逐风格分布熵的算术均值；金额条件熵 = 存在额度分布的行的均值。
4. 额度锚点 = 同额合并后份额 ≥15% 的档数；达到两档记为满足。
5. 机会域概率 = 先按节点合并手模式 units，再按节点等权平均；进池取 `call`/`bet`/`raise`，主动取 `bet`/`raise`。
6. 风格可分度 = 三个配对中至少两对在两域上各 ≥10 个百分点。
7. 风格间距离直读工件字段，不在本模块重算。
8. 稳定性 = 同一配对同一域的差值在全部种子块上符号一致（含恒为零）。
9. 不劣化 = 逐人数各自取最差族；块均值双侧 t 区间（`S=16`，`t=2.131`），块极值口径并列，二者不一致判未满足。
10. 针对性诊断的短段 = 每（对手族 × 风格）按清单种子序 / 人数 / 轮换排序的前 `20` 对。
11. 针对性诊断的面对事件 = 被测一方首次面对对手下注或加注；额度 ÷ 街起始底池（翻前 `1.5` 个大盲）分三档。

### 4.2 口径自洽核验

判定器在上一次独立验证（第四次验证）后**未被改动**；其「`11` 条钉子 + 黄金样本」回归由 `backend/tests/test_mixed_bot_judgment.py` 承载，含在本轮提交前实跑的全量测试内（`uv run pytest -q` → **`1271 passed, 3 skipped`**）。因此本轮实现与原口径同源，**未出现**「已公布读数无法复现」的情形。

### 4.3 未满足的前置（如实记账）

按 `docs/phase6/58f` §11 第 4 条，「用更多 seed 估计判定反转频率」本次**未做**。因此即使判据全部满足，结论也只能表述为「**本次独立验证下判据满足**」；**不得**表述为稳健达标、质量已证明或不可剥削。

### 4.4 本轮的披露项（汇总索引）

| # | 披露项 | 位置 |
|---|---|---|
| i | L3 第 3 项**两读法本轮不分歧**（`382/382`，结构性单档 `0`） | §7 |
| ii | L3 第 5 项「方向稳定」在本样本上是**结构性必然** | §7 |
| iii | L3 第 4 项域二**恰好 `2/3` 对达标**、第 4b 项最薄 `0.2028` ⇒ **邻近阈值** | §7 |
| iv | 判定载荷**未单独落盘**，靠确定性复算保真 | §2 |
| v | `code_identity` 与实际 HEAD **不同** | §10 |
| vi | `-v10` 清单内 `resource_envelope` 文本写死 `1 GiB`，判定上界为 `1.5 GiB` | §11 |

## 5. L1 机制与正确性（必要条件）

| 项 | 读数 | 解释性状态 |
|---|---|---|
| 违规数 | 阶段 `A` `violations=[]`、阶段 `B` `violations=[]` | 读数 `0` |
| 截断手数 | 阶段 `A` `truncated_hands=0`、阶段 `B` `truncated_hands=0` | 读数 `0` |
| 摘要一致性 | `digests_consistent = true`（阶段回执 `config_digest::manifest_digest` 与清单一致） | 一致 |
| 随机可重放 | `replay_hands_compared = 33792`、`replay_net_chips_exact = true`、`replay_behavior_exact = true`（来源：阶段 `B` 补算） | 确定性重放通过 |
| 信息边界 | 观测工件一致性核对 `observations_checked = true`（**该结论仅在观测工件存在时成立**）；定向契约测试 `tests/test_info_boundary.py` → **`7 passed`** | 未观察到越界 |

L1 为**必要条件**：上述任一为否即不可继续；本轮五项均为读数为「无异常」的形式，**不构成**质量证据。

## 6. L2 成本（必要条件）

**统计量**：端到端单次决策 `DECISION_BUDGET_MS=100` 为**硬预算**；`p95 <50 ms`、`p99 <80 ms` 为指导值；**逐人数报告分位与样本数**，**不把合并均值当各人数通过**；**不把查询延迟当端到端决策延迟**。

| 项 | 读数 |
|---|---|
| 硬预算 | `hard_budget_satisfied = true`；`timeout_count = 0` / `samples = 80000` |
| 指导值 | `guidance_satisfied = true` |
| 合并分位（`80,000` 样本） | `p50 0.2839 ms` / `p95 0.3889 ms` / `p99 0.6535 ms` / `max 21.7436 ms` |
| 样本数（逐人数） | 各 `10,000`（`2`–`9` 人共 `8` 组） |
| 超时（逐人数） | 各 `0` |
| 逐人数 `p95` 区间 | `0.3481 ms` – `0.4373 ms`（**逐人数分位见阶段 `B` 回执的分组读数**） |

**必须一并标注**：`coverage.executed_nodes = 125` 是**清单库存口径**（第六套清单的全部可执行节点），**不是**本轮执行数；本轮执行手数由 `matches` 给出（阶段 `B` `33,792`）。L2 为**必要条件**，其达标**不是**质量证据。

## 7. L3 不可预测性（D7 六项，估值块 = `3008262424`）

| # | 判据（门槛） | 本轮读数 | 解释性状态 |
|---|---|---|---|
| 1 | 实质动作混合覆盖率 **≥25%**，并分域报告翻前 / 面对下注 / 免费行动 | 整体 **`60.71%`**（`683/1,125`）；分域 翻前 `73.25%` / 面对下注 `50.97%` / 免费行动 `52.78%`；并列按 `125` 节点聚合 `74.40%`（`93/125`） | 读数高于门槛 |
| 2 | 平均类型熵 **≥0.15 bit** | `0.6079`（tight） / `0.6667`（aggressive） / `0.4454`（calling），各 `375` 分布；最小值 `0.4454` | 读数高于门槛 |
| 3 | 至少两档锚点**各 ≥15%**；金额条件熵 **≥0.5 bit** | 锚点满足率 **`382/382 = 100%`**；**结构性单档 `0`**；金额条件熵均值 **`1.4005`**（分风格 `1.3610` / `1.5395` / `1.2955`，行数 `117` / `133` / `132`） | **两种读法本轮不分歧，均满足** |
| 4 | **至少两档**在两个预先指定机会域上**各 ≥10 个百分点** | 域一（翻前进池率）：`tight 43.42%` / `aggressive 79.69%` / `calling 94.56%`；差值 `t-a −36.26` / `t-c −51.14` / `a-c −14.87` pp ⇒ **`3/3` 对达标**；域二（翻牌面对下注主动率）：`tight 4.71%` / `aggressive 29.34%` / `calling 14.02%`；差值 `t-a −24.63` / `t-c −9.32` / `a-c +15.31` pp ⇒ **`2/3` 对达标** | 满足（**恰好两对**） |
| 4b | 风格间 JS 距离 **≥0.15**（沿用，不重标定） | `0.2593`（tight–aggressive） / `0.3551`（tight–calling） / `0.2028`（aggressive–calling），各 `125` 节点；最薄 `aggressive–calling = 0.2028` | 读数高于门槛（**邻近**） |
| 5 | 风格稳定性：**方向稳定** | `6/6` 个（配对 × 域）组合在 **`16/16` 块**同号；`blocks_vary = false` | 方向稳定（见下方结构性披露） |

判定器给出的 `unpredictability.unsatisfied_items` 为 **`[]`**（无未满足项）。

**第 3 项的两读法与处置（如实记账）**：

- 读法甲（逐分布全满足）：`382/382` 个单元达成 ≥2 档锚点 ⇒ **满足**；
- 读法乙（退化行另报、非退化行满足）：结构性单档 `0`，非退化 `382/382` 全部满足 ⇒ **满足**；
- ⇒ 本轮**两种读法结论一致**，**不产生**上一轮那样的分歧；`docs/phase6/58f` §10 的**保守处置口径未变**（本轮只是恰好无需动用）。

**必须一并标注的四条**：

1. **邻近阈值**：第 4b 项最薄余量为 `aggressive–calling` 的 `+0.0528`；第 4 项域二为**恰好 `2/3` 对达标**（三对中最薄一对为 `−9.32 pp`，未达 10 pp）。按 `docs/phase6/58f` §5 第 1 条与 D7 第 4 条，**邻近阈值的达标不得表述为稳健达标**。
2. **域二的读数是决定性的**：按冻结定义（`docs/phase6/58f` §8.2，域二 = 主动概率）即为该口径。**不得**以「该域天然难有主动」为由在读数后更换域定义。
3. **不得剔除结构恒等节点**：本轮**未剔除**任何节点后再报告，分母为清单全部 `125` 节点 / `1,125` 个直接分布。
4. **不得在读数后放宽**：不得以「门槛原为旧口径提出、可能过紧」为由放宽；该风险已在读数前如实披露，属**已被接受的风险**。

**结构性披露（影响第 5 项的证据力）**：判定器读出 `stability.blocks_vary = false`，即**每一（配对 × 域）差值在 `16` 个种子块上取值完全相同**。因此：

- 第 5 项的「`16/16` 块同号」在本样本上是**结构性必然**，**不构成独立证据**；
- 第 1/2/3/4 项的「估值块取第 `1` 个主种子块」在本样本上**不产生差异**（口径仍按清单序执行，取值未改）；
- 该事实**只对本轮样本成立**，**不得**外推到其他身份或样本集。

**不得用于替代判定的相邻读数（防止换分母）**：逐节点回执字段（如 `has_active_candidate`、`structural_single_size`、`effective_scale_count`）**不是**覆盖率读数，**不得**用它们替代第 1 项、也**不得**反向推断覆盖率。

## 8. L4 不劣化（D4，逐人数独立判定）

**统计量**：相对 `heuristic@1` 的最差族**配对**收益差 95% 区间**下界 ≥ −5 BB/100**；区间按 `X1`（块均值 t 区间，`t(0.975,15)=2.131`）；最差族**逐人数各自取**。

| 人数 | 最差族 | 均值 BB/100 | t95 下界 | 块 min | 块 max | 状态 |
|---|---|---|---|---|---|---|
| 2 | `fixed-pressure-probe` | `−329.69` | `−1972.81` | `−5550.00` | `+4975.00` | 未满足 |
| 3 | `aggression-rate-probe` | `−654.58` | `−1585.54` | `−5550.00` | `+1211.11` | 未满足 |
| 4 | `marginal-call-pressure-probe` | `−880.57` | `−3188.49` | `−12213.33` | `+4819.17` | 未满足 |
| 5 | `marginal-call-pressure-probe` | `−1231.08` | `−3365.44` | `−9288.00` | `+4889.33` | 未满足 |
| 6 | `marginal-call-pressure-probe` | `−300.35` | `−999.92` | `−4772.22` | `+1221.11` | 未满足 |
| 7 | `marginal-call-pressure-probe` | `−839.97` | `−2036.36` | `−4403.81` | `+3582.86` | 未满足 |
| 8 | `marginal-call-pressure-probe` | `−582.34` | `−1830.73` | `−5241.67` | `+4381.67` | 未满足 |
| 9 | `random@1` | `−986.60` | `−2613.32` | `−6605.93` | `+7775.93` | 未满足 |

**逐人数判定结果：`0 / 8` 人数满足**；且 **t 区间与块极值两口径均为 `0/8`**（口径选择不改变结论）。故 L4 记为**不满足**。

配对完整性：`16,896` 对，**配对异常 `0`**。

**双向报告（D4 第 3 条，不得只报有利一侧）**

| 人数 | 最大正偏离族 | 均值 BB/100 |
|---|---|---|
| 2 | `heuristic@1` | `+107.71` |
| 3 | `heuristic@1` | `+15.63` |
| 4 | `position-pressure-probe` | `+843.54` |
| 5 | `fixed-pressure-probe` | `+385.21` |
| 6 | `aggression-rate-probe` | `+473.54` |
| 7 | `random@1` | `+1280.80` |
| 8 | `fixed-pressure-probe` | `+1119.30` |
| 9 | `aggression-rate-probe` | `+338.03` |

**跨人数合计（仅作报告，不得据此归因到具体人数）**：`mixed-focus` 净筹码 `+1,392,472` 对 `legacy-focus` `+1,405,338`；`16` 块中 `mixed` 更高者 `8` 块。

**该读数不是强度证据**：机械探针不是 best response、逐格手数有限、`16` 个固定种子块即便给出区间也只是粗区间。

## 9. L5 抗针对（无数值门槛，属报告层）

### 9.1 逐族读数（第 1 项）

| 对手族 | `mixed-focus` 净筹码 | 均值 BB/100 | 块 min | 块 max | 手数 |
|---|---|---|---|---|---|
| `aggression-rate-probe` | `+233,761` | `+1106.82` | `−213.33` | `+3129.70` | `2,112` |
| `fixed-pressure-probe` | `+228,652` | `+1082.63` | `−1074.24` | `+4162.50` | `2,112` |
| `heuristic@1` | `+9,580` | `+45.36` | `−340.00` | `+540.68` | `2,112` |
| `marginal-call-pressure-probe` | `+248,317` | `+1175.74` | `+70.23` | `+2019.24` | `2,112` |
| `passive-call-probe` | `+198,437` | `+939.57` | `−146.59` | `+2224.47` | `2,112` |
| `position-pressure-probe` | `+205,263` | `+971.89` | `−96.14` | `+2207.05` | `2,112` |
| `random@1` | `+219,695` | `+1040.22` | `−578.48` | `+2690.08` | `2,112` |
| `size-signal-probe` | `+48,767` | `+230.90` | `−14.85` | `+460.30` | `2,112` |

对手族集**沿用**既有 `8` 族，**未**增删、**未**重定义、**未**替换。逐人数分解可由阶段 `B` 回执的 `matches.rows` 复算；本节跨人数汇总**仅作报告**，**不得**据此归因到具体人数。

### 9.2 针对性诊断（第 2 项）

**口径**：短段 = 每（对手族 × 风格）内按（**清单种子序**、人数、轮换）排序的前 `20` 对（合计 `60` 对/族）；长段 = 其余 `2,052` 对/族；增益 = 配对差 bb/100 的 **短段 − 长段**。总对数 `16,896`；其中含面对事件者 `5,524`（占比 `32.69%`）。

| 族 | 短段 bb/100 | 长段 bb/100 | 差（短 − 长） |
|---|---|---|---|
| `aggression-rate-probe` | `−1205.33` | `+117.53` | `−1322.86` |
| `fixed-pressure-probe` | `+987.50` | `+359.95` | `+627.55` |
| `heuristic@1` | `+664.67` | `−7.98` | `+672.65` |
| `marginal-call-pressure-probe` | `+406.83` | `−605.52` | `+1012.35` |
| `passive-call-probe` | `−493.33` | `+12.29` | `−505.62` |
| `position-pressure-probe` | `−520.33` | `+40.57` | `−560.90` |
| `random@1` | `+682.50` | `+192.92` | `+489.58` |
| `size-signal-probe` | `−952.67` | `−159.87` | `−792.80` |

**尺度 / 牌力 / 街道分桶**（判定器钉死实现，本次**未**临时另写）：

| 维度 | 桶 | 短段对数 | 长段对数 | 短段 bb/100 | 长段 bb/100 | 差 |
|---|---|---|---|---|---|---|
| 尺度 | `>100%` | `81` | `3,932` | `+2013.83` | `+258.28` | `+1755.55` |
| 尺度 | `50–100%` | `7` | `443` | `+157.14` | `+43.48` | `+113.67` |
| 尺度 | `≤50%` | `31` | `1,030` | `−622.90` | `−645.27` | `+22.37` |
| 尺度 | 无面对加注 | `361` | `11,011` | `−472.91` | `−42.96` | `−429.95` |
| 牌力 | `strong` | `41` | `867` | `−5071.95` | `−1362.26` | `−3709.69` |
| 牌力 | `medium` | `132` | `4,566` | `+981.59` | `+247.93` | `+733.66` |
| 牌力 | `weak` | `208` | `9,745` | `+51.30` | `+11.60` | `+39.70` |
| 牌力 | `None` | `99` | `1,238` | `+423.23` | `−134.81` | `+558.04` |
| 街道 | `preflop` | `67` | `3,385` | `+1221.34` | `+327.45` | `+893.89` |
| 街道 | `flop` | `45` | `1,803` | `+1289.33` | `−425.87` | `+1715.20` |
| 街道 | `turn` | `6` | `132` | `+851.67` | `+631.14` | `+220.53` |
| 街道 | `river` | `1` | `85` | `−50.00` | `−632.00` | `+582.00` |

**结论边界（不得越过）**：不做 best response ⇒ 只能表述为「**本次样本集与本次探针池下未观察到 X**」；**不得**表述为「不可剥削」，**不得**作为 exploitability / NashConv / best response 上界。命中率与分桶增益**不是**质量证据，也**不得**作为判定输入（判定输入只有 §7 / §8 的 L3 / L4 读数）。

## 10. `code_identity` 差异披露

- 清单与各报告内的 `code_identity = d46c780a3c962bf17dcbe62443e0bde926caf372`（**清单冻结时的 HEAD**）；
- 判定执行时的实际 HEAD = `f128c8cabe359f9e3b8aa1eca29c84f10e6be357`；
- ⇒ 二者**不同**（清单冻结后已落的回执提交使 HEAD 前移），**不得**把本轮判定说成由其中任一 HEAD 的代码产生。

## 11. 包络文本与判定上界的差异（必须与读数并列）

- `-v10` 清单内 `resource_envelope` 文本仍写作 `RSS ≤ 1073741824 字节`（`1 GiB`），该常量**未获授权改动**；
- 本轮运行的峰值 RSS **判定上界**为 **`1.5 GiB`（整调用口径）**（`docs/phase6/58g`）；
- ⇒ 二者**存在已披露的差异**；三阶段的实测整调用峰值 RSS 分别为 `183.57 MiB`（阶段 `A`）/ `1201.00 MiB`（阶段 `B`）/ `950.94 MiB`（补算），**均在新上界内**；**不得**声称清单文本已更新，也**不得**把历史轮次（v1–v8 未记录整调用口径）说成「均未超界」。

## 12. 读数纪律与未做事项

- **本轮只做读数，未做签收**：`docs/phase6/58f` §11 绑定的**质量签收结论不在本篇**，须**另起一轮**单独出具；本篇**不得**被引用为签收文件；
- **未做**：任何签收或默认策略变更；修改任何判据 / 门槛 / 机会分母 / 估值块 / 区间方法；改判定器任一钉子；重跑任何阶段或补算；删除任何样本；任何代码改动；任何产物改写；推送；
- **判定器边界**：不产生对局、不新增样本、不使用随机数、**不写出任何文件**；不得被当作新证据来源；
- **仍未证**：第八版在第六套样本集上的**结论性**质量判断（签收未做）；「用更多 seed 估计判定反转频率」**未做**。

## 13. 契约、API 与默认策略

- **未改**任何代码；API 字段、数据库列、内部契约、报告 / 观测 / 补算 / 判定 schema、artifact schema **零改动**；`MixedFixtureManifest` **未加字段**；
- **未新增**策略身份或清单；`-v10` 六份工件 sha **未变**；
- 默认策略仍为 **`heuristic@1`**；四份「质量未获支持」签收（`docs/phase5/53c`、`docs/phase6/55g`、`56h`、`57k`）**未被反转**；`docs/phase4/37` §3.1 **未触发**。

## 14. 不得声称

- 不得把本轮的 `violations=[]`、`truncated_hands=0`、`digests_consistent=true`、`replay_*_exact=true` 或 `hard_budget_satisfied=true` 说成质量证据、判据达标或「验证通过」。
- 不得把本轮读数说成已签收、已改变默认策略或「已达生产门槛」；签收必须另起一轮。
- 不得把 L3 的邻近达标（域二 `2/3`、最薄 JS 距离 `0.2028`）表述为稳健达标。
- 不得把 §9 的探针读数说成不可剥削、exploitability / NashConv / best response 上界或强度认证。
- 不得把 §9.2 的分桶增益当作判定输入；不得因该分桶结果回头改读法或改门槛。
- 不得把「结构性必然」的第 5 项当作独立证据；不得把第 1/2/3/4 项在本样本上「估值块不产生差异」外推到其他样本集。
- 不得声称 `-v10` 清单文本已与判定上界一致；不得把历史轮次（v1–v8）说成「均未超包络」。
- 不得把本文件说成对 `docs/phase5/53c` / `docs/phase6/55g` / `56h` / `57k` 任一签收结论的反转、修正或追认；也不得说成对 `docs/phase6/58b`「质量未获支持」结论的改动。
- 不得据此修改 `docs/` 下任何已归档文档与任何已落盘工件；不得重跑任何已完成的阶段、补算或判定。

## 15. 本篇唯一下一步

`J3` 三轮已完成、判定读数已出。按 `docs/phase6/58f` §11 第 1 条，判定结果**直接决定**本次质量签收结论，因此下一步是**质量签收轮次**（**单独一轮、需单独授权**）。

签收轮的已知边界（须在授权时一并声明）：

1. 签收时**不得**新增判据、**不得**修改判据、**不得**删除失败样本、**不得**按结果换 seed、**不得**按结果更换机会分母；
2. 判定结构为**逐人数各自独立**、**禁止跨人数归因**；观测数 `<2` 明确失败；
3. L4 的读数（`0/8` 人数满足，两口径一致）与 L3 六项满足的读数**均须完整并列**，**不得**择一呈现；
4. 「用更多 seed 估计判定反转频率」**未做**，因此任何结论**不得**表述为稳健达标。
