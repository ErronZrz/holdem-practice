# 58b M8：第四次独立验证的判据判定与签收回执（`-v9`）

> 日期：2026-09-23。
>
> 本文件接在 `docs/phase6/58a-m8-stage-b-recheck-record.md` 之后，沿用 `docs/30` §10.1 D6 的编号族约定。归档路径按 `docs/README.md` 的维护约定：本文件属第四次验证族，**共用族号 `58`**，故取 **`58b`**。`docs/phase6/60`–`72` 与 `58a` 为已落盘文档，**不改写、不重编号**。
>
> 本文件记录：按 `docs/phase6/66` 冻结的 L1–L5 判据与全部判定口径、在**已冻结样本**（`-v9`）上作出的**判定读数**，以及由 D2（`docs/55` §10.4）绑定推出的**本次质量签收结论**。
>
> **措辞约束**：按 `docs/55` §7.5 第 1 条，判定只输出**解释性状态**，不使用「合格 / 不合格 / 通过 / 达标」。文中「满足 / 不满足」仅作为 D4（§10.6）与 D7（§10.7）原文所用的**绑定术语**出现。
>
> **本篇同时是披露文件**：它披露 (a) 第 3 项存在**两种读法**且本篇保守记为不满足，(b)「方向稳定」在本样本上是**结构性必然**，(c) 判定载荷**未单独落盘**。三处均逐条留痕（§4.4 / §7 / §2）。

## 1. 授权原文与边界

用户答复原文依次为：

> 「A.」——授权**判定轮次**（只读运行判定器 `tests.mixed_bot_judgment`，按 `docs/phase6/66` 冻结判据出读数）。

> 「A.」——**签收**问项：选定「签收「质量未获支持」结论」。

> 「A.」——**落盘与提交**问项：授权落盘本回执 + 单条 `docs:` 提交（**不含推送**）。

| 项 | 内容 |
|---|---|
| 被授权 | 按已冻结口径做判定计算；出具本判定与签收回执；**单条 `docs:` 提交** |
| 计算口径 | 只读**已落盘工件的现有字段**；判定器**不写任何文件、不产生对局、不使用随机数**；不改动代码、不重跑任何阶段或补算 |
| 明确排除 | 修改任何判据、门槛、机会分母、估值块或区间方法；改判定器任一钉子；重跑任何阶段或补算；删除任何样本；默认策略的自主切换；任何包络数值的修改；**推送** |
| 保留 | v1–v8 全部工件、`-v9` 全部 8 个工件**原样保留**（含 `-v9` 失败阶段 `A` 两份与 v6 失败阶段 A `86e84fed…`） |
| 未消耗 | 未消耗任何新的运行授权；`docs/37` §3.1 **未触发** |

## 2. 只读基线核验（判定前）

```text
$ git status --short --branch
## master...origin/master [ahead 1]

$ git status --porcelain=v1 -uall | wc -l
0

$ git rev-parse HEAD
82bad2e250d2d15d951d07d373b624e7d8fdd1d1

$ git rev-parse origin/master
07f0623581d75ed558247d5c8fb41ae736960a75
```

判定计算**开始前**工作区**零改动**；判定器**未写出任何文件**。**判定载荷未单独落盘**（沿用 v8 先例）：它以 stdout 形式产出并**逐字转录于本文件**，可由同一命令在**同一批已冻结工件**上**确定性复算**（判定器不使用随机数）。因此本回执是判定**之后**的记录文件，其中不含计算代码。

**判定期间额外运行的只读命令（如实记账）**：为取得 L1「信息边界」项的**直接读数**，运行了一次定向契约测试 `uv run pytest tests/test_info_boundary.py -q` → **`7 passed`**。这是本轮唯一的额外命令；它不产生对局、不写工件、不改代码。

**判定输入工件（`-v9`，共 `8` 个，判定前后 sha 逐字未变）**：

| 工件 | `sha256` | 角色 |
|---|---|---|
| `input/frozen-fixture-manifest.json` | `499258c993d1e59d5c2f2c218b79f2944059d216e05e3c20998675c20704a7c6` | 身份与摘要基准、种子清单序 |
| `runs/stage-a/mixed-validation-stage-A.json` | `ffdd79f49dd0aa5f2c91fb4f27cf1ab8652ab5bef52b2e255d5e741c0ac764a6` | **失败轮，不作读数来源**（原样保留） |
| `runs/stage-a/mixed-observations-stage-A.json` | `32d9d677fc75a2841da616deabf769af79aa35b48e67c9cccf43e36fb8851c66` | **失败轮，不作读数来源**（原样保留） |
| `runs/stage-a-retry/mixed-validation-stage-A.json` | `7e845cd00abaa2ca0a92e3057719cabe9b130dffab9fad4d7fcd7b616fe47d43` | 阶段 `A` 有效回执 |
| `runs/stage-a-retry/mixed-observations-stage-A.json` | `5a26864327e79f9d77d2cb16d19979dbf733f1d6593c797619b6e707c7c02d79` | 备用 |
| `runs/stage-b/mixed-validation-stage-B.json` | `963451e2c0e4d98abda8b007a7ee7db66c3c853dd1fb9b3ccbc6a44da827cf1f` | **主来源** |
| `runs/stage-b/mixed-observations-stage-B.json` | `33920ec5a9088e023b5a17296fa75ce0801cb00822ae94e8bcb0c92a47dc9dd6` | L5 第 2 项 |
| `runs/stage-b-recheck/mixed-recheck-stage-B.json` | `954ad61d21c3bac8ba1d856d8616ab2895c58157f6105e1993c5eb439d2268b9` | L1（`33,792` 手确定性重放） |

身份：`strategy_id=mixed-local@7`、`config_digest=fd7433e35fc8260d03635774b34fbdc419ad5a04ba0448916b036e385a546665`、`manifest_digest=319cbb967d66dabdd795c19d60ab91962cd9397926ea71349583d855edc6a918`。

**`code_identity` 差异（必须与读数并列）**：清单与各报告内的 `code_identity = ae3f74930f022a69a4b64a7fcb85188c47384a74`（**冻结时的 HEAD**）；判定执行时的实际 HEAD = `82bad2e250d2d15d951d07d373b624e7d8fdd1d1`。二者**不同**（冻结后已落的回执提交使 HEAD 前移），**不得**把本轮判定说成由其中任一 HEAD 的代码产生。

## 3. 判定命令

```text
$ cd backend && uv run python -m tests.mixed_bot_judgment \
    /Users/bryanylliu/holdem-mixed-bot-validation-v9/input/frozen-fixture-manifest.json \
    /Users/bryanylliu/holdem-mixed-bot-validation-v9/runs/stage-b/mixed-validation-stage-B.json \
    /Users/bryanylliu/holdem-mixed-bot-validation-v9/runs/stage-b/mixed-observations-stage-B.json \
    --allow-judgment \
    --recheck=阶段B补算=/Users/bryanylliu/holdem-mixed-bot-validation-v9/runs/stage-b-recheck/mixed-recheck-stage-B.json
exit=0
```

`payload_version = mixed-judgment-payload.v1`；载荷 `18,939 B`；`stderr` 为空。

## 4. 判定计算口径与固定时点

### 4.1 读数前已锁定的口径（强）

| 编号 | 口径 | 锁定位置 | 时点 |
|---|---|---|---|
| `X1` | L4 区间唯一方法：块均值双侧 95% t 区间，`df=S−1`，`S=16` ⇒ `t(0.975,15)=2.131`；**强制并列**块均值 / 块标准差 / 块极值 / 逐人数分解；两口径不一致判**未满足** | `docs/phase6/66` §8.1（转写自 `docs/56a` §6） | **读数之前** |
| `X2` | L3 第 1/2/3/4 项的固定估值块 = **清单种子序的第 `1` 个主种子块**（`VALUATION_BLOCK_INDEX = 0`）；第 5 项按全部 `16` 块 | `docs/phase6/66` §8.2（转写自 `docs/56b` §7） | **读数之前** |
| `S1` | 判定口径**声明沿用**判定器 `backend/tests/mixed_bot_judgment.py` 的 `11` 条钉子 + 黄金样本；**本轮未改动任一钉子** | `docs/phase6/66` §9（转写自 `docs/57l`） | **读数之前** |

**本轮的具体取值**：第 1/2/3/4 项的估值块 = 第五套清单种子序第 `1` 块 **`1780719137`**（**清单序**，不是按数值排序取最小值）。

**判定器的 `11` 条钉子（原样引用，未改）**：

1. 估值单元取清单种子序的第一个主种子块；短段排序同样使用清单序，不使用数值大小。
2. 实质动作混合 = 次高份额 ≥10%；分域按类别自然分区；节点读数先合并 units 再套同一规则。
3. 类型熵 = 逐风格分布熵的算术均值；金额条件熵 = 存在额度分布的行的均值。
4. 额度锚点 = 同额合并后份额 ≥15% 的档数；达到两档记为满足。
5. 机会域概率 = 先按节点合并手模式 units，再按节点等权平均；进池取 `call`/`bet`/`raise`，主动取 `bet`/`raise`。
6. 风格可分度 = 三个配对中至少两对在两域上各 ≥10 个百分点。
7. 风格间距离直读工件字段，不在本模块重算。
8. 稳定性 = 同一配对同一域的差值在全部种子块上符号一致（含恒为零）。
9. 不劣化 = 逐人数各自取最差族；块均值双侧 t 区间（`S=16`，`t=2.131`），块极值口径并列，二者不一致判未满足。
10. 针对性诊断的短段 = 每（对手族 × 风格）按清单种子序 / 人数 / 轮换排序的前 `20` 对。
11. 针对性诊断的面对事件 = 被测一方首次面对对手下注或加注；额度 ÷ 街起始底池（翻前 `1.5` 个大盲）分三档。

### 4.2 口径自洽核验

判定器在上一次独立验证（v8）后**未被改动**；其「`11` 条钉子 + 黄金样本」回归由 `backend/tests/test_mixed_bot_judgment.py` 承载，含在本轮提交前实跑的全量测试内（`uv run pytest -q` → **`1239 passed, 3 skipped`**）。因此本轮实现与原口径同源，**未出现** v7→v8 期间那种「已公布读数无法复现」的情形；本轮**不需要**对判定做保守处置以外的任何口径处置。

### 4.3 未满足的前置（如实记账）

按 `docs/phase6/66` §10 第 4 条，「用更多 seed 估计判定反转频率」本次**未做**。因此即使判据全部满足，结论也只能表述为「**本次独立验证下判据满足**」；本次判据未全满足，该限制不改变结论方向，但必须一并记录。

### 4.4 本轮的披露项（汇总索引）

| # | 披露项 | 位置 |
|---|---|---|
| i | L3 第 3 项存在**两种读法**；本篇按保守处置记为不满足，并并列另一读法 | §7 |
| ii | L3 第 5 项「方向稳定」在本样本上是**结构性必然** | §7 |
| iii | 判定载荷**未单独落盘**，靠确定性复算保真 | §2 |
| iv | `code_identity` 与实际 HEAD **不同** | §2 |
| v | `-v9` 清单内 `resource_envelope` 文本写死 `1 GiB`，判定上界为 `1.5 GiB` | §6 |

## 5. L1 机制与正确性（必要条件）

| 项 | 读数 | 解释性状态 |
|---|---|---|
| 违规数 | 阶段 `A` 重试 `violations=[]`、阶段 `B` `violations=[]`（阶段 `A` 失败轮不作读数来源） | 读数 `0` |
| 截断数 | 阶段 `B` `truncated_hands=0`；阶段 `B` 补算 `aggregates` 截断合计 `0` | 读数 `0` |
| 摘要一致 | `config_digest` / `manifest_digest` 在清单、阶段 `A` 重试、阶段 `B` 与阶段 `B` 补算**四处一致**；补算不重算摘要 | 读数一致 |
| 随机可重放 | 阶段 `B` 补算（`33,792` 手）`net_chips_exact=true`、`behavior_exact=true`；**观测工件自洽核对参与且通过**（`observations_checked=true`，工件存在） | 读数一致 |
| 信息边界 | **本轮取得直接读数**：`tests/test_info_boundary.py` → **`7 passed`**；工件层面仍无专门字段，如实标注为**契约测试口径**而非工件字段读数 | 读数通过（口径受限） |

L1 五项**均有读数**，未发现违反。

## 6. L2 成本（必要条件）

| 项 | 读数 | 解释性状态 |
|---|---|---|
| 硬预算 `DECISION_BUDGET_MS=100` | `timeout_count = 0`（样本 `80,000`）；逐人数（`2`–`9`）超时均为 `0`；`hard_budget_satisfied=true` | 未超硬预算 |
| 指导值 | p50 `0.2780` / p95 `0.4031` / p99 `0.6795` / max `15.4823` ms；`guidance_satisfied=true` | p95 <50、p99 <80 读数满足 |
| 逐人数分组 | 人数 `2`–`9` 共 `8` 组，各 `10,000` 样本；逐人数 p95 区间 `0.3597–0.4781 ms` | 已逐人数报告，**未以合并均值代替** |
| 样本量 | `80,000` | 满足 |
| 口径要求 | 查询延迟与端到端决策延迟**分列**；`coverage.executed_nodes=125` 如实标注为**清单库存口径**（`planned=128`、`not_run=0`、`structural=3`） | 满足 |
| 包络（**人工比对**，代码不据此改 `status`） | 阶段 `B`：wall `503.76 s`（整调用）/ 峰值 RSS 整调用 `1212.63 MiB`、自记 `511.38 MiB`；阶段 `B` 补算：wall `651.81 s`（整调用）/ 峰值 RSS 整调用 `918.44 MiB`、自记 `893.05 MiB`；`-v9` 保留文件合计 `88,204,203 B`（`84.12 MiB`） | wall 与输出在两种口径下均未触及 `≤1800 s` / `≤100 MiB`；**RSS 见下方披露** |

**RSS 口径与上界的如实披露（`docs/phase6/70` / `71` / `72` / `58a`）**：

1. 判定口径为**整个调用的最大常驻集**；模块自记 `peak_rss_bytes` 是**下界**，两者**已并列**报告；
2. 阶段 `B` 的整调用 `1212.63 MiB` **超出旧上界 `1 GiB`**（约 `+18.4%`），该事实**照实保留**、**未改写**、**不构成**「包络内通过」；
3. 峰值 RSS 上界自 `docs/phase6/72` 起为 **`1.5 GiB`**，**只对未来生效、不追溯**；本轮阶段 `B` 补算的整调用 `918.44 MiB` 在新上界之内；
4. `-v9` **冻结清单**内的 `resource_envelope` 文本仍写死旧上界 `1 GiB`，该清单是**冻结产物、不得改写**（`sha256` 仍为 `499258c9…`）——二者之间的差异是**已披露的既有差异**；
5. **补算路径仍无自己的声明包络**；其 RSS 余量**未归因**，**不得**外推；
6. 历史轮次（v1–v8）**未记录**整调用口径读数，其是否超包络**只能标注为未知**。

## 7. L3 不可预测性（D7 六项，估值块 = `1780719137`）

| # | 判据（门槛） | `-v9` 读数 | 解释性状态 |
|---|---|---|---|
| 1 | 实质动作混合覆盖率 **≥25%**，并分域报告翻前 / 面对下注 / 免费行动 | 整体 **`60.09%`**（`676/1,125`）；分域 翻前 `62.35%`（`303/486`）/ 面对下注 `54.50%`（`309/567`）/ 免费行动 `88.89%`（`64/72`）；并列按 `125` 节点聚合 `72.00%`（`90/125`） | 读数高于门槛 |
| 2 | 平均类型熵 **≥0.15 bit** | `0.6557`（tight） / `0.6163`（aggressive） / `0.3642`（calling），各 `375` 分布；最小 `0.3642` | 读数高于门槛 |
| 3 | 至少两档锚点**各 ≥15%**；金额条件熵 **≥0.5 bit** | 锚点满足 **`437/455 = 96.04%`**；未满足的 **`18` 行恰为 `structural_single_size` 标记的结构性单档**；金额条件熵均值 **`1.3485`**（分风格 `1.3018` / `1.4835` / `1.2443`；计数 `138` / `165` / `152`）；判定器 `anchors_above_threshold = false` | 金额熵高于门槛；**锚点项两种读法分歧**，本篇**保守记为不满足** |
| 4 | **至少两档**在两个预先指定机会域上**各 ≥10 个百分点** | 域一（翻前进池概率）`tight 64.37%` / `aggressive 94.13%` / `calling 100.00%`，差 `t-a −29.76` / `t-c −35.63` / `a-c −5.87` pp ⇒ **`2/3` 对满足**；**域二（翻牌面对下注主动概率）`tight 8.40%` / `aggressive 19.63%` / `calling 12.54%`，差 `t-a −11.23` / `t-c −4.14` / `a-c +7.09` pp ⇒ `1/3` 对满足** | **不满足** |
| 4b | 风格间 JS 距离 **≥0.15**（沿用，不重标定） | `0.2464`（tight–aggressive） / `0.3018`（tight–calling） / `0.2073`（aggressive–calling），各 `125` 节点 | 读数高于门槛 |
| 5 | 风格稳定性：**方向稳定** | `6/6` 个（配对 × 域）组合在 **`16/16` 块**同号；`direction_stable=true` | 方向稳定（见下方结构性披露） |

判定器自报 `unsatisfied_items = ["额度锚点档数", "两个机会域的风格可分度"]`，与上表第 3、4 项一致。

**第 3 项的两读法与处置（如实记账）**：

- 读法甲（逐分布全满足、且**不剔除结构恒等节点**）：`18 / 455` 个分布在低于 `15%` 的档位上只有一档 ⇒ **不满足**；
- 读法乙（退化行另报、非退化行满足）：`18` 行与 `structural_single_size` 完全重合，按 D7 原文「短码退化单档**另报**」的口径，非退化 `437/437` 全部满足 ⇒ 满足；
- 本篇按 `docs/phase6/66` §9 沿用的**保守处置**记为**不满足**并完整披露另一读法；**两种读法均不改变 L3 整体与总判定方向**（第 4 项与 L4 已独立不满足）。

**必须一并标注的四条**：

1. **D7 第 4 条**：第 4b 项最薄余量为 `aggressive–calling` 的 `0.2073`（余量 `+0.0573`）；按该条，**邻近阈值的达标不得表述为稳健达标**。
2. **域二的读数是决定性的**：按冻结定义（`docs/phase6/66` §7.2，源自 `docs/55a` §7.5：域二 = `flop-draw` 类节点上的**主动概率**）就是该口径。**不得**以「该域天然难有主动」为由在读数后更换域定义。
3. **`docs/55a` §7.3 + D7 第 5 条**：结构恒等节点**照实计入**；本次**未剔除**任何节点后再报告。
4. **D7 第 6 条**：不得以「门槛原为旧口径提出、可能过紧」为由在读数后放宽；该风险已在读数前如实披露，属**已被接受的风险**（`docs/phase6/66` §4 第 3 条）。

**结构性披露（影响第 5 项的证据力）**：判定器报 `blocks_vary = false`，即 `1,125` 个直接分布及其额度与熵值在 **`16` 个种子块上逐位相同**。因此：

- 第 5 项的「`16/16` 块同号」在本样本上是**结构性必然**，**不构成独立证据**；
- 第 1/2/3/4 项的「估值块取第 `1` 个主种子块」在本样本上**不产生差异**（口径仍按清单序执行，取值未改）；
- 该事实**只对本样本成立**，**不得**外推到其他身份或样本集。

## 8. L4 不劣化（D4，逐人数独立判定）

**统计量**：相对 `heuristic@1` 的最差族**配对**收益差 95% 区间**下界 ≥ −5 BB/100**；区间按 `X1`（块均值 t 区间，`t(0.975,15)=2.131`）；最差族按 `docs/phase6/66` §5（**逐人数各自取**）。

| 人数 | 最差族 | 均值 BB/100 | t95 下界 | 块 min | 块 max | 状态 |
|---|---|---|---|---|---|---|
| 2 | `marginal-call-pressure-probe` | `−975.42` | `−1927.80` | `−4808.33` | `+1426.67` | 未达门槛 |
| 3 | `marginal-call-pressure-probe` | `−298.33` | `−1301.78` | `−4727.78` | `+4647.78` | 未达门槛 |
| 4 | `marginal-call-pressure-probe` | `−2394.90` | `−5018.52` | `−11716.67` | `+5466.67` | 未达门槛 |
| 5 | `marginal-call-pressure-probe` | `−1986.50` | `−3380.79` | `−7501.33` | `+1316.67` | 未达门槛 |
| 6 | `heuristic@1` | `−131.88` | `−635.15` | `−3272.22` | `+997.78` | 未达门槛 |
| 7 | `random@1` | `−962.29` | `−2750.94` | `−7214.76` | `+4294.29` | 未达门槛 |
| 8 | `random@1` | `−1000.36` | `−2011.10` | `−4560.83` | `+2019.58` | 未达门槛 |
| 9 | `passive-call-probe` | `−153.03` | `−2074.18` | `−8448.15` | `+8646.67` | 未达门槛 |

**逐人数判定结果：`0 / 8` 人数达门槛**；且 **t 区间与块极值两口径均为 `0/8`**（口径选择不改变结论）。故 L4 记为**不满足**。

配对完整性：`16,896` 对，**配对异常 `0`**。

**双向报告（D4 第 3 条，不得只报有利一侧）**

| 人数 | 最大正偏离族 | 均值 BB/100 |
|---|---|---|
| 2 | `heuristic@1` | `+20.52` |
| 3 | `random@1` | `+906.18` |
| 4 | `fixed-pressure-probe` | `+443.23` |
| 5 | `position-pressure-probe` | `+148.54` |
| 6 | `fixed-pressure-probe` | `+641.32` |
| 7 | `fixed-pressure-probe` | `+473.21` |
| 8 | `aggression-rate-probe` | `+755.65` |
| 9 | `marginal-call-pressure-probe` | `+893.82` |

**跨人数合计（仅作报告，不得据此归因到具体人数）**：`mixed-focus` 净筹码 `+1,532,871` 对 `legacy-focus` `+1,643,481`；`16` 块中 `mixed` 更高者 `7` 块。

**该读数不是强度证据**：机械探针不是 best response、逐格手数有限、`16` 个固定种子块即便给出区间也只是粗区间。它与 L4 的判定结果**并列**呈现，**不得**单独当作结论。

## 9. L5 抗针对（无数值门槛，属报告层）

### 9.1 逐族读数（第 1 项）

| 对手族 | `mixed-focus` 净筹码 | 均值 BB/100 | 块 min | 块 max | 手数 |
|---|---|---|---|---|---|
| `aggression-rate-probe` | `+220,766` | `+1045.29` | `−712.12` | `+4466.06` | `2,112` |
| `fixed-pressure-probe` | `+241,484` | `+1143.39` | `−1039.02` | `+8200.00` | `2,112` |
| `heuristic@1` | `+4,213` | `+19.95` | `−574.77` | `+678.33` | `2,112` |
| `marginal-call-pressure-probe` | `+292,923` | `+1386.95` | `−172.27` | `+3483.26` | `2,112` |
| `passive-call-probe` | `+208,729` | `+988.30` | `−595.83` | `+4436.14` | `2,112` |
| `position-pressure-probe` | `+209,245` | `+990.74` | `−598.26` | `+4395.53` | `2,112` |
| `random@1` | `+302,957` | `+1434.46` | `−694.02` | `+4387.95` | `2,112` |
| `size-signal-probe` | `+52,554` | `+248.84` | `+9.32` | `+643.33` | `2,112` |

逐人数分解可由阶段 `B` 回执的 `matches.rows` 复算；本节跨人数汇总**仅作报告**，按 `docs/phase6/66` §5（D4 第 3 条）**不得**据此归因到具体人数。

### 9.2 针对性诊断（第 2 项）

**口径**：短段 = 每（对手族 × 风格）内按（**清单种子序**、人数、轮换）排序的前 `20` 对（合计 `60` 对/族，`480` 对）；长段 = 其余 `2,052` 对/族（`16,416` 对）；增益 = 配对差 bb/100 的 **短段 − 长段**。

| 族 | 短段 bb/100 | 长段 bb/100 | 差（短 − 长） |
|---|---|---|---|
| `aggression-rate-probe` | `+256.83` | `+134.24` | `+122.59` |
| `fixed-pressure-probe` | `+1573.83` | `+228.54` | `+1345.30` |
| `heuristic@1` | `−345.00` | `−25.52` | `−319.48` |
| `marginal-call-pressure-probe` | `+656.17` | `−294.89` | `+951.05` |
| `passive-call-probe` | `+350.67` | `+57.89` | `+292.77` |
| `position-pressure-probe` | `−241.00` | `−117.90` | `−123.10` |
| `random@1` | `+1449.67` | `−407.96` | `+1857.63` |
| `size-signal-probe` | `−225.83` | `−215.05` | `−10.78` |

**尺度 / 牌力 / 街道分桶**（判定器钉死实现，本次**未**临时另写）：

| 维度 | 桶 | n（短/长） | 短段 | 长段 | 差 |
|---|---|---|---|---|---|
| 尺度 | `≤50%` | `30 / 1008` | `−1240.00` | `+145.00` | `−1385.00` |
| 尺度 | `50–100%` | `10 / 451` | `+9935.00` | `+269.78` | `+9665.22` |
| 尺度 | `>100%` | `120 / 3853` | `+1344.33` | `+12.51` | `+1331.82` |
| 尺度 | 无面对加注 | `320 / 11104` | `−46.72` | `−146.85` | `+100.13` |
| 牌力 | `weak` | `343 / 9631` | `+959.88` | `−74.51` | `+1034.40` |
| 牌力 | `medium` | `100 / 4892` | `−1194.70` | `+227.34` | `−1422.04` |
| 牌力 | `strong` | `0 / 726` | — | `−1753.03` | — |
| 牌力 | `None` | `37 / 1167` | `−33.78` | `−373.98` | `+340.20` |
| 街道 | `preflop` | `101 / 3344` | `+1709.50` | `−63.76` | `+1773.26` |
| 街道 | `flop` | `55 / 1754` | `+957.45` | `+379.27` | `+578.18` |
| 街道 | `turn` | `0 / 134` | — | `−980.15` | — |
| 街道 | `river` | `4 / 80` | `−462.50` | `−58.25` | `−404.25` |

**稀疏度（限制该诊断的可解释力）**：有面对 `bet`/`raise` 的配对为 **`5,472 / 16,896 = 32.39%`**，整手无面对为 **`11,424 / 16,896 = 67.61%`**；`牌力 strong` 与 `街道 turn` 的短段样本为 `0`、`街道 river` 短段仅 `4` 对。

### 9.3 结论边界（D3 第 4 条）

因本次**不做** best response（`docs/phase6/66` §6 已裁定），L5 只能表述为「**本次样本集与本次探针池下未观察到 X**」；**不得**表述为不可剥削，**不得**作为 exploitability / NashConv / best response 的界。

## 10. 汇总判定

| 层 | 项数 | 满足 | 未满足 | 不可判定 |
|---|---|---|---|---|
| L1 机制与正确性 | 5 | 5 | 0 | **0** |
| L2 成本 | 6 | 6 | 0 | 0 |
| L3 不可预测性（D7） | 6 | 4（第 1、2、4b、5 项） | **2（第 3 项保守记、第 4 项）** | **0** |
| L4 不劣化（D4） | 1（逐人数 `8` 项） | 0 | **`8/8` 人数** | 0 |
| L5 抗针对 | 2（报告层） | 第 1 项已报告；第 2 项短/长段与三桶已报告 | 0（无数值门槛） | **0** |

**存在未满足项** ⇒ 按 D7 第 3 条（全部绑定 ⇒ 任一项不过即「质量未获支持」）、D4 第 4 条、`docs/phase6/66` §10：

> **本次结论：质量未获支持；默认策略保持 `heuristic@1`。**

## 11. 不支持的原因必须分开陈述

| 类别 | 本轮涉及项 | 性质 |
|---|---|---|
| **(a) 已计算且未达** | L4（`0/8`，两口径一致）；L3 第 4 项（域二 `1/3` 对，主动率三档在 `8.40%–19.63%`） | **读数明确低于门槛** |
| **(b) 在冻结口径下不可判定** | **无** | — |
| **(c) 口径/报告层缺陷（非判据，如实记账）** | L3 第 3 项的**两读法分歧**（本篇按保守处置记不满足）；判定载荷**未单独落盘** | **报告层**，不改变判定 |

**三条必须写明的限定**：

1. **(c) 类必须与 (a) 类分开陈述**：L3 第 3 项的读法分歧**既不是** L4 / L3 第 4 项未满足的原因，**也不得**被用来稀释它们的结论。
2. **不得**把 (a) 类说成「口径问题」或「工程问题」；**不得**把 L3 第 4 项的未满足说成「域定义偏严」。
3. 本轮**不存在**「无读数即记未满足」的项。

## 12. 用户签收

**签收原文（逐字记录）**：

> 「A. 签收「质量未获支持」结论」

**原文来源（如实披露）**：该原文取自助手在签收问项中提出的**选项 `A` 的文本**；**参考措辞由助手起草，选择与签收均由用户作出**。用户的答复原文为：

> 「A.」

**签收的绑定范围与限定**：

- 绑定范围**仅限本次独立验证**（`-v9` / `mixed-local@7` / 第五套样本集）；
- 签收**不新增、不修改任何判据**（`docs/phase6/66` §10 第 2 条）；本节只记录**用户表态**，不改动 §10 的机械判定结果；
- 签收**不反转、不修正、不追认** `docs/53c` / `docs/55g` / `docs/56h` / `docs/57k` 四份既有「质量未获支持」结论（本次是**再次维持**）；
- 按 `docs/phase6/66` §10 第 5 条：结论为「质量未获支持」，默认策略**保持 `heuristic@1`**；**不得**改判据、**不得**重跑同一集合、**不得**把同一集合反复调参后称为独立验证；若需再试，须**新开身份或新样本集并重新预注册**，且运行需**单独授权**。

## 13. 契约、API 与默认策略

| 对象 | 状态 |
|---|---|
| 公开 API 字段 / 数据库列 / `history_json` | **零改动** |
| `mixed-validation.v2` / `mixed-observations.v1` / `mixed-recheck.v1` | **零改动** |
| `mixed-judgment-payload.v1` | **未改动**（判定器与 `11` 条钉子逐字未改） |
| 清单模型 / `-v9` 冻结清单 | **零改动**（摘要逐字未变） |
| 新策略身份 / 新清单 | **均未新增**；被测身份仍为 `mixed-local@7` |
| `docs/37` §3.1 | **不触发** |
| 默认策略 | **仍为 `heuristic@1`**（本次未获质量支持，必须保持） |
| v1–v8 外部证据 | **零触碰**（目录文件数与关键 `sha256` 逐条复核未变） |
| `-v9` 八个工件 | **零改动、零新增**（本篇不写工件） |
| 包络数值 | **零改动**（`≤1800 s` / `≤1.5 GiB` / `≤100 MiB` 维持 `docs/phase6/72` 的值；清单文本差异见 §6） |

## 14. 未做事项与仍未证

- **未做**：best response 与任何 BR 构造（`docs/phase6/66` §6 已排除）；跨人数归因；用更多 seed 估计判定反转频率；任何参数、门槛或口径改动；任何重跑或补算；默认切换；RSS 包络的任何变更；推送。
- **仍未证**：任何强度、可剥削性或不可剥削结论；跨机器与云端成本；总体（非 `16` 固定种子块）覆盖；L5 第 2 项的桶级结论（`牌力 strong` 与 `街道 turn` 短段样本为 `0`、`river` 短段仅 `4` 对）。
- **未消耗**：本轮**未消耗**任何运行 authorization，也未触发 `docs/37` §3.1；判定期间仅额外运行了一次定向契约测试（`test_info_boundary.py`）。
- **永久已消耗的 A9 授权**（`a9-calibration-i10` 等）一律**不得重试**，与本轮无关。

## 15. 不得声称

- 不得把本次部分达标读数（覆盖率 `60.09%`、类型熵 `0.6557`/`0.6163`/`0.3642`、锚点 `96.04%`、金额熵 `1.3485`、JS `0.2464`/`0.3018`/`0.2073`、稳定性 `16/16`）表述为质量已证明、稳健达标或「基本通过」。
- 不得把 L4 的「已计算未达」说成「口径问题」；不得把 L3 第 4 项的未满足说成「域定义偏严」。
- 不得把 `-v9` 与 v6 / v7 / v8 的**质量读数**直接比较，或说成「更好 / 更差」（**不同节点集、不同种子集、不同策略身份**）；机械量（手数、wall、RSS）对照可作成本外推，但须标注且不得当作质量。
- 不得把 L5 的探针读数表述为不可剥削、exploitability 上界或 best response 结果；不得把 L5 第 2 项的极小样本桶当作发现。
- 不得把「方向稳定」说成独立证据（§7 结构性披露）。
- 不得把本次判定表述为「验证通过」「验收通过」或「已达生产门槛」；不得把 L1 / L2 达标、`completed`、`violations=[]`、确定性重放通过说成质量证据。
- 不得把阶段 `B` 的整调用 RSS 超出**旧**上界的事实说成「包络内通过」，也不得说成「包络已被放宽以适应历史轮次」；不得只报一种 RSS 口径（§6）。
- 不得从本回执或判定载荷中**顺带**计算 `-v9` 的其他读数（覆盖率 / 熵 / JS 距离 / 域差 / 跨人数净值）并当作判定输入——判定输入**仅限** §2 所列 8 个工件与本节读数。
- 不得删除或改写 `-v9` 的**失败阶段 `A`** 两份工件，也不得把它说成「未发生过」或与重试混为一谈。
- 不得把本次结论表述为对 `docs/53c`、`docs/55g`、`docs/56h` 或 `docs/57k` 的**反转**（本次是**再次维持**）。
- 不得据此修改 `docs/` 下任何已归档文档与任何已落盘工件；不得改写 `@1`–`@7` 任一身份的历史含义。
- 不得把 `docs/phase6/62` §6/§7 的**算术推演**说成读数，也不得把 `docs/phase6/72` 的放宽外推到其他规模 / 样本集 / 身份或说成 `wall` / 输出上限的变更。

## 16. 本篇唯一下一步

判据闭环已完成，结论为**质量未获支持**，默认保持 `heuristic@1`。

按 `docs/phase6/66` §10 第 5 条，若需再试，必须**新开策略身份或新样本集并重新预注册**（全新清单、全新种子与**单独运行授权**）；**不得**改判据、**不得**重跑 `-v9` 冻结集合、**不得**把同一集合反复调参后称为独立验证。

仍有**一个未裁定事项**（`X2`，候选 B）与**一项可选只读动作**：

- **建议的唯一下一步（需用户单独授权，本篇不自行执行）**：先就 **`X2`（候选 B）** 作出裁定——它是当前唯一悬置的路线决策，且与「是否新开身份 / 新样本集」直接相关；
- 若选择先做只读诊断：可就 L4 的负差在 `-v9` 已落盘工件上做一轮**只读定向诊断**（不新增样本、不重跑、不改代码），把负配对差定位到人数 / 风格 / 对手族与尺度 / 牌力 / 街道分桶；该诊断结论**不得**用于修改任何判据或口径。
