# 58a M8：第四次验证阶段 `B` 的确定性补算回执（`J3` 第三轮）

> 日期：2026-09-23。
>
> 本文件接在 `docs/phase6/72-m8-rss-envelope-relaxation-record.md` 之后，沿用 `docs/30` §10.1 D6 的编号族约定。归档路径按 `docs/README.md` 的维护约定：本文件属第四次验证族，**共用族号 `58`**（先例 `55a`–`55h`、`56a`–`56i`、`57a`–`57m`），故取 **`58a`**。`docs/phase6/60`–`72` 为已落盘历史文档，**不重命名、不重编号**。
>
> 本文件记录 `J3` 第三轮：**阶段 `B` 的确定性补算**（`tests.mixed_bot_recheck`）的运行命令、实测读数与**双口径包络核验**。
>
> **本篇不给任何质量结论，也不做任何判据计算**：补算**不产生新对局、不产生新样本、不追加强制种子**；行为分区只做**结构与自洽核对**，L1–L5 的判定**留待单独进行的判定轮次**。

## 1. 授权原文与边界

用户裁定（原文照录）：

> 「A，允许新建工作树外文件、允许采后台运行。」

其指向即助手在上一轮提出的**唯一下一步**（选项 `A`，理由随该选项提出并随之选定，此处照录以免与用户原话混淆）：

> 「授权补算（`J3` 第三轮），按 §七 命令后台运行 + 轮询，输出落 `<v9>/runs/stage-b-recheck/`……包络 `wall ≤1800 s` / 整调用 `RSS ≤1.5 GiB` / 输出 `≤100 MiB`；触及任一上界即停止并如实报告、不自行扩容。」

| 项 | 内容 |
|---|---|
| 被授权 | 在 `-v9` 下**单轮**运行阶段 `B` 的确定性补算；**新建**工作树外目录 `<v9>/runs/stage-b-recheck/` 并写入补算报告；采**后台运行 + 轮询**（日志写 `/tmp` 暂存）；随后出独立回执 |
| 门禁 | 补算门禁**引用阶段 `B` 回执**（`<v9>/runs/stage-b/mixed-validation-stage-B.json`），非阶段 `A` |
| 明确排除 | 任何判据计算或质量结论；**阶段 `B` 的重跑**；任何代码改动；改判据 / 门槛 / 族集 / 机会分母 / 估值块 / 区间方法 / 包络数值；删除或改写任何既有产物；默认策略切换；推送 |
| 包络 | `wall ≤1800 s`；**峰值 RSS `≤1.5 GiB`（整调用口径）**（`docs/phase6/71` + `72`）；输出 `≤100 MiB`；单进程、不并行；触及任一上界即停止并如实报告，**不自行扩容** |
| 未消耗 | **未消耗**判据判定与签收的任何授权；`docs/37` §3.1 **未触发** |

## 2. 只读基线核验（补算前）

```text
$ git status --short --branch
## master...origin/master

$ git status --porcelain=v1 -uall | wc -l
0

$ git rev-parse HEAD
07f0623581d75ed558247d5c8fb41ae736960a75

$ git rev-parse origin/master
07f0623581d75ed558247d5c8fb41ae736960a75
```

工作区**零改动**，与 `origin/master` **`0 / 0`**。补算前逐字复核：

| 核验对象 | `sha256` | 结论 |
|---|---|---|
| `<v9>/input/frozen-fixture-manifest.json` | `499258c993d1e59d5c2f2c218b79f2944059d216e05e3c20998675c20704a7c6` | 未变 |
| `<v9>/runs/stage-a/mixed-validation-stage-A.json`（失败轮，保留） | `ffdd79f49dd0aa5f2c91fb4f27cf1ab8652ab5bef52b2e255d5e741c0ac764a6` | 未变 |
| `<v9>/runs/stage-a/mixed-observations-stage-A.json`（失败轮，保留） | `32d9d677fc75a2841da616deabf769af79aa35b48e67c9cccf43e36fb8851c66` | 未变 |
| `<v9>/runs/stage-a-retry/mixed-validation-stage-A.json` | `7e845cd00abaa2ca0a92e3057719cabe9b130dffab9fad4d7fcd7b616fe47d43` | 未变 |
| `<v9>/runs/stage-a-retry/mixed-observations-stage-A.json` | `5a26864327e79f9d77d2cb16d19979dbf733f1d6593c797619b6e707c7c02d79` | 未变 |
| `<v9>/runs/stage-b/mixed-validation-stage-B.json`（**门禁回执**） | `963451e2c0e4d98abda8b007a7ee7db66c3c853dd1fb9b3ccbc6a44da827cf1f` | 未变 |
| `<v9>/runs/stage-b/mixed-observations-stage-B.json` | `33920ec5a9088e023b5a17296fa75ce0801cb00822ae94e8bcb0c92a47dc9dd6` | 未变 |

补算前 `<v9>` 内文件为 **`7`** 个；目标目录 `<v9>/runs/stage-b-recheck` **不存在**（`ls -d` 返回 `No such file or directory`）。既有八套证据目录文件数 `5 / 4 / 4 / 4 / 4 / 5 / 7 / 7`（含 v6 失败阶段 A `86e84fed…`，**原样保留**）；v6 / v7 / v8 全部工件 sha 与 `docs/phase6/55c`–`55f`、`56c`–`56g`、`57f`–`57j` 登记值**逐条吻合**。

**入口核验（只读）**：`backend/tests/mixed_bot_recheck.py` 的 CLI 与门禁（`--allow-recheck` 缺省拒绝；要求 `status=completed`；阶段 ∈ `{A,B}`；回执 `strategy_id` 须等于清单 `strategy_id`；重算行为指标须与回执一致；目标文件存在即拒写且**不写报告**）与 `docs/phase6/58` §13 的补算约定相符。

## 3. 运行命令与工件

```text
$ cd backend && nohup /usr/bin/time -l uv run python -m tests.mixed_bot_recheck \
    /Users/bryanylliu/holdem-mixed-bot-validation-v9/input/frozen-fixture-manifest.json \
    /Users/bryanylliu/holdem-mixed-bot-validation-v9/runs/stage-b/mixed-validation-stage-B.json \
    /Users/bryanylliu/holdem-mixed-bot-validation-v9/runs/stage-b-recheck --allow-recheck \
    > /tmp/v9-stage-b-recheck.stdout.log 2> /tmp/v9-stage-b-recheck.log &
```

单进程、不并行；`/usr/bin/time -l` 的进程级读数取自 `/tmp` 暂存日志（**不是**证据工件，仅用于交叉核对整调用口径）。

| 工件 | `sha256` | 字节数 |
|---|---|---|
| `<v9>/input/frozen-fixture-manifest.json`（**未改动**） | `499258c993d1e59d5c2f2c218b79f2944059d216e05e3c20998675c20704a7c6` | `215,371` |
| `<v9>/runs/stage-a/**` 与 `stage-a-retry/**`（**四份均未改动**） | 见 §2 | — |
| `<v9>/runs/stage-b/mixed-validation-stage-B.json`（**未改动**） | `963451e2c0e4d98abda8b007a7ee7db66c3c853dd1fb9b3ccbc6a44da827cf1f` | `20,686,354` |
| `<v9>/runs/stage-b/mixed-observations-stage-B.json`（**未改动**） | `33920ec5a9088e023b5a17296fa75ce0801cb00822ae94e8bcb0c92a47dc9dd6` | `45,901,341` |
| `<v9>/runs/stage-b-recheck/mixed-recheck-stage-B.json`（**本轮新增**） | `954ad61d21c3bac8ba1d856d8616ab2895c58157f6105e1993c5eb439d2268b9` | `324,436` |

- **首跑成功**：补算**一次通过**，未发生失败重跑；`<v9>` 文件数由 `7` 变为 **`8`**，既有工件**零改写**；
- 落盘工件与同一调用的 stdout 报告**逐字一致**（stdout = 工件内容 + 单个结尾换行，已校验）；stdout 与 `/tmp` 日志中**无** `traceback` / `MixedRecheckError` / 错误行，入口正常返回并打印完整报告；
- 本轮在 `-v9` 内新增**一个**目录、**一个**文件；未触及既有八套证据目录。

## 4. 实测读数

### 4.1 身份与结论字段

| 项 | 读数 |
|---|---|
| `schema_version` / `source_stage` / `status` | `mixed-recheck.v1` / `B` / `completed` |
| `strategy_id` | `mixed-local@7` |
| `config_digest` | `fd7433e35fc8260d03635774b34fbdc419ad5a04ba0448916b036e385a546665` |
| `manifest_digest` | `319cbb967d66dabdd795c19d60ab91962cd9397926ea71349583d855edc6a918` |
| `code_identity` | `ae3f74930f022a69a4b64a7fcb85188c47384a74`（**冻结时的 HEAD**，见 §7） |
| `environment` | `macOS-26.6.2-arm64-arm-64bit-Mach-O / python 3.13.8` |

身份与 `docs/phase6/67` / `69` / `70` 逐字一致：`config_digest` 与 `manifest_digest` 均未变。

### 4.2 一致性核对（回执自带字段）

| 项 | 读数 |
|---|---|
| `consistency.source_receipt` | `<v9>/runs/stage-b/mixed-validation-stage-B.json` |
| `consistency.source_receipt_sha256` | `963451e2c0e4d98abda8b007a7ee7db66c3c853dd1fb9b3ccbc6a44da827cf1f`（**与阶段 `B` 报告逐字一致**） |
| `consistency.hands_compared` | `33,792`（与阶段 `B` 的计划 / 完成对抗手数一致） |
| `consistency.net_chips_exact` | `true`（逐手净筹码按排期顺序**完全一致**） |
| `consistency.behavior_exact` | `true`（行为指标**完全一致**） |

- **任何差异都会中止且不写报告**：两处 `true` 是「确定性重放通过」的**唯一证据形式**；本轮回执工件的产出本身也意味着未触发中止路径；
- **观测工件一致性核对**：本轮补算旁**存在**观测工件（`33920ec5…9dd6`，`45,901,341 B`），因此该项核对**参与并已通过**。该结论**不带报告字段标记**；**只有工件存在时该结论才成立**，后续引用必须连同「工件存在」一并陈述。

### 4.3 结构自洽（**不做判据计算**）

| 项 | 读数 |
|---|---|
| `aggregates` 行数 | `384` |
| `worst_opponents` 条目数 | `6` |
| `behavior.direct_distributions` | `1,125` |
| `behavior.style_js_distances` | `3` |
| `behavior.categories` | `48` |
| `limitations` 条目数 | `4`（按 `16` 块沿用已登记文本，原样落盘、未改写） |

- 上表只证明**分区齐全、条目数自洽**；三分域、两个机会域与机会分母**均未在本轮触碰**，所有取值**未解读**，**不得**作为判定输入。

### 4.4 资源与包络（**双口径并列**，包络为人工比对，代码不据此改 `status`）

| 口径 | wall | 峰值 RSS | 输出 |
|---|---|---|---|
| 模块自记 `resources`（**下界**） | `650.97 s` | `936,427,520 B`（`893.05 MiB`） | `324,436 B` |
| **整个调用的最大常驻集**（`/usr/bin/time -l`，**判定口径**） | `651.81 s` `real`（`user 597.44 s` / `sys 15.55 s`） | **`963,051,520 B`（`918.44 MiB`）** | 同上 |

| 包络项 | 上界（`docs/phase6/71` + `72`） | 整调用实测 | 余量 | 结论 |
|---|---|---|---|---|
| wall | `≤1800 s` | `651.81 s` | 约 `63.8%` | **内** |
| 峰值 RSS | **`≤1.5 GiB`（`1,610,612,736 B`）** | `963,051,520 B` | `617.56 MiB`（约 `38.3%`） | **内** |
| 输出 | `≤100 MiB`（`104,857,600 B`） | `324,436 B`（≈`316.8 KiB`） | 远未触及 | **内** |
| 并发 | 单进程、不并行 | `single_process=true`、`parallel=false` | — | 满足 |

- 另记：`peak memory footprint = 10,944,968 B`（`/usr/bin/time -l` 的另一计量，非 RSS）；
- **包络未触发，未中止运行**：本轮全程在判定上界之内，无需触发「触及即停」路径；
- **不得**只报其中一种口径：整调用口径是**判定口径**，模块自记值是**下界**，两者**必须并列**。

**与冻结清单文本的既有差异（必须如实披露，沿用 `docs/phase6/72` §4）**：`-v9` 冻结清单内的 `resource_envelope` 文本仍写死旧上界 `1 GiB`（`1073741824` 字节），该清单是冻结产物、**不得改写**（`sha256` 仍为 `499258c9…`）；本轮的**判定上界取 `1.5 GiB`**。二者之间的差异是**已披露的既有差异**，本轮不改变清单内容，也**不得**声称清单已更新。

**记账（非重新验收）**：本轮整调用读数 `918.44 MiB` **同时低于**旧上界 `1 GiB`。这只是**本轮读数的算术事实**，**不构成**对阶段 `B`（其整调用 `1212.63 MiB` 已超旧上界、事实**照实保留**）的重新验收，**不构成**「包络一向足够」的表述，也**不构成**任何容量承诺或外推依据。

### 4.5 与 v8 同类补算的**机械量**对照（仅供成本参照，**不是质量比较**）

| 项 | v8 阶段 `B` 补算（`docs/57j`） | 本轮（v9 阶段 `B` 补算） | 变化 |
|---|---|---|---|
| wall（自记） | `456.751 s` | `650.969 s` | **`+42.5%`** |
| 峰值 RSS（自记） | `958.73 MiB` | `893.05 MiB` | **`-6.85%`** |
| 峰值 RSS（整调用） | `980.84 MiB`（`1,028,489,216 B`，`docs/57j` §4.4） | `918.44 MiB` | **`-6.4%`** |
| 输出 | `324,904 B` | `324,436 B` | `-0.14%` |

**必须一并声明**：

1. 上表是**机械量**（耗时、内存、体积）对照，**不得**与任何**质量**读数并列比较（不同节点集、不同种子集、不同策略身份）；
2. 本轮的 wall 上升与 RSS 下降**均未归因**，本文件**不据此推断趋势**，也**不据此**为任何后续规模承诺耗时或内存；
3. v8 的「峰值 RSS」两种口径**也都是同一取样结构**的产物，其是否超包络**只能按其记录时的口径理解**；历史轮次的整调用口径读数**未完整记录**，只能标注为**未知**（`docs/phase6/71` §4）。

## 5. 峰值 RSS 口径的观察（**不是归因结论**）

`docs/phase6/70` §7 与 `docs/phase6/71` §5 指出：模块自记 `peak_rss_bytes` 的取样时点早于大对象序列化，因此**是下界**；并据阶段 `B` 的载荷（`18,000` 行报告 × 最多 `3` 轮 + `45,901,341 B` 观测）**推演**补算的真实峰值将「接近其自记值」。

本轮实测与该推演**同向**：

| 轮次 | 自记峰值 | 整调用峰值 | 比值 |
|---|---|---|---|
| 阶段 `B`（`docs/phase6/70`） | `511.38 MiB` | `1212.63 MiB` | **`2.37×`** |
| **本轮补算** | `893.05 MiB` | `918.44 MiB` | **`1.028×`** |

**必须一并声明**：

1. 上表只是**两条读数**的并列；比值的差异**与序列化载荷规模的方向一致**，但本文件**不声称已完成归因**（`docs/phase6/71` §5 的推演**本身是推演**，不是读数）；
2. 本轮整调用峰值 `918.44 MiB` **低于** `docs/phase6/71` §5 推演所提示的「可能贴着 `1 GiB` 甚至越过」——这**不构成**对推演方法的证伪或校准，也**不构成**对任何其他规模、其他样本集或其他身份的承诺；
3. **不得**把模块自记值说成真实峰值；**不得**只报一种口径。

## 6. 估时对照（估算 vs 实测）

| 项 | 运行前估算（`docs/phase6/58` §13.2 与 `docs/phase6/70` §9、`docs/phase6/72` §8） | 实测 | 偏差 |
|---|---|---|---|
| 补算 wall | `450–650 s`（v8 同类自记 `456.751 s`，**未归因**） | `650.969 s`（自记）/ `651.81 s`（整调用） | 落在区间**上端**；对 v8 同类推值 **`+42.5%`** |
| 补算峰值 RSS | **无可靠外推依据**（v8 自记 `958.73 MiB`，余量 `6.37%`，**未归因**） | `918.44 MiB`（整调用） | **低于** v8 同类读数 |
| 补算输出 | `≈317 KiB` 量级 | `324,436 B` | 同量级 |

**本轮结论（含估算偏差，如实记账）**：

- 估时**区间成立**（wall 落在给定区间内），但点估**明显偏低**：`docs/phase6/58` §13.2 已自陈「估算不是读数」「`docs/54h` 有过 `41%` 偏差先例」「峰值 RSS 从未有过可用估算」——本轮偏差**不构成**估算方法已被校准的结论；
- **不得**据本轮实测为判定轮次或任何后续运行承诺耗时或内存。

## 7. `code_identity` 差异披露（必须逐次如实标注）

| 项 | 值 |
|---|---|
| 清单与补算报告中的 `code_identity` | `ae3f74930f022a69a4b64a7fcb85188c47384a74`（**冻结时的 HEAD**，`docs:`) |
| 本文件提交前实际执行代码的 HEAD | `07f0623581d75ed558247d5c8fb41ae736960a75`（本文件的提交会**再移动 HEAD**） |

⇒ 二者**不同**。这是「先冻结、后回执」的固有结果，**不是**清单或本轮产物的缺陷。**不得**把本轮补算回执表述为「由 `07f06235…` 代码产生」，也**不得**说成由清单内的 `ae3f7493…` 产生（实际执行代码是该 HEAD 之后的内容）。

## 8. 契约、API 与默认策略

| 对象 | 状态 |
|---|---|
| 公开 API 字段 / 数据库列 / `history_json` | **零改动** |
| `mixed-validation.v2` / `mixed-observations.v1` / `mixed-recheck.v1` | **零改动**（本轮无代码改动，无新增字段） |
| `MixedFixtureManifest` / `-v9` 冻结清单 | **零改动**（`sha256` 逐字未变；**未加字段**） |
| 策略身份 | **未新增**（仍为 `mixed-local@7`） |
| 新增清单 | **否** |
| 包络数值 | **未改**（`1.5 GiB` 为 `docs/phase6/72` 已批准值；清单文本仍为 `1 GiB`，差异已在 §4.4 披露） |
| `docs/37` §3.1 | **不触发** |
| 默认策略 | **仍为 `heuristic@1`**（未获质量签收，必须保持） |
| v1–v8 外部证据 / `-v9` 既有六份工件 | **零触碰** |

## 9. 未做事项与仍未证

- **未做**：任何判据计算；任何质量结论；**阶段 `B` 的重跑**；任何代码、判据、门槛、族集、机会分母、估值块、区间方法、包络数值的改动；删除或改写任何既有产物；默认策略切换；推送；
- **仍未证**：`-v9` 上 **L1–L5 的任何判据读数**与是否达标（覆盖率、类型熵、金额熵、锚点份额、三对 JS 距离、域一 / 域二差值、跨人数净值**全部未算**）；第七版在第五套样本集上的质量表现；
- **补算的边界**：**不产生新对局、不产生新样本、不追加强制种子**；`net_chips_exact` / `behavior_exact` 是**确定性重放**结果，**不得**当作新证据、新样本或质量证据；
- **成本方面**：补算路径**仍无自己的声明包络**；本轮整调用 `918.44 MiB` 是**读数**，**不得**外推；
- **未消耗**：本轮未消耗判据判定与签收的任何授权。

## 10. 不得声称

- 不得把本次补算的 `completed`、`net_chips_exact=true`、`behavior_exact=true` 说成**质量证据**、判据达标或「验证通过」；不得把「补算通过」说成阶段 `B` 已被独立复核。
- 不得把确定性补算当作**新样本**或**新证据**；不得从本回执或补算工件中顺带计算任何判据读数并作为判定输入。
- 不得在未同时声明「观测工件存在」的前提下声称观测工件已被确定性核对（§4.2）。
- 不得把 `2.37×` / `1.028×` 的比值说成已**归因**，也不得据此外推任何规模（§5）。
- 不得把本轮读数解释为「包络已被验证充足」：`38.3%` 余量是**本轮读数的算术后果**，**不构成**容量承诺。
- 不得把本次估时对照说成「估算已被校准」；不得为判定轮次或任何后续运行承诺耗时或内存（§6）。
- 不得把本轮回执说成由 `07f06235…` 或 `ae3f7493…` 代码产生（§7）。
- 不得把 §4.5 的机械量对照说成质量比较；不得把第五套样本集与 v6 / v7 / v8 的**质量**读数并列比较。
- 不得把 `docs/phase6/58a` 说成对 `docs/53c` / `55g` / `56h` / `57k` 任一签收结论的反转、修正或追认。
- 不得据此修改 `docs/` 下任何已归档文档与任何已落盘工件；不得重跑本次补算，也不得重跑 `-v9` 已完成的阶段 `A` / 阶段 `B`。

## 11. 本篇唯一下一步

`J3` 的机械步骤（阶段 `A` → 阶段 `B` → 阶段 `B` 补算）至此**全部结束**；`-v9` 现为 **`8`** 个文件。

按 `docs/phase6/62` §12 与 `docs/phase6/66` 的冻结判据，**下一步是判据判定与用户签收**，且**必须单独进行**：

- 判定**必须按 `docs/phase6/66` 冻结的判据与口径**执行（L1–L5、六项门槛、机会分母、两个机会域、区间方法、估值块），**取值一字不改**；
- 判定所需读数来源（阶段 `B` 报告与观测、阶段 `B` 补算、阶段 `A` 与重试）**均已落盘**；
- 判定**不是**本轮补算的一部分，本轮**未**做任何判据计算；若判据不满足，按既有处置为「**质量未获支持**」、默认策略**保持 `heuristic@1`**，且**不得**改判据、不得重跑同一集合；
- 判定之后的 `X2` 仍需**单独裁定**。

**该动作尚未授权**。
