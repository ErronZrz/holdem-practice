# 57h M8：第四次验证阶段 A 的确定性补算回执

> 日期：2026-09-23。
>
> 本文件接在 `docs/57g` 之后，沿用 `docs/30` §10.1 D6 的编号族约定。`docs/20` 至 `docs/57g` 未被改写。
>
> 本文件记录：`J3` 第 2 步（阶段 `A` 的确定性补算）的运行命令、实测读数与估时对照。
>
> **本篇不给出任何质量结论，也不做任何判据计算**：补算**不产生新对局、不产生新样本**；行为分区只做**结构与自洽核对**，L3 任何指标的读数**留待判定轮次**。

## 1. 授权原文与边界

用户答复原文：

> 「A. 授权阶段 A 的确定性补算（只跑 `--allow-recheck` 一轮（估 wall 4–5 s / RSS ≈140 MiB 量级 / 输出 ≈78 KB），写完回执；不动判据、不动清单、不重跑阶段 A、不提前做阶段 B）」

| 项 | 内容 |
|---|---|
| 被授权 | 单独一轮运行阶段 `A` 的确定性补算（清单与阶段 `A` 回执均已冻结） |
| 明确排除 | 阶段 `B`；改判据 / 门槛 / 机会分母 / 估值块 / 区间方法；改清单或种子；重跑阶段 `A`；重跑 v6 / v7 冻结集合；默认切换 |
| 包络 | 沿用既有 `≤1 GiB` 包络并保留「未归因」标注（`R4(a)`：**补声明、不改数值**）；本路径**仍无自己的声明包络** |
| 未消耗 | 未消耗阶段 `B` 及其补算的授权；`docs/37` §3.1 **未触发** |

## 2. 只读基线核验（补算前）

```text
$ git status --short --branch
## master...origin/master

$ git status --porcelain=v1 -uall | wc -l
0

$ git rev-parse HEAD
79d6b2d4b77d301cc3a198e8bc1fb32c1df47cbb
（与 origin/master 逐字一致）

$ find /Users/bryanylliu/holdem-mixed-bot-validation-v8 -type f | wc -l
3（仅清单 + 阶段 A 报告 + 阶段 A 观测）
```

工作区**零改动**。补算前逐字复核：v8 三工件 sha256 = `484a6ce8…a225` / `b9642098…3319` / `f776ab7c…8c0a`；v7 七份前 8 位 = `a68e9776 / 980a62f6 / 464daefe / dd89e5fb / f2b83bc1 / 04a5acb2 / 215835e5`；v6 五份前 8 位 = `0cb1dad4 / 86e84fed / 332607ca / 7ad4318b / 5597d221`（含**失败阶段 A `86e84fed`，原样保留**）——**均与移交卡片一致**。

## 3. 运行命令与工件

```text
$ uv run python -m tests.mixed_bot_recheck \
    /Users/bryanylliu/holdem-mixed-bot-validation-v8/input/frozen-fixture-manifest.json \
    /Users/bryanylliu/holdem-mixed-bot-validation-v8/runs/stage-a/mixed-validation-stage-A.json \
    /Users/bryanylliu/holdem-mixed-bot-validation-v8/runs/stage-a-recheck \
    --allow-recheck
（退出码 0；单进程不并行）
```

| 工件 | sha256 | 大小 |
|---|---|---|
| `<v8>/input/frozen-fixture-manifest.json`（**未改动**） | `484a6ce83687be436a2676081c6f9c35a7deed5c4e867b9bb95eec868141a225` | `215,371 B` |
| `<v8>/runs/stage-a/mixed-validation-stage-A.json`（**未改动**） | `b96420985ba2e0a16e76c892f4e7895ec62f2ac384b30cc0d0379b0601ce3319` | `10,120,702 B` |
| `<v8>/runs/stage-a/mixed-observations-stage-A.json`（**未改动**） | `f776ab7cce242a5f191fd78973c02d2d0648d9f6e65ee4e0637004e22eac8c0a` | `179,574 B` |
| `<v8>/runs/stage-a-recheck/mixed-recheck-stage-A.json`（**本轮新增**） | `974a019c6f2dce9f141b955b0133ce829e176d882db408dd6fbe33128ce411b5` | `77,030 B` |

- **首跑成功**：补算**一次通过**，未发生失败重跑；`<v8>` 文件数由 `3` 变为 `4`，源工件**零改写**。
- **不覆盖**：输出目录此前不存在（`stage-a-recheck` 于 `14:42` 首次创建）；入口「只创建、不覆盖」的纪律在本次未受检验（无重复落盘场景）。

## 4. 实测读数

### 4.1 身份与结论字段

| 项 | 读数 |
|---|---|
| `schema_version` / `source_stage` / `status` | `mixed-recheck.v1` / `A` / `completed` |
| `strategy_id` | `mixed-local@6` |
| `config_digest` | `b6678f65069c19779f683bd6a8df31f7bc3567cf813654d2758278c13151b4a1` |
| `manifest_digest` | `0a1b1ea8985dfbd0f9d5ddeeedbca53e348c97ca0f5c31d9f2273e87f912e8f6`（与冻结清单一致） |
| `code_identity` | `22e669a879acbf862aa8eb8953ca6fa90c29bf2c`（**冻结时的预承诺值**，见 §6） |
| `environment` | `macOS-26.6.2-arm64-arm-64bit-Mach-O / python 3.13.8` |

### 4.2 一致性核对（回执自带字段）

| 项 | 读数 |
|---|---|
| `source_receipt` | `<v8>/runs/stage-a/mixed-validation-stage-A.json` |
| `source_receipt_sha256` | `b96420985ba2e0a16e76c892f4e7895ec62f2ac384b30cc0d0379b0601ce3319`（**与阶段 A 报告逐字一致**） |
| `hands_compared` | `128`（与阶段 A 的对抗对照手数一致） |
| `net_chips_exact` | `true`（逐手净筹码按排期顺序**完全一致**） |
| `behavior_exact` | `true`（行为指标**完全一致**） |

- **任何差异都会中止且不写报告**：`true` 两项目前即为「核对通过」的唯一证据形式；本轮回执文件的产出本身也意味着未触发中止路径。
- **观测工件一致性核对**：本轮回执旁**存在**观测工件（`f776ab7c…`），因此该项核对**参与并已通过**。该结论**不带报告字段标记**（口径不新增字段）；**只有工件存在时该结论才成立**，后续引用时必须连同「工件存在」一并陈述。

### 4.3 结构自洽（**不做判据计算**）

| 项 | 读数 |
|---|---|
| `aggregates` 行数 | `128` |
| `worst_opponents` 条目数 | `2` |
| `behavior.direct_distributions` 条目数 | `1,125` |
| `behavior.style_js_distances` 条目数 | `3` |
| `behavior.categories` 条目数 | `48` |
| `limitations` 条目数 | `4`（原样落盘，未改写） |

- 上表只证明**分区齐全、条目数自洽**，**不构成**任何 L3 / L4 判据读数；三分域与机会分母**均未在本轮触碰**。
- `direct_distributions` / `style_js_distances` / `categories` 的**取值**未在本篇解读；风格可分度与逐人数不劣化的判定**留待判定轮次**。

### 4.4 资源（包络为**人工比对**，代码不据此改 `status`）

| 项 | 读数 | 对照 | 余量 |
|---|---|---|---|
| wall | **`4.100 s`** | 既有包络 ≤1800 s | 远未触及 |
| CPU | `3.975 s` | — | — |
| 峰值 RSS | **`142.23 MiB`**（`149,143,552 B`） | `≤1 GiB`（`R4(a)` 的**补声明**，未改数值） | 约 `86%` 余量 |
| 输出 | `77,030 B`（≈`75.2 KiB`） | ≤100 MiB | 远未触及 |
| 并发 | `single_process=true`、`parallel=false` | 单进程不并行 | 满足 |

- 外部 `/usr/bin/time -l` 交叉核对：`4.63 real / 4.14 user`、`maximum resident set size = 149,143,552`——**与进程内自测的 RSS 逐字节相同**，`wall` 差值来自解释器与运行时启动开销。
- **补算路径仍无自己的声明包络**：本轮的 `≤1 GiB` 只是沿用 `R4(a)` 的既有取值，**不是**新包络，也**不得**外推到任何其他规模。
- **未归因项（如实记账）**：补算峰值 RSS 相对阶段 `A` 运行的 `73.22 MiB` 为 **`1.943×`**；v7 同路径的同类比值为 `1.926×`（`146,898,944 / 76,251,136`）。两次比值接近，但**差异仍未有解释**，本文件**不声称已归因**，也**不**据此推出趋势或任何外推依据。

## 5. 估时对照（估算 vs 实测）

运行前给出的估时（`R3`）：wall `4–5 s`、峰值 RSS `≈140 MiB` 量级（依据 v7 补算/运行的 `1.93×` 比值乘本轮 `73.22 MiB`，并声明**无声明包络、不构成外推保证**）、输出 `≈78 KB`。

| 项 | 运行前估算 | 实测 | 偏差 |
|---|---|---|---|
| 补算 wall | `4–5 s` | `4.100 s` | 落在区间内 |
| 补算峰值 RSS | `≈140 MiB` 量级 | `142.23 MiB` | **`+1.6%`** |
| 补算输出 | `≈78 KB` | `77,030 B` | **`-1.2%`** |

**资源读数对照（仅成本口径，不涉判据）**：本轮补算 wall `4.100 s`、峰值 RSS `142.23 MiB`、输出 `77,030 B`；v7 同路径为 `4.231 s` / `140.09 MiB` / `77,244 B`。

**本轮结论**：三项均落在估算区间或量级内，但**不得**据此把估算表述为读数，也**不得**据此为阶段 `B` 承诺耗时或内存——`docs/54h` 的「估算偏差 `41%`」先例、`docs/56f` 的「补算 RSS 不得外推」、以及 v7 阶段 `B` 补算 `917.38 MiB`（**未归因**）的现状**均未改变**。

## 6. 身份差异（必须持续披露）

| 项 | 值 |
|---|---|
| 回执中的 `code_identity`（**冻结时的 HEAD**） | `22e669a879acbf862aa8eb8953ca6fa90c29bf2c`（`docs:` 提交） |
| `@6` 与 `-iqv3` 的**实际实现提交** | `4fbb9833425475c593916969ba0605fc3525a2c9`（`feat:`）、`720ee2d57d69e5e1af75013456331e6413484ac6`（`feat:`） |
| 差异内容 | 冻结时的 HEAD 与实际执行代码之间**只有文档差异**，代码内容与 `720ee2d…` 相同 |

**不得**把本次补算回执表述为「由 `22e669a` 代码产生」。

## 7. 契约、API 与默认策略

| 对象 | 状态 |
|---|---|
| 公开 API 字段 / 数据库列 / `history_json` | **零改动** |
| `mixed-validation.v2` / `mixed-observations.v1` / `mixed-recheck.v1` | **零改动**（本次无代码改动，无新增字段） |
| `MixedFixtureManifest` / 冻结清单 | **零改动**（摘要逐字未变） |
| 策略身份 | **未新增**（仍为 `mixed-local@6`） |
| 新增清单 | **否** |
| 训练记录 / artifact schema | **零改动** |
| `docs/37` §3.1 | **不触发** |
| 默认策略 | **仍为 `heuristic@1`**（未获质量签收，必须保持） |
| v1–v7 外部证据 / 封存 campaign 工件 | **零触碰** |

## 8. 未做事项与仍未证

- **未做**：阶段 `B`；阶段 `B` 的补算；`A0`；**任何判据计算**；任何质量结论；完整标定；默认切换。
- **仍未证**：L1–L5 的**任何判据读数**（阶段 `B` 为主来源，且判据计算属判定轮次）；`@6` 的强度、可剥削性、抗针对性；域二与 L4 是否改善；根因 C 是否成立；阶段 `B` 及其补算的 wall / RSS / 输出体积。
- **补算**：**不产生新对局、不产生新样本、不追加强制种子**；其 `net_chips_exact` / `behavior_exact` 是**确定性重放**结果，**不得**当作新证据、新样本或质量证据。
- **抗针对与剥削性检验仍未做**：探针族只是机械对照，**不是** best response；`shared-board` 的读数**只能是**「读数存在、可利用性未检验」。
- **补算路径无声明包络**：`R4(a)` 只是**补声明**，不得外推。
- **未消耗**：本轮未消耗阶段 `B` 及其补算的授权；全程只获**包络级**预算批准。

## 9. 不得声称

- 不得把本次补算的 `completed`、`net_chips_exact=true`、`behavior_exact=true` 说成**质量证据**或判据达标。
- 不得把确定性补算当作**新样本**或**新证据**；不得把「补算通过」说成阶段 `A` 已被独立复核。
- 不得在未同时声明「观测工件存在」的前提下声称观测工件已被确定性核对（§4.2）。
- 不得把 `1.943×` / `1.926×` 的 RSS 比值说成已归因，也不得据此外推到阶段 `B` 或任何其他规模。
- 不得把本次估时对照说成「估算已被验证」；不得为阶段 `B` 承诺耗时或内存。
- 不得把本次回执说成由 `22e669a` 代码产生（§6）。
- 不得把 `docs/57e` 的锁定说成 v7 的预注册口径或质量改善。
- 不得据此修改 `docs/20` 至 `docs/57g` 与任何已落盘工件；不得重跑本次阶段 `A`、不得重跑 v6 / v7 冻结集合；不得改写 v6 的失败阶段 A 回执。

## 10. 本篇唯一下一步

`J3` 第 2 步（阶段 `A` 的确定性补算）已完成并如实记账。按 `docs/57f` §10 与 `docs/57b` §5.6 的顺序，**下一步是阶段 `B`**：

- 须**引用阶段 `A` 的 `completed` 回执**通过门禁（该回执 sha 为 `b9642098…3319`，本轮已复核未变）；
- 命令形态须显式带 `--family-set=iqv --emit-observations`，输出写入 `<v8>/runs/stage-b/`；
- 与补算**各自单独一轮**，不得合并；**运行前须先给实测支撑的估时（`R3`）**。

**估时注意**：阶段 `B` 的匹配手数与输出体积**远大于**阶段 `A`（v7 阶段 `B` wall `511.948 s`、峰值 RSS `493.22 MiB`、保留文件合计 `62.78 MiB`），且**不得**用本轮补算或阶段 `A` 的读数外推阶段 `B` 的成本。
