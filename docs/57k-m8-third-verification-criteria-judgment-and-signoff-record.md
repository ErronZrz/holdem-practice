# 57k M8：v8 独立质量验证的判据判定与签收回执

> 日期：2026-09-23。
>
> 本文件接在 `docs/57j` 之后，沿用 `docs/30` §10.1 D6 的编号族约定。`docs/20` 至 `docs/57j` 未被改写。
>
> 本文件记录：按 `docs/57e` 锁定的 L1–L5 判据与全部判定口径、在**已冻结样本**（v8）上作出的**判定读数**，以及由 D2（`docs/55` §10.4）绑定推出的**本次质量签收结论**。
>
> **措辞约束**：按 `docs/55` §7.5 第 1 条，判定只输出**解释性状态**，不使用「合格 / 不合格 / 通过 / 达标」。文中「满足 / 不满足」仅作为 D4（§10.6）与 D7（§10.7）原文所用的**绑定术语**出现。
>
> **本篇同时是披露文件**：它披露 (a) 本轮**无法复现** v7 公布的两处读数（L3 第 4 项的域聚合、L5 第 2 项的分桶分类），(b) 「方向稳定」在本样本上是**结构性必然**，(c) 第 3 项存在两种读法分歧。三处均逐条留痕（§4.4 / §4.5 / §7）。

## 1. 授权原文与边界

用户答复原文依次为：

> 「RSS 预算未来允许适当放宽。授权判据判定。」——授权本轮做判据判定；并对 RSS 预算给出一条**面向未来**的表态。

> 「完整版签收」（签收问项）与「授权」（提交问项）——选定签收原文，并授权**写本回执 + 单条 `docs:` 提交 + 推送**。

| 项 | 内容 |
|---|---|
| 被授权 | 按已锁定口径做判定计算；出具本判定与签收回执；提交并推送 |
| 计算口径 | 只读**已落盘工件的现有字段** + **内联确定性计算**；不新增文件、不改动代码、不重跑、不产生新样本 |
| 明确排除 | 修改任何判据、门槛、机会分母、估值块或区间方法；重跑任何阶段或补算；删除任何样本；默认策略的自主切换；任何包络数值的修改 |
| 保留 | v1–v8 全部工件**原样保留**；v6 失败阶段 A 回执 `86e84fed…` **未被触碰** |
| 未消耗 | 未消耗任何新的运行授权；`docs/37` §3.1 **未触发** |

**关于「RSS 预算未来允许适当放宽」（原话如实记账，范围严格限定）**：

- 本轮**未**据此修改任何门槛、包络数值或判据；`≤1 GiB` **维持原值**；
- 该表态**只对未来生效**，且**不得**追溯改写任何已落盘回执或其「未归因」标注（含 v8 阶段 `B` 补算余量 `6.37%`）；
- 若要落地，须由用户**另行显式批准**并单独成文记录适用规模；**不得**由本回执自行解释为已生效的包络变更。

## 2. 只读基线核验（判定前）

```text
$ git status --short --branch
## master...origin/master

$ git status --porcelain=v1 -uall | wc -l
0

$ git rev-parse HEAD
8f4b03ad695523f219ce99a939efbb2d1b2b0370

$ find /Users/bryanylliu/holdem-mixed-bot-validation-v8 -type f | wc -l
7
```

判定计算**开始前**工作区**零改动**；全部计算为**内联确定性计算**，未新增文件、未改动代码、**未重跑任何阶段**。本回执是判定**之后**的记录文件，其中不含计算代码。

判定期间**额外运行了一次**定向契约测试（`tests/test_info_boundary.py`，用于 L1 的信息边界项），这是本轮唯一的额外命令。

## 3. 判定输入工件（v8，共 7 个）

| 工件 | sha256 | 角色 |
|---|---|---|
| `input/frozen-fixture-manifest.json` | `484a6ce83687be436a2676081c6f9c35a7deed5c4e867b9bb95eec868141a225` | 身份与摘要基准、种子清单序 |
| `runs/stage-a/mixed-validation-stage-A.json` | `b96420985ba2e0a16e76c892f4e7895ec62f2ac384b30cc0d0379b0601ce3319` | L1 |
| `runs/stage-a/mixed-observations-stage-A.json` | `f776ab7cce242a5f191fd78973c02d2d0648d9f6e65ee4e0637004e22eac8c0a` | 备用 |
| `runs/stage-a-recheck/mixed-recheck-stage-A.json` | `974a019c6f2dce9f141b955b0133ce829e176d882db408dd6fbe33128ce411b5` | L1（`128` 手） |
| `runs/stage-b/mixed-validation-stage-B.json` | `6d3838fb09d9b419bb4d9f16b510a692989428c2064c7ef4716fa8b682fff27b` | **主来源** |
| `runs/stage-b/mixed-observations-stage-B.json` | `087e0d024658c7e75038977f82c5ab392754c3688fe5f56cce3990fc09425132` | L5 第 2 项 |
| `runs/stage-b-recheck/mixed-recheck-stage-B.json` | `a3c945906ba87de675b1c463540aac929ba559d05d397160422582f8088dda54` | L1（`33,792` 手） |

身份：`strategy_id=mixed-local@6`、`config_digest=b6678f65069c19779f683bd6a8df31f7bc3567cf813654d2758278c13151b4a1`、`manifest_digest=0a1b1ea8985dfbd0f9d5ddeeedbca53e348c97ca0f5c31d9f2273e87f912e8f6`。

## 4. 判定计算口径与固定时点

### 4.1 读数前已锁定的口径（强）

| 编号 | 口径 | 锁定位置 | 时点 |
|---|---|---|---|
| X1 | L4 区间唯一方法：块均值双侧 95% t 区间，`df=S−1`，`S=16` ⇒ `t(0.975,15)=2.131`；**强制并列**块均值/块标准差/块极值/逐人数分解；两口径不一致判**未满足** | `docs/56a` §6 | **读数之前** |
| X2 | L3 第 1/2/3/4 项的固定估值种子块 = **清单中的第 1 个主种子块**；第 5 项按全部 `16` 块 | `docs/56b` §7 | **读数之前** |

**本轮的具体取值**：第 1/2/3/4 项的估值块 = 第四套清单的第 `1` 个主种子块 **`2992123152`**（**清单序**，不是报告行序的最小值——报告行按数值排序时首行为 `398151747`，用错会取到错误的块）。

### 4.2 `X3`–`X9` 的来源与固定时点（必须一并声明）

`X3`–`X9` 由 `docs/57e` §9 在本轮**任何读数之前**成文并锁定，但其**取值来源**是 `docs/56h` §4.2 的**事后固定**（固定时点在 v7 读数之后）。因此：

- 就**本轮**而言 `X3`–`X9` 同为**事前锁定**；
- 但**不得**把它们表述为「v7 的预注册口径」或「读数前锁定」；`docs/56h` §4.2 的时点披露**原样有效**；
- 本节的锁定**不是**对 `docs/56h` 的追溯改写，也**不是**质量改善。

### 4.3 口径自洽核验：内联实现**逐行复现**了 v7 已公布读数

为证明本轮实现在原轮同口径，先用 v7 工件复算下列读数：

| 项 | v7 已公布 | 本轮实现复算 | 结果 |
|---|---|---|---|
| L3 第 1 项整体 / 分域 / 节点聚合 | `39.82%` / `56.38%` / `30.69%` / `0.00%` / `50.40%` | `448/1,125` / `274/486` / `174/567` / `0/72` / `63/125` | **逐位一致** |
| L3 第 2 项平均类型熵 | `0.3181` / `0.4221` / `0.3838` | 同 | **逐位一致** |
| L3 第 3 项锚点 / 金额熵 | `237/237`；`1.3999`（`1.3610`/`1.5395`/`1.2955`） | 同 | **逐位一致** |
| L4 逐人数（`8` 行全部字段） | 均值 / t95 下界 / 块 min / 块 max | 同 | **逐行一致** |
| L5 第 2 项逐族短/长/差（`8` 行） | 如 `-189.00 / -788.39 / +599.39` | 同 | **逐行一致** |

**由此确定的实现口径（本轮沿用）**：

- 第 1 项「实质动作混合」= 分布中**至少两个动作的份额各 ≥10%**（次高份额 ≥10%）；
- 节点聚合读数 = 把该节点 `9` 个（风格 × 手模式）分布的 **units 相加后再套同一规则**；
- 第 2 项 = 各风格 `375` 个分布 `action_entropy` 的算术均值；
- 第 3 项单元 = `scale_entropy` 非 `None` 的分布（条件于已选 `BET`/`RAISE`）；「锚点」= 同额合并后份额 ≥`15%` 的档数；
- 第 5 项短段 = 每（族 × 风格）内按（**清单种子序**、人数、轮换）排序的前 `20` 对。

### 4.4 两处**无法复现**的已公布读数（必须披露）

**（i）L3 第 4 项的两个机会域聚合口径无法复原。**

- v7 已公布：域一 `20.22` / `33.91` / `13.69` pp；域二 `0.35` / `0.03` / `0.38` pp；
- 本轮在 **v7 工件**上用四种自然聚合（逐行、逐节点、按风格池化、按节点 × 风格池化）**全部**得到域一 `30.71` / `54.92` / `24.22` pp、域二 `0.62` / `0.25` / `0.37` pp；
- 进一步穷举：类别子集（含 `2`–`5` 个类别、`2^7`/`2^8` 节点子集）、逐块扫描、逐手模式拆分、`≥10%`/`≥15%` 两类份额口径、`fold`/`call`/`bet+raise` 等多种动作集，**均无一组与 v7 公布值吻合**；
- **结论**：该两行数字**不可由工件现有字段复现**，其实现细节**已不可考**。

**本轮的处理与鲁棒性检验**：按 `docs/57e` §7.2 的两个机会域定义与字段直读给出 v8 读数（§7），并做方向鲁棒性检验——

| 口径 | 域二差值（三对，pp） | 是否可能有任一对 ≥10 pp |
|---|---|---|
| 本轮直读 | `4.51` / `1.66` / `2.85` | **否** |
| v7 公布口径（原样引用） | `0.35` / `0.03` / `0.38` | **否** |

⇒ 第 4 项的**判定方向不依赖该歧义**：域二在两种口径下都远小于 `10` 个百分点。

**（ii）L5 第 2 项的分桶分类口径无法复原。**

- v7 已公布：有面对 `bet/raise` 的配对仅 `1,345 / 16,896`（`7.96%`），并给出尺度 / 牌力 / 街道三套分桶；
- 本轮在 **v7 工件**上按多种实现（首个 / 末个面对事件；`mixed-focus` / `legacy-focus` / 两臂一致）得「有面对」`5,366`–`6,000` 量级（约 `32%`），**无一组复现 `1,345`**；
- **结论**：该分桶分类**已不可复原**。本项属**报告层、无数值门槛**，故**不改变任何判据判定**，但**必须**记为**口径缺陷**。

## 5. L1 机制与正确性（必要条件）

| 项 | 读数 | 解释性状态 |
|---|---|---|
| 违规数 | 阶段 A `violations=[]`、阶段 B `violations=[]` | 读数 `0` |
| 截断数 | 阶段 B `truncated_hands=0`；两级补算 `aggregates` 截断合计 `0` | 读数 `0` |
| 摘要一致 | `config_digest` / `manifest_digest` 在阶段 A、阶段 B 与两级补算**四处一致**；补算不重算摘要 | 读数一致 |
| 随机可重放 | 阶段 A 补算（`128` 手）与阶段 B 补算（`33,792` 手）`net_chips_exact=true`、`behavior_exact=true`；**观测工件自洽核对参与且通过**（`128` + `33,792` 手，工件存在） | 读数一致 |
| 信息边界 | **本轮取得直接读数**：`tests/test_info_boundary.py` → **`7 passed`**；工件层面仍无专门字段，如实标注为**契约测试口径**而非工件字段读数 | 读数通过（口径受限） |

L1 五项**均有读数**，未发现违反。

## 6. L2 成本（必要条件）

| 项 | 读数 | 解释性状态 |
|---|---|---|
| 硬预算 `DECISION_BUDGET_MS=100` | `decision_path` 超 `100 ms` **`0`**（样本 `80,000`）；`engine_apply` / `public_summary_update` / `bot_step` 各 `80,000` 亦均为 `0`；补充场景 `240` 样本为 `0` | 未超硬预算 |
| 指导值 | p50 `0.2767` / p95 `0.3598` / p99 `0.4818` / max `15.7714` ms | p95 <50、p99 <80 读数满足 |
| 逐人数分组 | 人数 2–9 共 `8` 组，各 `10,000` 样本，各组超时 `0`；p95 区间 `0.3417–0.3798` | 已逐人数报告，未以合并均值代替 |
| 样本量 | `80,000` ≥ 建议 `10,000` | 读数满足 |
| 口径要求 | 查询延迟与端到端决策延迟**分列**；`coverage.executed_nodes=125` 已如实标注为**清单库存**口径（`planned=128`、`not_run=0`、`structural=3`） | 满足 |
| 包络（**人工比对**，代码不据此改 `status`） | 阶段 B wall `500.907 s` / 峰值 RSS `465.64 MiB`；阶段 B 补算 wall `456.751 s` / 峰值 RSS `958.73 MiB`；保留文件合计 `73.74 MiB` | 未触及 `≤1800 s` / `≤1 GiB` / `≤100 MiB`；**补算路径无声明包络**，其 RSS 余量仅约 `6.37%` 且**未归因**（`R4(a)` 仅为**补声明**） |

## 7. L3 不可预测性（D7 六项，估值块 = `2992123152`）

| # | 判据（门槛） | v8 读数 | 解释性状态 |
|---|---|---|---|
| 1 | 实质动作混合覆盖率 **≥25%**，并分域报告翻前 / 面对下注 / 免费行动 | 整体 **`53.33%`**（`600/1,125`）；分域 翻前 `67.90%` / 面对下注 `47.62%` / 免费行动 `0.00%`；并列按 `125` 节点聚合 `68.00%`（`85/125`） | 读数高于门槛 |
| 2 | 平均类型熵 **≥0.15 bit** | `0.5333`（tight） / `0.5518`（aggressive） / `0.3856`（calling），各 `375` 分布 | 读数高于门槛 |
| 3 | 至少两档锚点**各 ≥15%**；金额条件熵 **≥0.5 bit** | 锚点满足率 **`246/264 = 93.18%`**；未满足的 **`18` 行恰为 `structural_single_size` 标记的结构性单档**（每风格 `6` 行）；金额条件熵（非 `None`，`n=264`）**`1.3090`**（分风格 `1.2506` / `1.4452` / `1.2110`） | 金额熵高于门槛；**锚点项两种读法分歧**，本篇**保守记为不满足** |
| 4 | **至少两档**在两个预先指定机会域上**各 ≥10 个百分点** | 域一 `t-a 18.57` / `t-c 18.57` / `a-c 0.00` pp ⇒ **`2/3` 对满足**；**域二 `t-a 4.51` / `t-c 1.66` / `a-c 2.85` pp ⇒ `0/3` 对满足** | **不满足** |
| 4b | 风格间 JS 距离 **≥0.15**（沿用，不重标定） | `0.2498`（tight–aggressive） / `0.3156`（tight–calling） / `0.1938`（aggressive–calling），各 `125` 节点 | 读数高于门槛 |
| 5 | 风格稳定性：**方向稳定** | `6/6` 个（配对 × 域）组合在 **`16/16` 块**同号 | 方向稳定（见下方结构性披露） |

**第 3 项的两读法与处置（如实记账）**：

- 读法甲（逐分布全满足）：`18 / 264` 个分布在低于 `15%` 的档位上只有一档 ⇒ **不满足**；
- 读法乙（退化行另报、非退化行满足）：`18` 行与 `structural_single_size` 完全重合，按 P/D7 原文「短码退化单档**另报**」的口径，非退化 `246/246` 全部满足 ⇒ 满足；
- 本篇按**保守处置**记为**不满足**并完整披露另一读法；**两种读法均不改变 L3 整体与总判定方向**（第 4 项与 L4 已独立不满足）。

**必须一并标注的四条**：

1. **D7 第 4 条**：第 4b 项最薄余量为 `aggressive–calling` 的 `0.1938`（余量 `+0.0438`）；按该条，**邻近阈值的达标不得表述为稳健达标**。
2. **域二的读数是决定性的**：三档风格在 `flop-draw`（翻牌面对下注）节点上的主动率分别为 `tight 3.46%` / `calling 5.12%` / `aggressive 7.97%`；按冻结定义（`docs/55a` §7.5 域二 = 主动概率）就是该口径。**不得**以「该域天然难有主动」为由在读数后更换域定义。
3. **`docs/55a` §7.3 + D7 第 5 条**：结构恒等节点**照实计入**；本次**未剔除**任何节点后再报告。
4. **D7 第 6 条**：不得以「门槛原为旧口径提出、可能过紧」为由在读数后放宽；该风险已在本判定前如实披露，属**已被接受的风险**。

**结构性披露（本轮新增，影响第 5 项的证据力）**：`1,125` 个直接分布及其 `scale_units` 与两个熵值在 **`16` 个种子块上逐位相同**（`0 / 1,125` 组随块变化）。因此：

- 第 5 项的「`16/16` 块同号」在本样本上是**结构性必然**，**不构成独立证据**；
- 第 1/2/3/4 项的「估值块取第 `1` 个主种子块」在本样本上**不产生差异**（口径仍按清单序执行，取值未改）；
- 该事实**只对本轮样本成立**，**不得**外推到其他身份或样本集。

**不得用于替代判定的相邻读数（防止换分母）**：`no_active_candidate` = `301 / 277 / 283`（各 `/375`）；`structural_single_size` = `6 / 6 / 6`。它们是回执字段，但**不是**覆盖率读数，**不得**用它替代第 1 项、也**不得**反向推断覆盖率。

## 8. L4 不劣化（D4，逐人数独立判定）

**统计量**：相对 `heuristic@1` 的最差族**配对**收益差 95% 区间**下界 ≥ −5 BB/100**；区间按 X1（块均值 t 区间，`t(0.975,15)=2.131`）；最差族按 X9（逐人数各自取）。

| 人数 | 最差族 | 均值 BB/100 | t95 下界 | 块 min | 块 max | 状态 |
|---|---|---|---|---|---|---|
| 2 | `random@1` | `-488.23` | `-1406.36` | `-4913.33` | `3341.67` | 未达门槛 |
| 3 | `position-pressure-probe` | `-1556.25` | `-3312.73` | `-7700.00` | `4095.56` | 未达门槛 |
| 4 | `fixed-pressure-probe` | `-1011.20` | `-1825.50` | `-3750.00` | `2404.17` | 未达门槛 |
| 5 | `marginal-call-pressure-probe` | `-1497.75` | `-3894.22` | `-12370.67` | `4607.33` | 未达门槛 |
| 6 | `marginal-call-pressure-probe` | `-1541.49` | `-3308.26` | `-9456.67` | `4102.22` | 未达门槛 |
| 7 | `marginal-call-pressure-probe` | `-880.80` | `-1769.47` | `-5855.24` | `1241.43` | 未达门槛 |
| 8 | `random@1` | `-935.62` | `-2974.32` | `-11130.42` | `6492.08` | 未达门槛 |
| 9 | `size-signal-probe` | `-652.13` | `-1209.67` | `-2726.67` | `579.63` | 未达门槛 |

**逐人数判定结果：`0 / 8` 人数达门槛**；且 **t 区间与块极值两口径均为 `0/8`**（口径选择不改变结论）。故 L4 记为**不满足**。

配对完整性：`16,896` 对，**配对异常 0**；缺 `seed_block` / `rotation` 的行 **`0`**；`33,792` 行全部为单手行，`bb_per_100 == net_chips × 10` **零不符**。

**双向报告（D4 第 3 条，不得只报有利一侧）**

| 人数 | 最大正偏离族 | 均值 |
|---|---|---|
| 2 | `heuristic@1` | `-43.54` |
| 3 | `fixed-pressure-probe` | `+177.78` |
| 4 | `position-pressure-probe` | `+236.98` |
| 5 | `position-pressure-probe` | `+562.58` |
| 6 | `aggression-rate-probe` | `+814.72` |
| 7 | `heuristic@1` | `+159.46` |
| 8 | `heuristic@1` | `-64.14` |
| 9 | `fixed-pressure-probe` | `+794.44` |

（人数 2 与 8 的「最大正偏离族」均值仍为负，说明该两档人数下 8 族均值**全为负**。）

**必须并列呈现的已知方向性风险（D4 第 5 条强制）**

| 来源 | 读数 |
|---|---|
| `docs/53c` §4 第 1 条（旧记录） | 旧启发式 arm 在 4 个主种子块中的 **3 个**高于新 Bot |
| 本次 v8 对应读数（跨人数合计，仅作报告） | `mixed-focus` 净筹码 `+1,472,108` 对 `legacy-focus` `+1,967,319` → 方向与旧记录**一致**；**`16` 块中 `4` 块** mixed 更高 |

**该读数不是强度证据**：机械探针不是 best response、逐格手数有限、`16` 个固定种子块即便给出区间也只是粗区间。它**只**说明方向不利于新 Bot，与 L4 的判定结果**并列**呈现，不得单独当作结论。

## 9. L5 抗针对（无数值门槛，属报告层）

### 9.1 逐族读数（第 1 项）

| 对手族 | `mixed-focus` 净筹码 | 均值 BB/100 | 块 min | 块 max |
|---|---|---|---|---|
| `aggression-rate-probe` | `+227,509` | `+1077.22` | `-365.23` | `+4772.50` |
| `fixed-pressure-probe` | `+241,291` | `+1142.48` | `-1855.68` | `+4833.33` |
| `heuristic@1` | `+21,429` | `+101.46` | `-416.06` | `+1245.38` |
| `marginal-call-pressure-probe` | `+300,232` | `+1421.55` | `-510.76` | `+5662.58` |
| `passive-call-probe` | `+200,260` | `+948.20` | `-231.36` | `+3545.45` |
| `position-pressure-probe` | `+208,327` | `+986.40` | `-424.55` | `+4177.80` |
| `random@1` | `+226,202` | `+1071.03` | `-1910.23` | `+3758.64` |
| `size-signal-probe` | `+46,858` | `+221.87` | `+14.85` | `+540.83` |

逐人数分解可由阶段 B 回执的 `matches.rows` 复算；本节跨人数汇总**仅作报告**，按 `docs/55` §10.4.2 第 3 条**不得**据此归因到具体人数。

### 9.2 针对性诊断（第 2 项）

**口径（X8，已用 v7 公布值逐行验证通过）**：短段 = 每（对手族 × 风格）内按（**清单种子序**、人数、轮换）排序的前 `20` 对（合计 `60` 对/族，`480` 对）；长段 = 其余 `2,052` 对/族（`16,416` 对）；增益 = 配对差 bb/100 的 `短段 − 长段`。

| 族 | 短段 bb/100 | 长段 bb/100 | 差（短 − 长） |
|---|---|---|---|
| `aggression-rate-probe` | `+2257.00` | `-100.51` | `+2357.51` |
| `fixed-pressure-probe` | `+1332.50` | `-172.00` | `+1504.50` |
| `heuristic@1` | `+339.50` | `-12.49` | `+351.99` |
| `marginal-call-pressure-probe` | `-598.17` | `-660.15` | `+61.98` |
| `passive-call-probe` | `+2003.83` | `-320.46` | `+2324.29` |
| `position-pressure-probe` | `+1713.83` | `-288.16` | `+2002.00` |
| `random@1` | `-268.67` | `-537.40` | `+268.73` |
| `size-signal-probe` | `-525.00` | `-505.04` | `-19.96` |

**尺度 / 牌力 / 街道分桶（X8b）——本轮**无法沿用 v7 的分类口径**（§4.4 (ii)）**。本篇改用**明示规则**并在此完整声明，以免口径暗换：

> 分类规则（本篇明示）：取 **`mixed-focus` 臂**在该手中**首次**面对 `bet`/`raise` 的事件；尺度 = 面对的额度 ÷ **街起始底池**（= 上一街结束底池；翻前 = `1.5 BB` = `15` 筹码），三档 `≤50%` / `50–100%` / `>100%`；牌力 = 焦点座位牌力档（两臂相同）；街道 = 该事件所在街。

| 维度 | 桶 | n（短/长） | 短段 | 长段 | 差 |
|---|---|---|---|---|---|
| 尺度 | `≤50%` | `33 / 1084` | `+1106.97` | `-683.35` | `+1790.32` |
| 尺度 | `50–100%` | `9 / 406` | `-5738.89` | `-653.62` | `-5085.27` |
| 尺度 | `>100%` | `96 / 3879` | `+664.58` | `-135.41` | `+799.99` |
| 尺度 | 无面对加注 | `342 / 11047` | `+955.00` | `-343.62` | `+1298.62` |
| 牌力 | `weak` | `276 / 9174` | `+1045.14` | `-203.77` | `+1248.91` |
| 牌力 | `medium` | `96 / 5300` | `-5.00` | `-332.45` | `+327.45` |
| 牌力 | `strong` | `70 / 871` | `+1188.43` | `-1808.91` | `+2997.34` |
| 牌力 | `None` | `38 / 1071` | `+108.42` | `-112.47` | `+220.90` |
| 街道 | `preflop` | `86 / 3430` | `+611.63` | `-301.14` | `+912.77` |
| 街道 | `flop` | `49 / 1757` | `-275.92` | `-287.54` | `+11.62` |
| 街道 | `turn` | `3 / 107` | `+3200.00` | `-18.32` | `+3218.32` |
| 街道 | `river` | `0 / 75` | — | `+115.87` | — |

**稀疏度（限制该诊断的可解释力）**：按本篇规则，有面对 `bet/raise` 的配对为 **`5,507 / 16,896 = 32.59%`**，整手无面对为 **`11,389 / 16,896 = 67.41%`**；`river` 短段样本为 `0`、`turn` 短段仅 `3` 对。

**口径缺陷披露（必须与读数并列）**：v7 公布的对应计数为「有面对 `1,345 / 16,896 = 7.96%`」，**与本篇规则的 `32.59%` 相差约 `4` 倍**，且其分类实现不可复原（§4.4 (ii)）。因此：

- 本节的**分桶读数不得与 v7 逐桶比较**，也不得跨轮合并；
- 本节读数**只能**支持「本次样本集与本次探针池下未观察到 X」的边界表述；
- **不得**据此作强度或剥削性结论，**更不得**表述为「探针随样本增长而变准」。

### 9.3 结论边界（D3 第 4 条）

因本次**不做** best response（D3 已裁定），L5 只能表述为「**本次样本集与本次探针池下未观察到 X**」；**不得**表述为不可剥削，**不得**作为 exploitability / NashConv / best response 的界。`shared-board` 的读数**只能**表述为「**读数存在、可利用性未检验**」。

## 10. 汇总判定

| 层 | 项数 | 满足 | 未满足 | 不可判定 |
|---|---|---|---|---|
| L1 机制与正确性 | 5 | 5 | 0 | **0** |
| L2 成本 | 6 | 6 | 0 | 0 |
| L3 不可预测性（D7） | 6 | 4（第 1、2、4b、5 项） | **2（第 3 项保守记、第 4 项）** | **0** |
| L4 不劣化（D4） | 1（逐人数 `8` 项） | 0 | **`8/8` 人数** | 0 |
| L5 抗针对 | 2（报告层） | 第 1 项已报告；第 2 项短/长段已报告 | 0（无数值门槛） | **0** |

**存在未满足项** ⇒ 按 **D7 第 3 条**（全部绑定 ⇒ 任一项不过即「质量未获支持」）、**D4 第 4 条**、**§10.4.3**：

> **本次结论：质量未获支持；默认策略保持 `heuristic@1`。**

**未满足的前置（如实记账）**：按 §10.4.2 第 4 条，「用更多 seed 估计判定反转频率」本次**未做**。因此本结论**不是**稳健达标的反面表述，而是**本次独立验证下的判定结果**。

## 11. 不支持的原因必须分开陈述

| 类别 | 本轮涉及项 | 性质 |
|---|---|---|
| **(a) 已计算且未达** | L4（`0/8`，两口径一致）；L3 第 4 项（域二 `0/3` 对，主动率三档在 `3.46%–7.97%`） | **读数明确低于门槛** |
| **(b) 在冻结口径下不可判定** | **无** | — |
| **(c) 口径缺陷（非判据，如实记账）** | L5 第 2 项的分桶分类实现不可复原（§4.4 (ii)）；L3 第 3 项的判定单元在冻结文本中未明示到「逐分布 vs 比例」层级 | **报告层缺陷**，不改变判定 |

**三条必须写明的限定**：

1. **(c) 类必须与 (a) 类分开陈述**：L5 第 2 项与第 3 项的读法分歧**既不是** L4 / L3 第 4 项未满足的原因，**也不得**被用来稀释它们的结论。
2. **不得**把 (a) 类说成「口径问题」或「工程问题」；**不得**把 L3 第 4 项的未满足说成「域定义偏严」。
3. 本轮**不存在**「无读数即记未满足」的项；v6 §11 中关于「不得用点估计声称达标」的处置**未被触发**，该纪律本身**继续有效**。

## 12. 用户签收

**签收原文（由用户出具，逐字记录）**：

> 「认可本次判定结果与「质量未获支持」结论，保持 `heuristic@1` 默认；认可 L3 第 3 项与 L5 第 2 项分桶存在口径分歧/缺陷的如实披露；同意不再就该冻结集合做任何补算、重跑或口径改换。」

**原文来源（如实披露）**：该原文由用户从本轮提供的**三个参考措辞**中选定「完整版」；**参考措辞由助手起草，选择与签收均由用户作出**。用户的答复原文为：

> 「完整版签收」（签收问项）与「授权」（提交问项）

**用户同时出具的表态（原话，范围见 §1）**：

> 「RSS 预算未来允许适当放宽。」

- 签收形式：由用户在参考措辞中选定（`docs/55g` §12 的同一形式）；
- 签收**不新增、不修改任何判据**（D2 §10.4.2 第 2 条）；
- 本节只记录**用户表态**，不改动 §10 的机械判定结果；
- 该 RSS 表态**未**在本轮改动任何数值，其适用范围以 §1 的限定为准。

## 13. 契约、API 与默认策略

| 对象 | 状态 |
|---|---|
| 公开 API 字段 / 数据库列 / `history_json` | **零改动** |
| `mixed-validation.v1` / `v2` / `mixed-observations.v1` / `mixed-recheck.v1` | **零改动** |
| 清单模型 / 冻结清单 | **零改动**（摘要逐字未变） |
| 新策略身份 / 新清单 | **均未新增**；被测身份仍为 `mixed-local@6` |
| `docs/37` §3.1 | **不触发** |
| 默认策略 | **仍为 `heuristic@1`**（本次未获质量支持，必须保持） |
| v1–v7 外部证据 | **零触碰**（目录文件数与关键 sha256 逐条复核未变） |
| v8 七个工件 | **零改动、零新增**（本篇不写工件） |
| 包络数值 | **零改动**（`≤1800 s` / `≤1 GiB` / `≤100 MiB` 维持原值） |

## 14. 未做事项与仍未证

- **未做**：best response 与任何 BR 构造（D3 已排除）；跨人数归因；用更多 seed 估计判定反转频率；任何参数、门槛或口径改动；任何重跑或补算；默认切换；RSS 包络的任何变更。
- **仍未证**：任何强度、可剥削性或不可剥削结论；跨机器与云端成本；总体（非 `16` 固定种子块）覆盖；L5 第 2 项的桶级结论（分数桶样本量 `3`–`96` 对，且分类口径已不可复原）。
- **即便判据全部满足**，结论也只能表述为「本次独立验证下判据满足」（§10.4.2 第 5 条）；本次判据未全满足，该限制不改变结论方向，但必须一并记录。
- **未消耗**：本轮**未消耗**任何运行 authorization，也未触发 `docs/37` §3.1；判定期间仅额外运行了一次定向契约测试（`test_info_boundary.py`）。
- **永久已消耗的 A9 授权**（`a9-calibration-i10` 等）一律**不得重试**，与本轮无关。

## 15. 不得声称

- 不得把本次部分达标读数（覆盖率 `53.33%`、类型熵 `0.5333`/`0.5518`/`0.3856`、锚点 `93.18%`、金额熵 `1.3090`、JS `0.2498`/`0.3156`/`0.1938`、稳定性 `16/16`）表述为质量已证明、稳健达标或「基本通过」。
- 不得把 L4 的「已计算未达」说成「口径问题」；不得把 L3 第 4 项的未满足说成「域定义偏严」。
- 不得说成「v8 比 v7 差」或「v8 比 v7 好」——两者建立在**不同节点集、不同种子集与不同策略身份**上，数值**不可直接比较**。
- 不得把 L5 的探针读数表述为不可剥削、exploitability 上界或 best response 结果；不得把 L5 第 2 项的极小样本桶当作发现；**不得**把本节分桶读数与 v7 逐桶比较。
- 不得把「方向稳定」说成独立证据（§7 结构性披露）。
- 不得把本次判定表述为「验证通过」「验收通过」或「已达生产门槛」。
- 不得把 `X3`–`X9` 表述为「v7 的预注册口径」；其**取值来源**的时点在 v7 读数之后（§4.2）。
- 不得删除或改写 v6 的失败阶段 A 回执，也不得把它说成「未发生过」。
- 不得把本次结论表述为对 `docs/53c`、`docs/55g` 或 `docs/56h` 的**反转**（本次是**再次维持**）。
- 不得把「RSS 预算未来允许适当放宽」说成已生效的包络变更，也不得据此追溯改写任何回执（§1）。
- 不得据此修改 `docs/20` 至 `docs/57j` 与任何已落盘工件；不得改写 `@1`–`@6` 任一身份的历史含义。

## 16. 本篇唯一下一步

判据闭环已完成，结论为**质量未获支持**，默认保持 `heuristic@1`。

按 D4 第 4 条 / §10.4.3，若需再试，必须**新开策略身份或新样本集并重新预注册**（全新清单、全新种子与**单独运行授权**）；**不得**改判据、**不得**重跑 v8 冻结集合、**不得**把同一集合反复调参后称为独立验证（`docs/53` §13.2）。

**建议的唯一下一步（需用户单独授权，本篇不自行执行）**：先就 L4 的负差做一轮**只读定向诊断**（只分析 v8 已落盘工件，不新增样本、不重跑、不改代码），把负配对差定位到人数 / 风格 / 对手族的哪些格子与哪些尺度 / 牌力 / 街道分桶，用于回答「是否值得新开身份重做」；诊断结论**不得**用于修改任何判据。

**附带提醒**：若同时要处理 §4.4 (ii) 的分桶口径缺陷，应先**只读**复原该口径的**唯一性**（或在下一轮预注册中把分类规则写死）；在口径未唯一化之前，任何分桶派生结论都**不得**作为判定输入。
