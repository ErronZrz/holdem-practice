# 55g M8：独立质量验证的判据判定与签收回执

> 日期：2026-09-22。
>
> 本文件接在 `docs/55f` 之后，沿用 `docs/30` §10.1 D6 的编号族约定。`docs/20` 至 `docs/55f` 未被改写。
>
> 本文件记录：按 `docs/55` §7 的 L1–L5 判据、在**已冻结样本**上作出的**判定读数**，以及由 D2（§10.4）绑定推出的**本次质量签收结论**。
>
> **措辞约束**：按 §7.5 第 1 条，判定只输出**解释性状态**，不使用「合格 / 不合格 / 通过 / 达标」。文中「满足 / 不满足」仅作为 D4（§10.6）与 D7（§10.7）原文所用的**绑定术语**出现。
>
> **本篇同时是披露文件**：它披露了一项此前未被记录的**实现层粒度限制**（§3、§11），该限制使 L3 的 4 项无法按预注册口径闭环。披露不等于改口径，也不允许事后据此补算并宣称达标（§11）。

## 1. 授权原文与边界

用户在本轮前一次裁定中的答复原文：

> 「第一件事：A；第二件：A；第四件：A；第五件：我已经 push。」

对应选项原文与含义：

| 编号 | 选项原文（要点） | 含义 |
|---|---|---|
| 第一件（本轮的授权） | 「A. 只用现有回执字段 + 内联确定性计算，不新增任何文件、不动代码」 | 判定所需的 L3/L4/L5 指标按此口径计算 |
| 第二件 | 「A. 由你给出一句签收原文，我逐字记录进结论文档」 | 签收原文由用户出具（§12） |
| 第四件 | 「A. 先做定向诊断，再决定是否重做」 | 结论为「质量未获支持」时的既定后续路线 |
| 第五件 | 「我已经 push」 | 推送由用户自行完成（§2 已核验） |

| 项 | 内容 |
|---|---|
| 被授权 | 按 `docs/55` §7 的 L1–L5 判据出具结论；计算只用**已落盘工件的现有字段** + **内联确定性计算** |
| 明确排除 | 修改任何判据、门槛、机会分母；重跑阶段 A/B 或补算；重跑同一冻结集合；新增文件或改动代码；默认策略的自主切换；把本次判定追溯写入任何已落盘回执 |
| 保留 | v6 的五份工件**全部原样保留**（含失败阶段 A 回执），本篇不新增也不改写任何工件 |

## 2. 只读基线核验（判定前）

```text
$ git status --short --branch
## master...origin/master

$ git rev-parse HEAD
1fa3373af7f655e9fdbb166b92a1830cc71ec32b

$ git rev-parse origin/master
1fa3373af7f655e9fdbb166b92a1830cc71ec32b
```

判定计算**开始前**工作区**零改动**；判定的全部计算均为**内联确定性计算**，未新增文件、未改动代码——本回执 `docs/55g` 是判定**之后**的记录文件，其中不含任何计算代码。

**判定输入的五个工件（sha256 全部与 `docs/55c`–`docs/55f` 记录一致）**

| 工件 | sha256（前 8 位） |
|---|---|
| 冻结清单 | `0cb1dad4` |
| 失败阶段 A 回执 | `86e84fed`（**原样保留，未删除未改写**） |
| 重跑阶段 A 回执 | `332607ca` |
| 阶段 B 回执 | `7ad4318b` |
| 补算回执 | `5597d221` |

## 3. 判定输入与计算口径（如实声明）

### 3.1 本次实际可用的字段粒度（关键事实）

| 字段族 | 实际粒度 | 数量 |
|---|---|---|
| 行为指标（`behavior.styles`） | **风格级聚合**（动作熵、额度熵、有效档位数、结构性单档、无活跃候选的三风格均值/计数） | `3` 条 |
| 行为指标（`behavior.categories`） | **类别级聚合**（16 类 × 3 风格；含类别内**均值**动作单位） | `48` 条 |
| 逐手行（`matches.rows`） | **逐手**（人数、风格、对手族、焦点臂、种子块、轮换、净筹码、`bb_per_100`） | `16,896` 行 = `8,448` 对配对 |
| 分布明细 | **未保存**（`behavior.direct_distributions` 只是计数 `1,125`） | — |

**由此推出的硬约束**：凡要求**逐节点/逐分布的条件分布**的判据，**无法从已落盘工件计算**；凡只需**逐手净筹码**的判据（配对差、逐族读数），可以计算。

### 3.2 计算口径与自洽核验

- 配对差 = 同一（人数、风格、对手族、种子块、轮换）下 `mixed-focus` 与 `legacy-focus` 的净筹码之差；
- 配对完整性：`8,448` 对，**配对异常 0**（每对恰含两臂各一行）；
- 币值换算：`16,896` 行全部为单手行，且 `bb_per_100 == net_chips × 10` **零不符** → `1 BB = 10 筹码` 在本样本上自洽；
- 判定结构：按 §10.4.2 第 3 条，**逐人数各自独立判定、禁止跨人数归因**（L4 在 §7 逐人数给出；跨人数数字仅作报告）。

## 4. L1 机制与正确性（必要条件）

| 项 | 读数 | 解释性状态 |
|---|---|---|
| 违规数 | 阶段 A 重跑 `violations=[]`、阶段 B `violations=[]` | 读数 0 |
| 截断数 | 阶段 B `truncated_hands=0`；补算聚合行截断合计 `0` | 读数 0 |
| 摘要一致 | `config_digest=8896013b…82f2`、`manifest_digest=381d205e…3ccf` 在阶段 B 与补算两处一致；补算不重算摘要 | 读数一致 |
| 随机可重放 | 补算 `net_chips_exact=true`、`behavior_exact=true`（`16,896` 手逐手一致） | 读数一致 |
| 信息边界 | **本次工件无专门字段**；由既有契约测试覆盖（`pytest -q` → `1122 passed, 3 skipped`），本次**未单独复核** | **未直接读数** |

L1 的四项有直接读数且均为零/一致；**信息边界项本次未直接读数**，如实标注为未复核，不据此声称已验证。

## 5. L2 成本（必要条件）

| 项 | 读数 | 解释性状态 |
|---|---|---|
| 硬预算 `DECISION_BUDGET_MS=100` | `decision_path` 超 `100 ms` 计数 **`0`**（样本 `80,000`）；`bot_step` / `engine_apply` / `public_summary_update` 亦均为 `0`；补充场景 `240` 样本为 `0` | 未超硬预算 |
| 分位 | `decision_path` p50 `0.276` / p95 `0.371` / p99 `0.540` / max `37.212` ms | 指导值 p95 <50、p99 <80 读数满足 |
| 逐人数分组 | 人数 2–9 共 `8` 组，各 `10,000` 样本，各组超时计数均 `0` | 已逐人数报告，未以合并均值代替 |
| 样本量 | `80,000` ≥ `10,000` 建议 | 读数满足 |
| 口径要求 | 查询延迟与端到端决策延迟**分列**；`coverage.executed_nodes=125` 已如实标注为**清单库存**口径 | 满足 |
| 包络 | 阶段 B wall `283.146 s` / 峰值 RSS `73.19 MiB`；补算 wall `234.209 s` / 峰值 RSS `89.92 MiB` | 未触及 ≤1800 s / ≤1 GiB / ≤100 MiB |

## 6. L3 不可预测性（D7 六项，逐项）

| # | 判据（门槛） | 可用读数 | 解释性状态 |
|---|---|---|---|
| 1 | 实质动作混合覆盖率 **≥25%**，并分域报告翻前 / 面对下注 / 免费行动 | **无逐节点分布**（回执仅有类别级与风格级**均值**） | **未满足（不可判定）**：按冻结分母（非强制节点）不可计算 |
| 2 | 平均类型熵 **≥0.15 bit** | `0.3485` / `0.4345` / `0.3716` bit（三风格各 `375` 个分布的动作熵均值） | 读数高于门槛 |
| 3 | 额度多样性：至少两档锚点**各 ≥15%**；金额条件熵 **≥0.5 bit** | 金额条件熵（`scale_entropy` 均值）`1.1974` / `1.4336` / `1.2391` bit → 该半项读数高于门槛；**「两档锚点各 ≥15%」回执无占比字段** | **未满足（不可判定）**：两项中有一项无读数 |
| 4 | 风格可分度：**至少两档**在两个预先指定机会域上**各 ≥10 个百分点** | **无逐节点条件分布**（域定义在节点级，见 `docs/55a` §7.5） | **未满足（不可判定）** |
| 4b | 风格间 JS 距离 **≥0.15**（新节点集沿用，不重标定） | 均值 `0.1871`（tight–aggressive）/ `0.3004`（tight–calling）/ `0.1627`（aggressive–calling） | 读数高于门槛（三对均≥0.15） |
| 5 | 风格稳定性：**方向稳定** | 需逐种子块的**风格倾向分布**，回执无 | **未满足（不可判定）** |

**必须一并标注的三条**：

1. **D7 第 4 条**：第 4b 项最薄余量为 `aggressive–calling` 的 `0.1627`（余量 `+0.0127`）；按该条，**邻近阈值的达标不得表述为稳健达标**。第 2 项同理，不因读数明显高于门槛而声称稳健。
2. **`docs/55a` §7.3 + D7 第 5 条**：结构恒等节点**照实计入**；本次**未剔除**任何节点后再报告。
3. **D7 第 6 条**：不得以「门槛原为旧口径提出、可能过紧」为由在读数后放宽；该风险已在 `docs/55a` §10 与本判定前如实披露，属**已被接受的风险**。

**不得用于替代判定的相邻读数（防止换分母）**：`no_active_candidate` = `308 / 295 / 299`（各 / `375`，语义为「该分布内**无任何合法 BET/RAISE 尺寸**」）。它是回执字段，但**不是**覆盖率读数，**不得**用它替代第 1 项、也**不得**反向推断覆盖率。

## 7. L4 不劣化（D4，逐人数独立判定）

**统计量**：相对 `heuristic@1` 的最差族**配对**收益差 95% 区间**下界 ≥ −5 BB/100**；`legacy-focus` 的焦点座位由旧启发式驱动，故两臂配对差即被测身份相对 `heuristic@1` 的差。

**区间方法的口径空白（如实披露）**：`docs/55` §7.7 只要求「报告 8 个种子块的**粗区间**」，**未固定区间方法**。本次**同时**给出两种口径，结论在两种口径下一致，故不因方法选择而改变：

- 口径一：块均值 t 区间（`t_{0.975,7}=2.3646`）；
- 口径二：块极值区间（8 个块的最小值即下界）。

| 人数 | 最差族 | 均值 BB/100 | t 95% 下界 | 块 min | 块 max | 状态 |
|---|---|---|---|---|---|---|
| 2 | `fixed-pressure-probe` | `-715.63` | `-1785.42` | `-2866.67` | `1158.33` | 未达门槛 |
| 3 | `fixed-pressure-probe` | `-875.00` | `-2114.57` | `-3216.67` | `527.78` | 未达门槛 |
| 4 | `marginal-call-pressure-probe` | `-1760.63` | `-5187.26` | `-7329.17` | `4748.33` | 未达门槛 |
| 5 | `marginal-call-pressure-probe` | `-3720.58` | `-7824.26` | `-12011.33` | `2700.00` | 未达门槛 |
| 6 | `aggression-rate-probe` | `-1040.63` | `-3432.72` | `-8116.67` | `136.11` | 未达门槛 |
| 7 | `marginal-call-pressure-probe` | `-1690.77` | `-3750.80` | `-5633.33` | `1046.19` | 未达门槛 |
| 8 | `aggression-rate-probe` | `-3640.05` | `-9575.23` | `-17257.92` | `541.67` | 未达门槛 |
| 9 | `aggression-rate-probe` | `-788.66` | `-3039.66` | `-5444.44` | `1911.11` | 未达门槛 |

**逐人数判定结果：`8 / 8` 人数未达门槛**，且两种区间口径下界**均远低于 `−5 BB/100`**。故 L4 记为**不满足**。

**双向报告（D4 第 3 条，不得只报有利一侧）**

| 人数 | 最小负偏离族 | 均值 | 最大正偏离族 | 均值 |
|---|---|---|---|---|
| 2 | `fixed-pressure-probe` | `-715.62` | `position-pressure-probe` | `+415.83` |
| 3 | `fixed-pressure-probe` | `-875.00` | `marginal-call-pressure-probe` | `+1113.75` |
| 4 | `marginal-call-pressure-probe` | `-1760.62` | `random@1` | `+2017.81` |
| 5 | `marginal-call-pressure-probe` | `-3720.58` | `fixed-pressure-probe` | `+397.92` |
| 6 | `aggression-rate-probe` | `-1040.62` | `random@1` | `+349.72` |
| 7 | `marginal-call-pressure-probe` | `-1690.77` | `random@1` | `+515.71` |
| 8 | `aggression-rate-probe` | `-3640.05` | `heuristic@1` | `-447.92` |
| 9 | `aggression-rate-probe` | `-788.66` | `passive-call-probe` | `+253.01` |

**必须并列呈现的已知方向性风险（D4 第 5 条强制）**

| 来源 | 读数 |
|---|---|
| `docs/53c` §4 第 1 条（旧记录） | 旧启发式 arm 在 4 个主种子块中的 **3 个**高于新 Bot；总计 新 `+47,007` 对旧 `+136,878` |
| 本次 v6 对应读数（跨人数合计，仅作报告） | `mixed-focus` 净筹码 `+322,754` 对 `legacy-focus` `+916,464`（各 `8,448` 手） → 方向与旧记录**一致** |
| 跨人数最差族（仅作报告，不作判定） | `marginal-call-pressure-probe`，配对差 `-150,410` 筹码 |

**该读数不是强度证据**：机械探针不是 best response、逐格手数有限、8 个固定种子块只给粗区间。它**只**说明方向不利于新 Bot，与 L4 的判定结果**并列**呈现，不得单独当作结论。

## 8. L5 抗针对（无数值门槛，属报告层）

### 8.1 逐族读数（第 1 项，可计算）

| 对手族 | `mixed-focus` 净筹码 | `mixed-focus` 均值 BB/100 | 块 min | 块 max | `legacy-focus` 均值 BB/100 |
|---|---|---|---|---|---|
| `aggression-rate-probe` | `+3,223` | `+30.52` | `-104.92` | `+204.85` | `+1405.40` |
| `fixed-pressure-probe` | `+73,945` | `+700.24` | `-897.73` | `+2836.74` | `+1142.90` |
| `heuristic@1` | `-8,109` | `-76.79` | `-425.30` | `+143.64` | `+51.73` |
| `marginal-call-pressure-probe` | `+95,806` | `+907.25` | `-351.67` | `+2067.58` | `+2331.59` |
| `passive-call-probe` | `+4,484` | `+42.46` | `-81.29` | `+196.67` | `+885.57` |
| `position-pressure-probe` | `+8,679` | `+82.19` | `-233.18` | `+702.27` | `+740.82` |
| `random@1` | `+137,442` | `+1301.53` | `+105.45` | `+2784.70` | `+1533.30` |
| `size-signal-probe` | `+7,284` | `+68.98` | `-75.68` | `+306.89` | `+587.33` |

> 逐人数分解可由阶段 B 回执的 `matches.rows` 复算；本节跨人数汇总**仅作报告**，按 §10.4.2 第 3 条**不得**据此归因到具体人数。

### 8.2 针对性诊断（第 2 项，**不可计算**）

`docs/55a` §7.8 第 2 项要求「前 20 手与更长样本后的增益对比，按**尺度 / 牌力 / 街道**分桶分别报告」。逐手行字段只有人数、风格、对手族、焦点臂、种子块、轮换、净筹码、`bb_per_100`——**无尺度、无牌力、无街道**，且「前 20 手」在冻结排期中没有对应分桶字段。故该项**未满足（不可判定）**。

### 8.3 结论边界（D3 第 4 条）

因本次**不做** best response（D3 已裁定），L5 只能表述为「**本次样本集与本次探针池下未观察到 X**」；**不得**表述为不可剥削，**不得**作为 exploitability / NashConv / best response 的界。

## 9. 汇总判定

| 层 | 项数 | 读数高于门槛 | 未达门槛 | 未直接读数 |
|---|---|---|---|---|
| L1 机制与正确性 | 5 | 4 | 0 | 信息边界（1） |
| L2 成本 | 6 | 6 | 0 | 0 |
| L3 不可预测性（D7） | 6 | 2（类型熵、JS） | **4（覆盖率、额度多样性、可分度、稳定性，均不可判定）** | 0 |
| L4 不劣化（D4） | 1（逐人数 8 项） | 0 | **8 / 8 人数** | 0 |
| L5 抗针对 | 2（报告层） | 第 1 项已报告 | **第 2 项不可判定** | 0 |

**存在未满足项** ⇒ 按 **D7 第 3 条**（「全部绑定 ⇒ 任一项不过，本次结论即为『质量未获支持』」）、**D4 第 4 条**、**§10.4.3**：

> **本次结论：质量未获支持；默认策略保持 `heuristic@1`。**

## 10. 结论与对称处置

1. `docs/53c` 的「质量结论未获支持」**未被反转**，本次为**再次维持**；`docs/53c` 的既有签收结论**未被改写**。
2. 默认策略**保持 `heuristic@1`**（§10.4.3）。这是判据未全满足的**自动后果**，不需要也不允许另行选择。
3. 按 §10.4.3 第 2 条：**不得**改判据、**不得**重跑同一冻结集合、**不得**把同一集合反复调参后称为独立验证；**不得**按结果换 seed、换机会分母、删样本。
4. 按 §10.4.2 第 5 条：即便有部分项读数高于门槛，本次也**不得**表述为稳健达标、质量已证明或不可剥削。
5. 若需再试，须**新开身份或新样本集并重新预注册**，且运行需**单独授权**（§10.4.3 第 3 条）。

## 11. 不支持的两类原因必须分开陈述（本篇最重要的如实披露）

| 类别 | 涉及项 | 性质 |
|---|---|---|
| **(a) 已计算且未达** | L4（`8/8` 人数；两种区间口径下界均远低于 `−5 BB/100`） | **读数明确低于门槛**；且 8 族配对差方向一致为负（§7） |
| **(b) 在冻结口径下不可判定** | L3 第 1 / 3 / 4 / 5 项；L5 第 2 项 | 原因是**已落盘工件的粒度不含逐节点分布与逐手尺度/牌力/街道字段**；按 §7.5 第 4 条，无读数时**不得**用点估计声称达标 → 记为未满足 |

**两条必须写明的限定**：

1. **「不可判定」不等于「已证不合格」**。本篇不把 (b) 类项表述为已失败，也不把 (a) 类项表述为「只是口径问题」。二者不得互相顶替。
2. **粒度限制是本次新增披露的实现层事实，不是改口径**。若要在**同一冻结集合**上补齐这些读数，必须改变报告粒度并**重新运行**——那已属 §10.4.3 第 2 条禁止的「重跑同一集合」；正确路径是新开身份/新样本集并**重新预注册**。**禁止**在读到本次结果后用更细的新数据回头宣称本次判据达标（那正是 `docs/20` §6.1 第 1 条禁止的「看到有利结果后更换分母」）。

**附：本次判定的三项方法论限制（不得回避）**

- 区间方法在预注册中未固定（§7 已披露），本次以两种口径并列，结论不因选择而变；
- 8 个固定种子块属小固定种子集，`interval_status` 仍为 `not-estimated-small-fixed-seed-set`，**不构成总体覆盖承诺**；
- 「用更多 seed 估计判定反转频率」本次**未做**（`docs/39` §3.7 的同一精神），因此**即便判据全部满足**，结论也只能表述为「本次独立验证下判据满足」（§10.4.2 第 5 条）——本次判据未全满足，该限制不改变结论方向，但必须一并记录。

## 12. 用户签收

**签收原文（由用户出具，逐字记录）**：

> 「认可本次判定结果与「质量未获支持」结论，保持 `heuristic@1` 默认；同意按第四件事的路线 A 先做只读定向诊断。」

**原文来源（如实披露）**：该原文由用户从本轮提供的**三个参考措辞**中选定 A 项；**参考措辞由助手起草，选择与签收均由用户作出**。用户的答复原文为：

> 「A.」

- 签收形式：第二件选项 A（由用户给出一句签收原文，逐字记录进本文件）；
- 签收**不新增、不修改任何判据**（D2 §10.4.2 第 2 条）；
- 本节只记录**用户表态**，不改动 §9 的机械判定结果。

## 13. 契约、API 与默认策略

| 对象 | 状态 |
|---|---|
| 公开 API 字段 / 数据库列 / `history_json` | **零改动** |
| `experiment` / `measurement` / 候选 A artifact schema | **零改动** |
| v6 五份工件 | **零改动、零新增**（本篇不写工件） |
| 清单文件（v6） | **零改动** |
| 新策略身份 / 新清单 | **均未新增**；被测身份仍为 `mixed-local@5` |
| `docs/37` §3.1 | **不触发**（未触碰参考实现、保守判据、`equity` 实现、采样数或固定种子） |
| 默认策略 | **仍为 `heuristic@1`** |
| v1–v5 外部证据 | **零触碰** |

## 14. 未做事项与仍未证

- **未做**：best response 与任何 BR 构造（D3 已排除）；跨人数归因；用更多 seed 估计判定反转频率；任何参数、门槛或口径改动；任何重跑；默认切换。
- **仍未证**：L3 第 1 / 3 / 4 / 5 项与 L5 第 2 项（不可判定）；任何强度、可剥削性或不可剥削结论；跨机器与云端成本；总体（非 8 固定种子块）覆盖。
- **未消耗**：本轮**未消耗**任何 authorization，也未触发 `docs/37` §3.1。
- **永久已消耗的 A9 授权**（`a9-calibration-i10` 等）一律**不得重试**，与本轮无关。

## 15. 不得声称

- 不得把本次部分达标读数（类型熵 `0.3485`/`0.4345`/`0.3716`、JS `0.1871`/`0.3004`/`0.1627`、金额条件熵 `1.1974`/`1.4336`/`1.2391`）表述为质量已证明、稳健达标或「基本通过」。
- 不得把 (b) 类「不可判定」说成「已证不合格」，也不得把 (a) 类「已计算未达」说成「口径/工程问题」。
- 不得把本次判定表述为「验证通过」「验收通过」或「已达生产门槛」。
- 不得把 L5 的探针读数表述为不可剥削、exploitability 上界或 best response 结果。
- 不得删除或改写 v6 的失败阶段 A 回执，也不得把它说成「未发生过」。
- 不得把本次结论表述为对 `docs/53c` 的**反转**（本次是**维持**）。
- 不得据此修改 `docs/20` 至 `docs/55f` 与任何已落盘工件；不得改写 `@1`–`@5` 任一身份的历史含义。

## 16. 本篇唯一下一步

判据闭环已完成，结论为**质量未获支持**，默认保持 `heuristic@1`。用户已在第四件选定路线 **A：先做定向诊断，再决定是否重做**。

**建议的唯一下一步（需用户单独授权，本篇不自行执行）**：**授权一轮只读定向诊断**——

1. **范围**：只分析**已落盘工件**（阶段 B / 补算回执的 `16,896` 逐手行与聚合字段），**不新增样本、不重跑、不改代码、不产出新证据**；
2. **对象**：把 L4 的负配对差定位到人数 / 风格 / 对手族的哪些格子，并按 `docs/53c` §4 的疑似弱点（共享公共大牌上不主动）做只读复核；
3. **禁止**：以诊断为名调参、改门槛、重跑冻结集合，或把诊断结论当作质量证据；
4. **产出**：诊断结论只能用于回答「是否值得新开身份重做」，**不得**用于修改任何判据。

诊断完成后，再由用户裁定是否**新开身份 + 新样本集 + 重新预注册**（该路径需全新清单、全新种子与单独运行授权）。
